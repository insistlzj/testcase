# 原型规范

本目录按项目根目录 `AGENTS.md` 的 HTML 原型规范执行。页面采用 390 × 844 逻辑尺寸，使用本地共享资源，无网络请求；页面树、批注、Mock 与页面文件同步维护。配置维护类列表的主操作直接使用「编辑」，进入独立页面的可编辑状态，不使用「详情 → 编辑」二次进入或编辑弹窗。管理后台新建页采用单列表单，字段名与输入控件在同一行；基础信息在顶部，上传区域在其后，业务配置与表格放最后；多行内容的字段名与第一行内容顶部对齐。

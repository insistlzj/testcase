# 项目档案

> 档案版本：1.0
>
> 档案状态：AI 自动生成草稿
>
> 维护模式：首次自动生成；存在后不自动覆盖
>
> 生成原则：有证据才填写，无法确认则标记“待人工确认”

## 1. 检查快照

- 生成日期：2026-08-26
- 项目名称：Luma Live 原型（liveshow-proto）
- 项目根目录：`D:\testcase_html\liveshow-proto`
- Git 分支：`main`
- Commit ID：`a8f883348e220256822818fce744b7c09d3b6f74`
- 工作区状态：生成前干净；生成后仅新增 `.agents` 项目知识文件
- 操作系统：Windows / PowerShell

## 2. 项目用途

- 主要用途：面向印尼市场的 Luma Live 三端高保真、可点击、离线静态产品原型。
- 核心用户：普通用户、主播、公会运营人员、管理后台人员。
- 核心业务流程：注册与资料补全、观看与开播、社交互动、公会入退会、主播运营、充值消费、收益分成与提现、后台审核处置与数据分析。
- 业务重要性：原型与批注被项目规则定义为开发和测试理解现行需求的主要依据。

## 3. 技术与结构

- 编程语言：HTML、CSS、JavaScript；另有 Node.js 导出脚本。
- 主要框架：纯静态页面；页面本地引入 Tailwind 浏览器脚本及项目公共脚本，不使用前端路由框架。
- 包管理或构建工具：未发现 `package.json` 或锁文件；原型本身零构建运行。
- 启动入口：`prototype/index.html`。
- 业务页面目录：`prototype/pages/user/`、`prototype/pages/guild/`、`prototype/pages/admin/`。
- 配置和公共资源：`prototype/assets/`。
- 测试目录：未发现自动化测试目录或测试配置。
- 数据模型或数据库目录：无数据库；共享虚构数据在 `prototype/assets/mock.js`、`prototype/assets/admin-mock.js`。
- 生成代码、第三方代码和构建产物目录：待人工确认；`app-store-screenshots/` 为展示素材目录，`export-admin-prototype.mjs` 为离线导出脚本。

## 4. 已确认的常用命令

只能记录项目文档、脚本或配置中明确存在的命令，不得凭经验补全。

- 安装依赖：不需要；项目规则要求静态原型离线可打开。
- 启动项目：使用浏览器打开 `prototype/index.html`。
- 单元测试：未发现。
- 集成或接口测试：未发现。
- lint 或静态检查：未发现。
- 构建：不需要。
- 可选导出脚本：`node export-admin-prototype.mjs`；本次未执行，脚本输出位置和页面清单需执行前人工复核。

## 5. 业务、状态与权限

- 用户角色：普通用户、主播、公会运营账号、管理后台账号。
- 身份认证方式：原型模拟第三方、手机号、邮箱登录；不发起真实认证请求。
- 功能权限规则：用户与主播共用用户 App；加入公会并通过平台认证后获得主播身份和直播权限。主播中心、公会端和管理后台操作按角色区分，详细规则见 `context/` 和原型批注。
- 数据权限或租户隔离：公会人员仅应访问所属公会数据的具体实现口径待人工确认；静态原型不提供真实后端隔离证明。
- 关键状态及转换：主播申请与认证、入退会审核、直播中与结束、内容审核与举报处置、订单与退款、分成与提现、资料风控审核。
- 金额、数量、时间等关键规则：主播有效天为单个自然日累计直播不少于 3 小时，每自然日最多计 1 天；虚拟金币不计入真实收益和分账；其他公式以 `context/`、页面批注及 Mock 口径为准。
- 不允许被破坏的数据约束：原型页面、`prototype/index.html` 登记、批注和共享 Mock 应同步；同一业务对象在多页面的字段与状态应保持一致。

## 6. 数据与外部依赖

- 数据库：无。
- 缓存：无业务缓存；查看器使用浏览器本地存储保存查看偏好。
- 消息队列或异步任务：无。
- 文件或对象存储：无真实存储；素材均为仓库内静态文件。
- 第三方接口：无真实调用。
- 定时任务：无。
- 可能产生真实外部副作用的操作：原型操作本身无外部副作用；Git 提交、推送及导出覆盖外部文件不在普通分析授权范围内。

## 7. 环境与安全边界

- 允许使用的测试环境：本地静态浏览器环境。
- 允许使用的测试账号：原型中的虚构账号和 Mock 数据。
- 测试数据库或测试数据：仅仓库内 Mock 数据。
- 禁止连接的环境：未获授权的真实业务环境。
- 禁止执行的命令：未经用户明确授权不得执行远程 Git/GitHub 修改。
- 敏感数据类型：真实手机号、账号、收款账户、身份认证资料和财务数据不得写入 Mock 或文档。
- 运行测试可能产生的副作用：静态浏览仅产生本地浏览器状态；导出脚本可能写入项目父目录下的目标文件，执行前需核对。

## 8. 检查重点

- 核心入口：`prototype/index.html`、`prototype/assets/annotations.js`、`prototype/assets/mock.js`、`prototype/assets/admin-mock.js`。
- 高风险模块：账号与权限、主播认证与直播权限、举报与内容审核、公会入退会、充值消费与退款、分成提现、数据报表口径。
- 已有测试：未发现自动化测试。
- 明显测试覆盖空白：缺少页面树完整性、跨页链接、批注登记、状态视图、业务公式和跨端状态一致性的自动校验。
- 默认排除目录：`.git/`；分析业务页面时可排除 `app-store-screenshots/` 和历史沟通文本。
- 已知但本次不处理的问题：`prototype/pages/user/profile/my-outfits.html` 未登记页面树；`admin-monthly-host-earnings.html` 已登记且可由报表中心进入，但未归入 `adminPageGroups` 的数据分析模块清单；专属原型说明仍引用 3 个磁盘中不存在的旧提现审核页面。

## 9. 证据索引

| 事实 | 证据文件 | 说明 |
|---|---|---|
| 产品包含三端 | `项目说明.md` | 明确列出用户/主播 App、公会 App、管理后台 Web |
| 查看器和全部页面登记 | `prototype/index.html` | 定义三端页面数组、模块分组和页面路径 |
| 用户与主播共用 App | `context/01-用户主播App-页面架构.md` | 主播页面从“我的”进入，不设置独立底部导航 |
| 原型是纯静态且禁止真实网络请求 | `AGENTS.md` | 定义零构建、离线和交互边界 |
| 业务批注集中维护 | `prototype/assets/annotations.js` | 页面级业务、交互、权限与边界说明 |
| Mock 数据集中维护 | `prototype/assets/mock.js`、`prototype/assets/admin-mock.js` | 用户/公会端和后台的虚构数据 |

## 10. 待人工确认

- [ ] `my-outfits.html` 是否为应删除的旧版“我的装扮”页面，还是需要补回页面树。
- [ ] “主播礼物打赏明细报表”是否应正式加入管理后台“数据分析”模块树。
- [ ] 原型说明中的公会提现审核、后台提现审核及详情是否为应删除的历史条目，还是尚未落地的计划页面。
- [ ] `我的银行卡【待定】` 的产品去留及提现链路最终方案。
- [ ] 公会数据权限、管理后台角色权限和真实后端接口口径。
- [ ] 分成、提现、退款红冲/冻结的最终责任方与完整状态机。
- [ ] 自动化校验和回归测试的工具与执行标准。

## 11. 档案维护规则

- 本档案用于快速理解原型，不是需求文档的替代品。
- “待人工确认”内容不得作为确认业务缺陷的唯一依据。
- 自动初始化后不自动覆盖；代码变化导致档案过期时，应先报告差异。
- 人工确认内容时，应尽量注明依据、负责人或文档位置。
- 不得在本档案中保存秘密凭据或完整个人数据。

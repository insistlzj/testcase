# 原型与需求清单同步指令

## 1. 目的

在生成测试用例前，自动核对项目系统概要、`prototype/` 与 `context/` 派生需求清单，解决业务规则或原型变化后遗漏维护需求清单的问题。对声明为 `prototype-primary` 的项目，已登记的系统概要是最高业务规则来源，审核通过的原型与批注是页面、入口和交互来源，需求清单是可追溯、可回退的派生文档。

本指令只负责同步需求事实，不负责生成测试用例。同步通过后，才使用 `Cem Kaner.txt` 的输入与 JSON 输出模板，并按照 `AGENTS.md` 的当前规则生成用例。

## 2. 自动触发

当用户要求生成、重写、补充或审查测试用例时，先按 `新项目首次接入指令.md` 判断项目是否已经登记或符合旧项目兼容依据，再查找项目根目录 `需求来源策略.json`：

- `prototype-primary`：自动执行完整同步，不需要用户重复发送同步指令。
- `requirement-primary`：比较来源但不自动修改需求清单；冲突进入“需求待确认”。
- `conflict-review`：比较来源但不选择覆盖方；冲突进入“需求待确认”。
- 已登记旧项目没有配置：按 `conflict-review` 处理，不自行假定原型优先。
- 新项目没有配置：先生成来源策略草稿并等待用户确认，不进入本同步流程，也不生成测试用例。

只有项目配置明确列出的派生需求清单允许自动修改。该清单是写入白名单，不是读取白名单；配置的公共证据、全部现存 `context/` 文件、项目说明引用和扫描发现的关联来源仍须按全局扫描规则读取。公共证据、历史只读来源、旧 Excel、沟通记录和已有测试用例不得被自动覆盖，除非同一文件也被明确列入派生需求清单。

## 3. 同步步骤

### 3.1 确定范围

1. 根据用户指定的端和模块定位相关原型页面、页面树节点和派生需求文件。
2. 同时纳入系统概要、业务对象、角色权限、页面架构、全局批注、公共 Mock、共享组件、页面状态和该模块的跨端、跨模块依赖；不得因文件未列入派生需求清单而跳过读取。
3. 首次同步、策略变化、证据原子或映射失效时执行全量扫描；其他情况按“文件 -> 页面或章节 -> 模块 -> 角色 -> 动作 -> 稳定规则标识”的依赖索引执行增量同步，只重读变化文件及其依赖闭包。
4. 发现关联模块后自动扩大范围，不得只读取单个截图或单页静态文本。

### 3.2 固定同步来源基线

同步开始时记录系统概要及范围内每个原型、批注文件的：

- 项目内相对路径。
- 文件修改时间。
- SHA-256。

比较、修改需求和生成用例必须使用同一基线。同步期间系统概要、原型或批注中任一相关文件的修改时间或 SHA-256 变化，本次结果立即失效，重新读取后再同步。

### 3.3 建立需求映射

建立以下稳定关系：

```text
系统概要规则 / 原型文件 / 页面节点 / 结构化批注 -> 端 -> 模块 -> 功能结构 -> context 文件 -> 章节
```

落入需求清单前，再把确定内容转换为 `稳定规则标识 -> 角色或身份 -> 可执行动作 -> 必要状态 -> 可观察结果 -> 证据位置` 的原子映射。同一来源句包含多个角色、多个互斥动作或多个独立结果时先拆分；稳定规则标识由归一化业务键生成或沿用，不依赖段落和数组顺序。

页面名称相似不能单独证明属于同一功能。需要结合页面树、跳转、批注、共享业务对象和需求章节判断。无法唯一定位时记录为“无法定位”的阻塞异常，不自动选择最相似章节。

### 3.4 比较并分类

每项差异只能属于以下一种：

- `新增`：系统概要、原型页面或结构化批注有明确功能或规则，派生需求没有。
- `修改`：派生需求与系统概要、原型或批注描述同一功能但内容不同；系统概要覆盖该事项时以系统概要为准，系统概要未覆盖时采用明确的原型或批注。
- `明确删除`：来源明确说明删除、取消、下线、废弃或不支持；或者完成规则缺失复核后，确认该条目只存在于派生需求清单，在系统概要、全部关联原型页面和结构化批注中均不存在。
- `原型未覆盖`：系统概要已明确业务规则，但原型没有表达目标端入口或观察方式；保留业务规则，不据此虚构目标端操作。
- `来源冲突`：系统概要未覆盖该事项，原型内部、批注之间或与用户确认存在无法直接解决的冲突。
- `无法定位`：不能唯一确定应修改的需求文件或章节。

不得把“单个页面没有出现”“静态截图没有展示”或“只搜索一个关键词没有命中”直接认定为 `明确删除`。孤立条目删除前必须完成 `AGENTS.md` 第 2.3.1 节的同义词、全关联来源和上下文复核。

页面初始化的默认展示值、占位值或 Mock 记录中的示例数据，与同一页面批注明确写明的默认规则、筛选规则、状态含义或计算口径不一致时，以批注为准。该差异不归入 `来源冲突`，应按批注同步需求，并在同步结果中记录批注位置和被覆盖的页面值或 Mock 示例。

### 3.5 自动修改白名单

有直接系统概要、原型或批注证据时，可以自动同步：

- 页面结构、页面名称和功能入口。
- 按钮、字段、选项、标签和明确展示文案。
- 原型真实实现的弹窗、抽屉、Tab、跳转和状态切换。
- 页面状态注册中可以复现的空态、禁用态、错误态和角色视角。
- 原型批注明确说明的交互、权限、边界和操作结果。
- 系统概要明确表达的角色、关系、权限、状态、金额、公式、统计、跨端结果和历史数据规则。
- Mock 明确表达的字段名称、枚举和页面间共享对象口径；Mock 示例值不得覆盖系统概要或批注明确规则。

每次只修改与差异直接相关的最小文本，不顺带调整无关标题、排序、空格或整份文件格式。

### 3.6 自动修改保护

- 系统概要与原型、批注或派生需求冲突时，以系统概要为准；同步结果同时记录被覆盖来源及位置。
- 仅派生需求清单中的孤立条目，必须在系统概要、全部关联原型和结构化批注均无命中后才可删除。删除前把完整原文、检索对象、检索词、已查文件、未采用命中和回退方法写入 `需求清单变更日志.md`。
- 系统概要明确但原型未展示的权限、金额、公式、统计、异步时效、异常恢复、跨端结果、历史数据和兼容规则继续保留；不能因此创建原型不存在的目标端入口。
- 系统概要未覆盖且原型或批注明确否定旧规则但没有给出完整替代规则时，不自动改成新的确定性规则，记录为“来源冲突”并进入“需求待确认”。
- 原型默认展示值、占位值或 Mock 示例数据与明确批注不一致时，按第 3.4 节采用批注，不阻断同步。
- 除上述例外外，原型内部的说明、批注、真实可执行交互、字段结构、枚举、数据类型、状态切换和操作结果相互冲突时，不自行挑选；能够依据明确版本或替代关系解决时记录依据，能够定位到具体规则时局部隔离并进入“需求待确认”，只有核心冲突无法限定影响范围时才阻断同步批次。
- 历史只读来源和历史 Excel 不修改、不删除。

### 3.7 写入需求清单和日志

修改前再次确认原型基线未变化。修改完成后，在根目录唯一 `需求清单变更日志.md` 追加连续记录，至少包括：

- 变更编号、时间、项目和来源策略。
- 本次原型基线或同步结果文件位置。
- 目标需求文件和章节。
- 差异分类和原型证据位置。
- 修改前完整原文和修改后完整原文。
- 修改原因、影响端、模块、角色、流程和数据。
- 受影响用例及处理状态。
- 可直接执行的回退方法。

没有实际修改时只在同步结果写摘要；策略初始化或策略变化可以在日志记录一次，但不得为每个未变化文件制造空变更。

### 3.8 评估已有用例

不得自动覆盖或删除历史 Excel。对相关已有用例逐条标记：

- `继续有效`：需求变化不影响验证意图和预期。
- `需要重审`：可能影响步骤、数据、预期或适用范围。
- `已被替代`：新需求已经形成新的替代用例。
- `应当废弃`：原型有明确删除依据，原用例不再适用。

同步结果必须列出后三类用例；无法确定具体编号时记录需要重审的范围，不编造编号。

### 3.9 MainBasis 衔接入口（按 BASIS-01 执行）

项目配置 `MainBasis` 时，生成执行者自动按以下顺序衔接。命令在 testcase 仓库根目录运行；`<任务目录>` 使用本次新建的 `work/...` 相对路径，不复用历史任务目录。

1. 运行 `node scripts/mainbasis.mjs verify liveshow-proto`。版本一致时复用该需求基线；失败时按返回原因进入下一步，不能把旧文档哈希补写成新基线跳过分析。
2. 运行 `node scripts/mainbasis.mjs start liveshow-proto <任务目录>` 固定同步前快照与派生清单原文。执行全局扫描、系统概要优先的 context 同步，然后按 `TRACE-01` 运行 prepare-context、完成逐项对照、运行 context，回读核对全部 context 并固定 checkpoint。此后才读取这些文件汇总两份 MainBasis；不得同一批整理结果双写后直接 seal。首次衔接重新核对现有文档优先级和旧绝对链接。
3. 保留 `global-evidence-scan-result.json`，其中 `文件清单` 使用仓库相对 `路径` 和 `SHA-256`（兼容 `SHA256`）；同步完成后更新派生需求的文件哈希。保存 `prototype-context-sync-result.json`，按第 4 节记录实际结果，并补充下列衔接字段。
4. 对每份派生清单及两份 MainBasis，在 `文件核对` 中保存 `路径、修改前SHA256、修改后SHA256、核对说明`。首次新建文件的修改前哈希为 `null`；即使内容无变化，也说明核对范围和采用依据。实际修改须记录根目录 `需求清单变更日志.md` 的连续编号。
5. 同步结果增加 `MainBasis复核`：`业务优先级` 固定为 `系统概要优先，原型和批注补充`；`复核说明` 写本次实际分析与隔离情况；`待确认问题` 保存问题编号数组。`来源映射` 逐项包含 `来源路径、来源SHA256、来源位置、目标路径、目标原文`，将规则及风险追溯到两份文档中的实际内容，不得使用单条示例代替完整映射。运行 `node scripts/mainbasis.mjs inspect liveshow-proto`，将实际返回的 `上游指纹` 写入该复核对象。哈希清单不表示已完成语义分析。
6. 两文档汇总后运行 prepare-basis，完成 context 到两文档的逐项双向对照，再运行 `node scripts/mainbasis.mjs seal liveshow-proto <任务目录>`。具体字段见 `流程追溯文件说明.md`。校验通过才发布新版 latest.json 和任务内不可覆盖的基线；缺少 checkpoint、来源漏项、目标无依据或记录变化都不能发布。旧版基线不能补写哈希冒充新版。
7. 再次 verify，在各端 `generation-input-manifest.json` 中填写 `MainBasis基线: {路径, SHA-256}`。输入两文档分别使用 `当前业务证据/true` 和 `风险与缺口/false`；上游材料保留在需求同步记录中，不传为用例业务输入。由生成执行者从文档建立独立规则、共享对象状态和端侧场景，使用现有结构化设计渲染与共用校验生成，不运行旧任务的直接导出脚本。三端共用此基线，下游各端可以有独立任务子目录。
8. 运行 prepare-coverage，从两份文档全文初始化全部未覆盖的清单；执行 `TRACE-01` 逐条拆分和核对场景、状态与用例契约。候选前和最终输出前分别执行 `validate-generation-input.mjs`，导出及缓存发布继续执行共用门禁。本节发布的只是需求版本，不能当作全部业务覆盖或产品测试已通过。

`mainbasis.mjs` 自动完成文件版本核对和基线发布；读取原型、解释差异、补充需求、隔离风险和用例设计由生成执行者按本指令完成。仅执行 start/inspect 不会更新需求正文；仅有哈希相同也不能替代首次同步。

项目配置全新生成模式时，按 BASIS-01 第 8 项跳过本指令第 3.8 节和后文的历史用例影响分析，不读取旧任务或归档补齐材料；记录“不评估历史用例：用户禁用历史输入”。本次重建 MainBasis 不沿用已归档文档的规则、场景或待确认结论，所有判断回到当前上游。

## 4. 同步结果

每次生成任务在自己的 `work/` 目录保存 `prototype-context-sync-result.json`，至少包含：

```json
{
  "项目名称": "Luma Live",
  "来源策略": "prototype-primary",
  "同步状态": "通过",
  "原型基线": [],
  "扫描范围": [],
  "目标需求清单": [],
  "差异统计": {
    "新增": 0,
    "修改": 0,
    "明确删除": 0,
    "原型未覆盖": 0,
    "来源冲突": 0,
    "无法定位": 0
  },
  "需求清单变更日志编号": [],
  "受影响用例": [],
  "阻塞异常": []
}
```

`同步状态` 只允许：

- `通过`：同步完成，没有待确认或阻塞项。
- `有非阻塞待确认`：同步完成，原型未覆盖项已进入需求待确认，不影响当前明确用例生成。
- `阻塞`：不能保证需求清单正确或可回退，禁止继续生成 JSON 和 Excel。

## 5. 生成门禁

满足以下全部条件后才能生成测试用例：

1. 新项目首次接入已经确认；旧项目兼容依据可以核对。
2. 项目来源策略已识别。
3. `prototype-primary` 项目已生成本次同步结果。
4. 系统概要、原型和批注基线在同步完成后未变化。
5. 所有需求修改均有直接证据和日志编号。
6. 每个删除项均已完成系统概要、全部关联原型和结构化批注的缺失复核，没有因单页或静态截图未展示执行删除。
7. 系统概要已明确但原型未覆盖的隐藏业务规则已经保留，且未被转换成无原型依据的目标端入口。
8. 已有用例影响已经分类。
9. 同步状态不是 `阻塞`。
10. 同步新增或修改的确定规则已形成角色、动作、状态、结果和证据位置完整的原子映射；不存在一个规则标识对应多个执行角色或多个独立结果。

任一条件不满足时，先按 `AGENTS.md` 的 `GATE-01` 判断影响范围：能够定位到具体规则、角色或模块时隔离受影响内容并进入“需求待确认”；公共基线、输入完整性、核心规则或结果一致性失效且无法限定影响范围时，才停止当前批次。不得跳过同步或使用旧同步结果。

## 6. 交付说明

最终交付测试用例时，同时简要报告：

- 本次扫描的端、模块和关联范围。
- 新增、修改、明确删除、原型未覆盖和来源冲突数量。
- 需求清单变更日志编号。
- 需要重审、已被替代和应当废弃的旧用例数量。
- 是否存在不阻塞当前生成的需求待确认。

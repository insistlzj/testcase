# 原型与需求清单同步指令

## 1. 目的

在生成测试用例前，自动核对项目 `prototype/` 与 `context/` 需求清单，解决产品修改原型后遗漏维护需求清单的问题。对声明为 `prototype-primary` 的项目，原型是当前有效需求源，需求清单是可追溯、可回退的派生文档。

本指令只负责同步需求事实，不负责生成测试用例。同步通过后，才使用 `Cem Kaner.txt` 的输入与 JSON 输出模板，并按照 `AGENTS.md` 的当前规则生成用例。

## 2. 自动触发

当用户要求生成、重写、补充或审查测试用例时，先查找项目根目录 `需求来源策略.json`：

- `prototype-primary`：自动执行完整同步，不需要用户重复发送同步指令。
- `requirement-primary`：比较来源但不自动修改需求清单；冲突进入“需求待确认”。
- `conflict-review`：比较来源但不选择覆盖方；冲突进入“需求待确认”。
- 没有配置：按 `conflict-review` 处理，不自行假定原型优先。

只有项目配置明确列出的派生需求清单允许自动修改。该清单是写入白名单，不是读取白名单；配置的公共证据、全部现存 `context/` 文件、项目说明引用和扫描发现的关联来源仍须按全局扫描规则读取。公共证据、历史只读来源、旧 Excel、沟通记录和已有测试用例不得被自动覆盖，除非同一文件也被明确列入派生需求清单。

## 3. 同步步骤

### 3.1 确定范围

1. 根据用户指定的端和模块定位相关原型页面、页面树节点和派生需求文件。
2. 同时纳入系统概要、业务对象、角色权限、页面架构、全局批注、公共 Mock、共享组件、页面状态和该模块的跨端、跨模块依赖；不得因文件未列入派生需求清单而跳过读取。
3. 首次同步、策略变化、索引缺失或映射失效时执行全量扫描；其他情况执行增量扫描。
4. 发现关联模块后自动扩大范围，不得只读取单个截图或单页静态文本。

### 3.2 固定原型基线

同步开始时记录范围内每个原型文件的：

- 项目内相对路径。
- 文件修改时间。
- SHA-256。

比较、修改需求和生成用例必须使用同一基线。同步期间任一相关文件的修改时间或 SHA-256 变化，本次结果立即失效，重新读取后再同步。

### 3.3 建立需求映射

建立以下稳定关系：

```text
原型文件或页面节点 -> 端 -> 模块 -> 功能结构 -> context 文件 -> 章节
```

页面名称相似不能单独证明属于同一功能。需要结合页面树、跳转、批注、共享业务对象和需求章节判断。无法唯一定位时记录为“无法定位”的阻塞异常，不自动选择最相似章节。

### 3.4 比较并分类

每项差异只能属于以下一种：

- `新增`：原型有明确功能，派生需求没有。
- `修改`：原型与派生需求描述同一功能，但明确内容不同。
- `明确删除`：原型明确说明删除、取消、下线、废弃或不支持。
- `原型未覆盖`：派生需求存在规则，但原型没有表达是否保留或删除。
- `来源冲突`：原型内部、原型与用户确认或其他有效来源存在无法直接解决的冲突。
- `无法定位`：不能唯一确定应修改的需求文件或章节。

不得把“页面没有出现”“静态截图没有展示”“没有找到按钮”直接认定为 `明确删除`。

页面初始化的默认展示值、占位值或 Mock 记录中的示例数据，与同一页面批注明确写明的默认规则、筛选规则、状态含义或计算口径不一致时，以批注为准。该差异不归入 `来源冲突`，应按批注同步需求，并在同步结果中记录批注位置和被覆盖的页面值或 Mock 示例。

### 3.5 自动修改白名单

有直接原型证据时，可以自动同步：

- 页面结构、页面名称和功能入口。
- 按钮、字段、选项、标签和明确展示文案。
- 原型真实实现的弹窗、抽屉、Tab、跳转和状态切换。
- 页面状态注册中可以复现的空态、禁用态、错误态和角色视角。
- 原型批注明确说明的交互、权限、边界和操作结果。
- Mock 明确表达的字段名称、枚举和页面间共享对象口径。

每次只修改与差异直接相关的最小文本，不顺带调整无关标题、排序、空格或整份文件格式。

### 3.6 自动修改保护

- 只有明确删除证据才能删除。删除前把完整原文写入 `需求清单变更日志.md`。
- 原型没有表达的权限、金额、公式、统计、异步时效、异常恢复、跨端结果、历史数据和兼容规则保留原文，并标记“原型未覆盖”。
- 原型明确否定隐藏业务规则但没有给出完整替代规则时，不自动改成新的确定性规则，记录为“来源冲突”并进入“需求待确认”。
- 原型默认展示值、占位值或 Mock 示例数据与明确批注不一致时，按第 3.4 节采用批注，不阻断同步。
- 除上述例外外，原型内部的说明、批注、真实可执行交互、字段结构、枚举、数据类型、状态切换和操作结果相互冲突时，不自行挑选；能够依据明确版本或替代关系解决时记录依据，否则阻断同步。
- 历史只读来源和历史 Excel 不修改、不删除。

### 3.7 写入需求清单和日志

修改前再次确认原型基线未变化。修改完成后，在根目录唯一 `需求清单变更日志.md` 追加连续记录，至少包括：

- 变更编号、时间、项目和来源策略。
- 本次原型基线或同步结果文件位置。
- 目标需求文件和章节。
- 差异分类和原型证据位置。
- 修改前完整原文和修改后完整原文。
- 修改原因、影响端、模块、角色、流程和数据。
- 受影响用例及处理状态。
- 可直接执行的回退方法。

没有实际修改时只在同步结果写摘要；策略初始化或策略变化可以在日志记录一次，但不得为每个未变化文件制造空变更。

### 3.8 评估已有用例

不得自动覆盖或删除历史 Excel。对相关已有用例逐条标记：

- `继续有效`：需求变化不影响验证意图和预期。
- `需要重审`：可能影响步骤、数据、预期或适用范围。
- `已被替代`：新需求已经形成新的替代用例。
- `应当废弃`：原型有明确删除依据，原用例不再适用。

同步结果必须列出后三类用例；无法确定具体编号时记录需要重审的范围，不编造编号。

## 4. 同步结果

每次生成任务在自己的 `work/` 目录保存 `prototype-context-sync-result.json`，至少包含：

```json
{
  "项目名称": "Luma Live",
  "来源策略": "prototype-primary",
  "同步状态": "通过",
  "原型基线": [],
  "扫描范围": [],
  "目标需求清单": [],
  "差异统计": {
    "新增": 0,
    "修改": 0,
    "明确删除": 0,
    "原型未覆盖": 0,
    "来源冲突": 0,
    "无法定位": 0
  },
  "需求清单变更日志编号": [],
  "受影响用例": [],
  "阻塞异常": []
}
```

`同步状态` 只允许：

- `通过`：同步完成，没有待确认或阻塞项。
- `有非阻塞待确认`：同步完成，原型未覆盖项已进入需求待确认，不影响当前明确用例生成。
- `阻塞`：不能保证需求清单正确或可回退，禁止继续生成 JSON 和 Excel。

## 5. 生成门禁

满足以下全部条件后才能生成测试用例：

1. 项目来源策略已识别。
2. `prototype-primary` 项目已生成本次同步结果。
3. 原型基线在同步完成后未变化。
4. 所有需求修改均有直接证据和日志编号。
5. 没有因“原型未展示”执行删除。
6. 原型未覆盖的隐藏业务规则已保留或进入需求待确认。
7. 已有用例影响已经分类。
8. 同步状态不是 `阻塞`。

任一条件不满足时停止生成，并说明阻塞原因，不得跳过同步或使用旧同步结果。

## 6. 交付说明

最终交付测试用例时，同时简要报告：

- 本次扫描的端、模块和关联范围。
- 新增、修改、明确删除、原型未覆盖和来源冲突数量。
- 需求清单变更日志编号。
- 需要重审、已被替代和应当废弃的旧用例数量。
- 是否存在不阻塞当前生成的需求待确认。

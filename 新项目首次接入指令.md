# 新项目首次接入指令

## 1. 目的

当用户在 `testcase` 根目录新增一个项目时，先确认项目身份、实际资料入口和来源策略，再进入测试用例生成流程。不同项目不要求具有与 Luma Live 相同的文件结构；缺少某个特定文件名不等于项目资料缺失。

本指令只负责新项目登记和草稿准备，不负责生成测试用例，也不得把其他项目的需求、缓存、原型或已有用例作为新项目证据。

## 2. 触发条件

满足任一条件时执行首次接入：

1. 用户明确说明这是刚放入 `testcase` 根目录的新项目。
2. 项目没有状态为 `已确认` 且稳定项目标识匹配的 `work/<项目目录名>-project-onboarding/latest.json`，同时也没有可作为旧项目兼容依据的历史全局证据缓存。
3. 项目目录名、Git 远程地址或其他稳定项目标识与已有接入记录不一致。
4. 项目虽有同名接入记录，但记录中的项目路径、来源策略或关键入口已经无法与当前目录对应。

仅删除普通全局扫描缓存不自动把已有有效接入记录的项目变成新项目。旧项目没有接入记录时，可以使用以下兼容依据：项目根目录已有可读取的 `项目说明.md` 和 `需求来源策略.json`；本规则生效前最后一次全局证据缓存的扫描状态为 `通过` 或 `有非阻塞待确认`；缓存中的项目目录与当前目录一致；缓存记录的这两个核心文件 SHA-256 与当前文件一致。核心文件已发生合理修改但尚未扫描时，只有用户明确确认“这是原有项目而非新增或替换项目”才能继续作为兼容接入。该历史缓存即使因本次规则或 Schema 变化失效，也只能证明旧项目身份，不能复用其中的业务证据；下次通过首次全量扫描后补建接入记录。

## 3. 只读盘点

首次接入只读检查以下内容：

- 项目目录名、绝对路径、Git 仓库根和 Git 远程地址作为稳定项目标识；当前分支和提交号只作为本次盘点基线。不是 Git 项目时明确记录“不适用”。
- README、项目说明、需求文档、验收标准、原型说明、原型入口、批注、Mock、代码模块和已有测试资料。
- 现有 `项目说明.md`、`需求来源策略.json`、`context/`、`prototype/` 或功能相同但名称不同的目录和文件。
- 项目实际包含的端、模块、角色和可识别的资料入口；无法从文件确认的内容标记为“待用户确认”。
- 是否存在嵌套 Git 仓库，以及该项目由父仓库还是自身仓库管理。

盘点阶段只记录事实，不根据 Luma Live 的目录、文件名或来源策略补造同类文件。

## 4. 草稿产物

首次接入时，在本次任务的 `work/` 目录始终生成接入结果；项目说明或来源策略需要新增、补充或修订时，再生成对应草稿：

```text
<本次任务work目录>/
├── project-onboarding-result.json
├── 项目说明草稿.md          # 按需
└── 需求来源策略草稿.json    # 按需
```

如果项目已经存在相应文件，只生成差异或缺口草稿，不覆盖原文件。草稿必须清楚标记“未确认，不得作为正式生成来源”。

### 4.1 项目说明草稿

只写入能够从当前项目直接确认的内容：

- 项目名称、项目目录和项目范围。
- 已确认存在的端和模块。
- 实际存在的需求、原型、代码、批注、Mock 和测试资料入口。
- 缺失、失效或无法定位的引用。
- Git 项目标识和本次盘点基线。

无法确认的项目名称、端、模块、角色、业务规则、文件替代关系和废弃关系使用“待用户确认”，不得猜测。

### 4.2 来源策略草稿

- 未发现明确来源优先关系时，草稿只能建议 `conflict-review`，不得直接确认 `prototype-primary` 或 `requirement-primary`。
- `派生需求清单` 只列有直接证据表明允许自动同步修改的文件；没有证据时使用空数组并关闭生成前同步。
- `公共证据清单` 可以登记已确认存在的系统概要、业务对象、角色权限、页面架构等公共来源。
- 不得复制其他项目的目录、策略、写入白名单或公共证据清单。

### 4.3 接入结果

`project-onboarding-result.json` 至少包含：

```json
{
  "schemaVersion": "1.0",
  "项目目录": "",
  "项目身份": {
    "稳定标识": {
      "绝对路径": "",
      "Git仓库根": "",
      "Git远程地址": ""
    },
    "盘点基线": {
    "Git分支": "",
    "Git提交": ""
    }
  },
  "触发原因": [],
  "盘点文件": [],
  "发现的证据入口": [],
  "缺失或失效引用": [],
  "项目说明处理": "保留 / 生成草稿 / 建议修订",
  "来源策略处理": "保留 / 生成草稿 / 建议修订",
  "待用户确认": [],
  "接入状态": "待确认"
}
```

接入状态只允许：

- `待确认`：草稿或关键项目身份尚未由用户确认，禁止生成测试用例。
- `已确认`：用户已确认项目身份、资料入口和来源策略，可以进入首次全量扫描。
- `阻塞`：目录不可读、项目身份冲突或无法形成最小证据入口，禁止继续。

## 5. 用户确认与正式落地

1. 向用户展示项目名称、端和模块、实际证据入口、建议来源策略、缺失或失效引用以及拟新增或修改的文件。
2. 用户确认前，不得在项目目录创建或修改 `项目说明.md`、`需求来源策略.json`、`context/` 或其他项目文件。
3. 用户明确确认后，才按确认内容将草稿写入项目目录；已有文件只做确认范围内的最小修改。
4. 正式落地后，将确认结果保存到 `work/<项目目录名>-project-onboarding/latest.json`，记录确认时间、确认依据、稳定项目标识、盘点基线、正式文件路径及 SHA-256。
5. 首次正式生成必须执行 `full` 全量证据扫描，并按 `全局证据扫描指令.md` 在语义读取前清除该项目缓存目录内全部旧文件；不复用其他项目或接入确认前产生的扫描结论。
6. 接入确认只证明项目身份、入口和来源策略已经登记，不证明所有业务需求完整；业务缺口仍按 `AGENTS.md` 进入需求待确认。

## 6. 已有项目兼容与变化处理

- 已登记项目新增普通需求、原型或代码文件时，按全局增量或回退全量规则处理，不重复执行首次接入。
- Git 远程地址、项目目录身份或项目根发生变化时，重新执行首次接入，不把旧项目缓存迁移给新项目。
- 仅分支或提交变化时，先按全局缓存失效规则判断；稳定项目标识未变化且接入入口仍有效时，不要求重新确认项目。
- 项目新增、删除或重命名正式证据入口时，更新项目说明和来源策略应另行获得用户授权，并记录相应日志。

## 7. 生成门禁

新项目满足以下全部条件后才能进入测试用例生成：

1. 接入状态为 `已确认`，或者目录、核心文件哈希和历史扫描状态符合本指令定义的旧项目兼容依据；核心文件已变化时已有用户明确确认。
2. 稳定项目标识与接入记录或旧项目兼容依据一致。
3. 正式 `项目说明.md` 和 `需求来源策略.json` 中引用的文件可以定位，或者已经明确记录替代关系。
4. 来源策略中的写入白名单、公共证据和同步开关已经确认。
5. 本次全局扫描模式为 `full`，已记录并完成当前项目旧缓存清除，且未复用其他项目缓存。

任一条件不满足时，只交付接入草稿和阻塞说明，不生成测试用例 JSON 或 Excel。

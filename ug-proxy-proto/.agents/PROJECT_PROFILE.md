# 项目档案

> 档案版本：1.0
>
> 档案状态：AI 自动生成草稿
>
> 维护模式：首次自动生成；存在后不自动覆盖
>
> 生成原则：有证据才填写，无法确认则标记“待人工确认”

## 1. 检查快照

- 生成日期：2026-07-29
- 项目名称：UG 代理 App 原型
- 项目根目录：`D:\testcase_html\ug-proxy-proto`
- Git 分支：`main`
- Commit ID：`7d7e6d00fb9c73d9b2b1589af90680fe0eee7011`
- 工作区状态：生成前干净；本档案生成后存在未提交的 `.agents` 文件
- 操作系统：Windows

## 2. 项目用途

- 主要用途：提供 UG 代理 App 的高保真、可点击、离线静态原型，作为开发与测试理解页面和业务批注的依据。
- 核心用户：UG 代理用户；客户/会员作为被绑定、邀请和订单关联的业务对象。
- 核心业务流程：启动与登录；客户邀请或验证码绑定；客户、订单和团队查看；成长值查看与提现；消息、学习中心和个人设置。
- 业务重要性：待人工确认。

## 3. 技术与结构

- 编程语言：HTML、CSS、JavaScript。
- 主要框架：无运行时框架；本地 Tailwind 浏览器脚本用于样式。
- 包管理或构建工具：无。
- 启动入口：`prototype/index.html`，可直接在浏览器打开。
- 业务代码目录：`prototype/pages`、`prototype/assets`。
- 配置目录：无独立配置目录。
- 测试目录：未发现。
- 数据模型或数据库目录：无；原型数据与字段口径位于 `prototype/assets/common.js`、`prototype/assets/mock.js` 和批注文件。
- 生成代码、第三方代码和构建产物目录：`prototype/assets/tailwind.js` 属本地化第三方脚本；未发现构建产物。

## 4. 已确认的常用命令

- 安装依赖：不需要。
- 启动项目：直接打开 `prototype/index.html`。
- 单元测试：未发现。
- 集成或接口测试：未发现。
- lint 或静态检查：未发现。
- 构建：不需要。

## 5. 业务、状态与权限

- 用户角色：代理用户；会员/客户；下级代理。更细角色待人工确认。
- 身份认证方式：手机号和密码登录；具体服务端认证机制待人工确认。
- 功能权限规则：未登录访问受保护功能时跳转登录；间推客户信息脱敏且不可查看订单详情。
- 数据权限或租户隔离：成长页仅展示当前代理自己的消费、成长值和提现数据；服务端实现待人工确认。
- 关键状态及转换：登录失败/受限；客户绑定；订单待发放/已发放/已取消/已退款；提现待审核/已到账/已拒绝。
- 金额、数量、时间等关键规则：成长值单位 POC；提现到账币种说明含 USDG；待发放成长值暂不可提现；具体计算、精度和时效以批注及后端规则为准。
- 不允许被破坏的数据约束：会员手机号具有唯一性；客户代理归属绑定后永久锁定；App 只绑定已有会员、不创建会员。

## 6. 数据与外部依赖

- 数据库：原型未包含。
- 缓存：原型未包含。
- 消息队列或异步任务：原型未包含。
- 文件或对象存储：原型未包含。
- 第三方接口：原型不发起真实网络请求。
- 定时任务：原型未包含。
- 可能产生真实外部副作用的操作：无；提交、提现、删除等在原型中仅模拟反馈。

## 7. 环境与安全边界

- 允许使用的测试环境：当前本地静态原型。
- 允许使用的测试账号：不适用；原型使用虚构 Mock 数据。
- 测试数据库或测试数据：`prototype/assets/common.js` 中的虚构展示数据。
- 禁止连接的环境：未经用户确认，不连接真实后端或共享环境。
- 禁止执行的命令：未经用户确认，不运行可能连接真实服务或修改远程仓库的命令。
- 敏感数据类型：手机号、客户身份、订单、收款账户、邀请码、成长值和提现数据。
- 运行测试可能产生的副作用：本地浏览原型无真实业务副作用。

## 8. 检查重点

- 核心入口：`prototype/index.html`、`prototype/assets/page-data.js`、`prototype/assets/common.js`、`prototype/assets/annotations.js`。
- 高风险模块：登录与会话、客户归属及脱敏、订单与成长值状态、提现金额和收款账户、验证码绑定。
- 已有测试：未发现。
- 明显测试覆盖空白：未发现自动化测试或工作簿形式的功能测试用例。
- 默认排除目录：`prototype/assets/tailwind.js` 的第三方实现细节、纯视觉样式检查。
- 已知但本次不处理的问题：后端接口、真实权限、持久化、并发和资金处理只能作为待确认项。

## 9. 证据索引

| 事实 | 证据文件 | 说明 |
|---|---|---|
| 项目为零构建静态原型 | `AGENTS.md` | 明确规定纯静态、离线打开 |
| 页面清单与状态 | `prototype/UG代理App-原型说明.md` | 列出 20 个页面及主要状态 |
| 页面树、路由和状态 | `prototype/assets/page-data.js` | 定义页面、状态、路由和父子关系 |
| 页面功能和交互 | `prototype/assets/common.js` | 定义各页面渲染、控件和跳转 |
| 业务规则、权限和字段口径 | `prototype/assets/annotations.js` | 包含客户、团队、订单、成长值和提现规则 |

## 10. 待人工确认

- [ ] 代理角色层级及完整权限矩阵
- [ ] 登录失败次数、锁定时长和密码策略
- [ ] 成长值计算、发放、撤销和提现的后端规则
- [ ] 验证码有效期、发送频率和错误次数限制
- [ ] 提现最小/最大金额、手续费、精度和账户校验
- [ ] 消息已读机制、内容有效期和跳转失败处理
- [ ] 真实测试环境、测试账号和测试数据

## 11. 档案维护规则

- 本档案用于帮助测试分析，不是需求文档的替代品。
- “待人工确认”内容不得作为确认业务缺陷的唯一依据。
- 自动初始化后不自动覆盖；代码变化导致档案过期时，应先报告差异。
- 人工确认内容时，应尽量注明依据、负责人或文档位置。
- 不得在本档案中保存秘密凭据或完整个人数据。

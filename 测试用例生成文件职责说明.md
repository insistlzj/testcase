# 测试用例生成文件职责说明

## 1. `AGENTS.md`

`AGENTS.md` 是测试用例生成与交付的唯一规则来源，负责定义强制规范和质量门禁，主要包括：

- 需求、原型和代码的证据优先级，以及历史用例只用于比较和追溯的边界。
- 明确规则、合理扩展和需求待确认之间的边界。
- 测试用例与需求待确认的统一字段。
- 用例类型、优先级、编号和语义去重规则。
- 测试步骤、预期结果和来源备注的编写要求。
- Excel 工作表、列顺序、样式和数据校验标准。
- 交付前必须执行的完整性和质量检查。

它相当于测试用例工作的“制度和验收标准”。证据、同步、扫描、测试设计、字段语义、质量标准或交付格式发生变化时，只在 `AGENTS.md` 维护规则正文；只有任务输入或 JSON 输出接口也发生变化时，才同步修改 `Cem Kaner.txt`。

## 2. `Cem Kaner.txt`

`Cem Kaner.txt` 是提供给 AI 的 JSON 用例生成执行模板，只负责：

- 提供项目名称、端名、模块名、来源策略、扫描结果、同步结果、生成输入角色清单、当前业务规则清单和当前证据等输入占位。
- 定义启动生成任务的最小执行顺序。
- 引用 `AGENTS.md` 对应章节，不重复解释证据、同步、扫描、测试设计、去重和质量规则。
- 约束正式用例和需求待确认的 JSON 字段、数据类型及初始默认值。
- 提供合法 JSON 示例。

它相当于测试用例生成时的“调用模板和输出接口”。输入占位、执行入口、JSON 字段接口或示例需要调整时修改 `Cem Kaner.txt`；测试设计方法和质量规则只修改 `AGENTS.md`。

## 3. `新项目首次接入指令.md`

该文件负责处理新增到 `testcase` 根目录、尚未登记的项目：

- 只读盘点项目身份、Git 信息和实际存在的需求、原型、代码及测试资料入口。
- 在任务 `work/` 目录生成项目说明草稿、来源策略草稿和接入结果，不直接写入项目。
- 要求用户确认项目身份、证据入口和来源策略后才正式落地。
- 保存独立项目接入记录，并要求首次正式生成执行全量扫描。
- 禁止把其他项目的目录、业务规则、缓存或已有用例用于补齐新项目。

它相当于“新项目登记和生成前准入”。接入草稿不是正式需求，也不能作为测试用例证据。

## 4. `<项目>/需求来源策略.json`

项目来源策略文件负责声明该项目在需求、原型发生差异时采用哪一种处理方式：

- `prototype-primary`：已登记系统概要时由系统概要确定最高业务规则，审核通过的原型和批注补充页面、入口与交互；生成用例前自动同步派生需求清单。系统概要、原型或批注有而清单缺失的内容补入，冲突时系统概要优先，三者均无依据的清单孤立条目经完整缺失复核后删除。
- `requirement-primary`：正式需求优先，原型冲突进入需求待确认。
- `conflict-review`：不自动选择覆盖方，冲突进入需求待确认。

它还负责指定原型目录、允许修改的派生需求清单、公共证据清单、历史只读来源、同步结果位置和阻断条件。派生需求清单只控制自动写入范围，不控制证据读取范围；已登记旧项目没有该文件时使用 `conflict-review`，新项目没有该文件时必须先生成草稿并经用户确认，不能自行假定原型优先。

## 5. `原型与需求清单同步指令.md`

该文件负责定义生成用例前的自动同步流程，主要包括：

- 固定系统概要、原型和批注的路径、修改时间和 SHA-256，保证一次任务使用同一同步来源基线。
- 建立原型页面与端、模块、功能结构、需求文件和章节的映射。
- 将差异分类为新增、修改、明确删除、原型未覆盖、来源冲突或无法定位。
- 限定可以自动修改和删除的范围：系统概要优先，原型和批注补充；孤立条目只有完成三类来源缺失复核后才能删除。
- 记录需求变化、评估已有用例影响，并在同步阻塞时停止生成 Excel。

它相当于“原型变更转换为派生需求”的执行流程。

## 6. `需求清单变更日志.md`

该文件是根目录唯一的需求清单同步日志，负责记录：

- 项目、来源策略和原型基线。
- 目标需求文件、章节和差异分类。
- 原型证据、修改前原文和修改后原文。
- 影响范围、受影响用例和处理状态。
- 可以直接执行的回退方法。

它只记录项目来源策略和 `context/` 派生需求的变化。`AGENTS.md`、`Cem Kaner.txt` 等规则变化仍记录在 `规则文件变更日志.md`。

## 6.1 `全局证据扫描指令.md` 与扫描结果

`全局证据扫描指令.md` 负责定义首次全量、后续增量、分层缓存失效与清理、回退全量、项目指纹、细粒度依赖扩展、规则缺失判定复核、阶段耗时和输出前证据复核规则。

- 项目级缓存保存在 `work/<项目目录名>-global-evidence-cache/`；`latest.json` 保存各层索引，`layers/` 分别保存文件清单、证据原子、归一化规则、用例与历史比较、工作簿层，用于只重建失效层及其下游。
- 每次任务在自己的 `work/` 目录保存 `global-evidence-scan-result.json`，记录本次扫描模式、模式依据、规则基线、文件变化、读取和复用范围、依赖关系、关联映射、规则缺失复核、缺口、阻塞项及输出前复核。
- 项目级缓存只在任务通过全部交付门禁后更新；任务快照独立保留，不因后续缓存变化而改写。
- `full` 和 `fallback-full` 在语义读取前清理证据原子及全部下游层；局部规则或输出变化只清理对应层及下游。任务快照按层记录旧文件路径、哈希、清理时间、数量、残留检查和有效上游复用依据；历史任务快照、接入记录、历史用例与输出不属于清理范围。

它相当于“项目证据索引和增量失效控制”。它先确定本次需要读取和同步的范围，`原型与需求清单同步指令.md` 再处理该范围内的原型与派生需求差异。

## 6.2 `generation-input-manifest.json`

每次任务在自己的 `work/` 目录保存输入角色清单，负责：

- 登记当前项目目录，以及生成过程读取的全部文件路径和 SHA-256。
- 将文件唯一分类为当前业务证据、本次派生产物、历史参照、样式参照或执行工具。
- 明确每个文件是否允许定义业务规则。
- 指定本次 `business-rule-catalog.json`、`historical-case-comparison.json` 和生成脚本。
- 作为生成脚本的输入白名单，阻止未登记文件或其他项目文件作为当前业务证据参与规则生成。

它相当于“生成输入防火墙”。`work/` 历史文件默认只能作为历史参照；只有本次任务保存的当前用户明确确认记录，或从正式决策载体提取且完成当前适用性复核的 `confirmed-decision-extract.json`，可以例外登记为当前业务证据。决策摘录只包含已确认结论及其来源位置，原工作簿中的历史用例仍属于历史参照。

## 6.3 当前规则清单与历史比较结果

- `business-rule-catalog.json` 在候选用例生成前建立，只归一化当前需求、原型、接口、代码和用户确认中的规则。每条规则只包含一个执行角色、一个动作和一个可观察结果，稳定规则标识由归一化业务键生成或沿用；规则必须引用哈希一致的当前证据，不能引用历史用例证明业务预期。
- `current-testcase-candidate.json` 只从当前规则清单生成，并在历史比较前固定 SHA-256；它尚不是可交付结果。
- `historical-case-comparison.json` 在当前候选形成后建立，优先按历史备注中的稳定规则标识精确匹配；只对未匹配记录在相同模块、角色和动作类别桶内执行语义回退。它记录候选路径和哈希、匹配方式、比较次数、当前稳定规则标识、差异、处理判定和编号追溯，不能向当前规则清单或候选回写步骤、预期、提示、阈值或状态。
- 最终 JSON 必须与通过比较的候选 JSON 保持相同 SHA-256。发现遗漏时回到当前证据和规则清单重新生成候选，不能直接修补。
- 质量上下文保存“正式用例 -> 稳定规则标识”映射，用于证明最终用例来自当前规则而不是历史复制。

这两个文件把“现在应该怎样工作”和“过去写过什么”分开：前者决定候选用例，后者只负责比较和追溯。

## 7. `scripts/validate-testcase-json.mjs`

`scripts/validate-testcase-json.mjs` 负责执行所有项目共用的交付前阻断检查，主要包括：

- 校验测试用例和需求待确认的字段、编号、分类与数组结构。
- 识别明显包含多个独立观察点的预期结果。
- 检查存在数据驱动操作的 P0 用例是否提供 `测试数据：` 记录。
- 校验证据编号、来源类型以及正式用例是否具有可证明业务预期的来源。
- 校验覆盖清单是否追溯到正式用例、需求待确认或不适用依据。

检查失败时不得生成 Excel。它只负责可以稳定自动判断的质量问题，不能替代生成过程中的业务语义审查。

### 7.1 `scripts/validate-generation-input.mjs`

该脚本在候选用例生成前和最终输出前各执行一次，负责：

- 校验输入清单的项目、任务目录、目标范围、文件角色和 SHA-256。
- 阻止历史 JSON、历史 Excel、历史缓存和样式文件取得定义业务规则的权限。
- 校验当前规则清单的稳定规则标识、可生成状态及当前证据引用。
- 校验历史比较结果只引用已登记的历史文件和当前稳定规则标识。
- 检查生成脚本没有直接引用清单中的原始历史用例文件。
- 最终阶段检查历史比较引用的候选哈希，以及候选 JSON 与最终 JSON 的哈希是否完全一致。

检查失败时不得生成候选用例、正式 JSON 或 Excel。该脚本只校验输入隔离和追溯结构，不能代替证据语义判断。

## 8. `*-质量上下文.json`

质量上下文文件保存在对应任务的 `work/` 目录，不进入正式 Excel，包含：

- `证据索引`：记录证据编号、来源类型、实际位置和能够证明的规则。
- `覆盖清单`：记录当前功能适用的业务对象、角色、状态、操作、渠道和终态，以及对应的用例、待确认问题或不适用依据。
- `用例规则映射`：逐条记录正式用例编号、稳定规则标识和当前规则清单位置，证明用例不是从历史编号直接继承。
- `输入隔离校验`：记录 `pre-generate` 与 `final` 两次校验状态、输入清单哈希和失败项；任一阶段未通过时不得交付。
- `阶段耗时`：引用 `pipeline-metrics.json`，记录各阶段输入、输出、复用数量和耗时最长阶段，用于识别下一次优化对象。

它让证据资格和覆盖完整性能够被检查程序追溯，同时避免把某个项目的字段写成所有项目的固定要求。

## 9. 文件之间的关系

- `AGENTS.md` 是唯一规则来源和上层强制规范。
- `新项目首次接入指令.md` 先判断新项目是否具备可确认的项目身份、证据入口和来源策略；未确认时只生成草稿并停止用例生成。
- 项目 `需求来源策略.json` 决定该项目采用系统概要优先的原型同步、需求优先还是冲突待确认。
- `全局证据扫描指令.md` 通过项目指纹和依赖关系决定本次使用全量、增量或回退全量扫描；全量扫描先清理证据原子及下游失效层，再从当前证据重建索引，仍有效的文件清单层可以保留；在声明规则缺失前复核全部关联来源，并保存可追溯的任务快照。
- `原型与需求清单同步指令.md` 在生成前把系统概要、原型和批注中的当前内容同步到允许修改的派生需求清单，并清理完成全来源缺失复核的孤立条目。
- `需求清单变更日志.md` 保存需求同步的证据、前后内容和回退方法。
- `generation-input-manifest.json` 把当前业务证据、派生产物、历史参照、样式参照和执行工具隔离，并由共用脚本校验。
- `business-rule-catalog.json` 只从当前证据建立本次业务规则和候选用例依据。
- `historical-case-comparison.json` 在候选形成后处理历史差异和编号追溯，不能定义当前规则。
- `Cem Kaner.txt` 是遵守该规范的 JSON 生成执行模板，只保留输入、执行引用、输出契约和示例。
- `Cem Kaner.txt` 必须完整读取并引用当前 `AGENTS.md`，不得维护一套平行规则。
- 两个文件冲突时以 `AGENTS.md` 为准；治理规则只修改 `AGENTS.md`，输入或 JSON 输出接口变化时才修改 `Cem Kaner.txt`。
- 质量上下文为本次生成提供证据和覆盖追溯。
- 共用检查程序按照 `AGENTS.md` 校验最终 JSON 和质量上下文。

可以简单理解为：

> `AGENTS.md` 决定“必须遵守什么标准和怎样设计用例”，首次接入指令决定“新项目是否可以进入生成流程”，项目来源策略决定“以谁为准”，全局扫描指令决定“读取哪些当前证据及何时失效”，同步指令决定“怎样更新派生需求”，输入清单决定“哪些文件能做什么”，当前规则清单决定“现在应生成什么”，历史比较决定“旧用例怎样追溯”，`Cem Kaner.txt` 负责“接收哪些输入并输出什么 JSON”，检查程序决定“是否允许生成 Excel”。

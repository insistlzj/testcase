# 测试用例生成文件职责说明

## 1. `AGENTS.md`

`AGENTS.md` 负责定义测试用例生成与交付的强制规范和质量门禁，主要包括：

- 需求、原型、代码和已有用例的证据优先级。
- 明确规则、合理扩展和需求待确认之间的边界。
- 测试用例与需求待确认的统一字段。
- 用例类型、优先级、编号和语义去重规则。
- 测试步骤、预期结果和来源备注的编写要求。
- Excel 工作表、列顺序、样式和数据校验标准。
- 交付前必须执行的完整性和质量检查。

它相当于测试用例工作的“制度和验收标准”。凡是字段定义、证据边界、质量标准或交付格式发生变化，应优先修改 `AGENTS.md`。

## 2. `Cem Kaner.txt`

`Cem Kaner.txt` 负责提供给 AI 的具体测试用例生成提示词，主要包括：

- 告诉 AI 如何读取需求、原型、接口、代码和已有用例。
- 指导 AI 使用主流程、边界值、状态转换、权限和异常恢复等测试方法。
- 要求 AI 只生成有依据、可执行、可观察结果的正式用例。
- 指导 AI 将证据不足或规则冲突的内容写入“需求待确认”。
- 约束 AI 输出符合约定的结构化 JSON。

它相当于测试用例生成时的“执行模板和工作指令”。凡是生成流程、提示语、测试设计方法或 JSON 输出指导需要调整，应修改 `Cem Kaner.txt`。

## 3. `scripts/validate-testcase-json.mjs`

`scripts/validate-testcase-json.mjs` 负责执行所有项目共用的交付前阻断检查，主要包括：

- 校验测试用例和需求待确认的字段、编号、分类与数组结构。
- 识别明显包含多个独立观察点的预期结果。
- 检查存在数据驱动操作的 P0 用例是否提供 `测试数据：` 记录。
- 校验证据编号、来源类型以及正式用例是否具有可证明业务预期的来源。
- 校验覆盖清单是否追溯到正式用例、需求待确认或不适用依据。

检查失败时不得生成 Excel。它只负责可以稳定自动判断的质量问题，不能替代生成过程中的业务语义审查。

## 4. `*-质量上下文.json`

质量上下文文件保存在对应任务的 `work/` 目录，不进入正式 Excel，包含：

- `证据索引`：记录证据编号、来源类型、实际位置和能够证明的规则。
- `覆盖清单`：记录当前功能适用的业务对象、角色、状态、操作、渠道和终态，以及对应的用例、待确认问题或不适用依据。

它让证据资格和覆盖完整性能够被检查程序追溯，同时避免把某个项目的字段写成所有项目的固定要求。

## 5. 文件之间的关系

- `AGENTS.md` 是上层强制规范。
- `Cem Kaner.txt` 是遵守该规范的具体生成提示词。
- `Cem Kaner.txt` 必须遵守 `AGENTS.md`。
- 两个文件内容冲突时，以 `AGENTS.md` 为准。
- 质量上下文为本次生成提供证据和覆盖追溯。
- 共用检查程序按照 `AGENTS.md` 校验最终 JSON 和质量上下文。

可以简单理解为：

> `AGENTS.md` 决定“必须遵守什么标准”，`Cem Kaner.txt` 决定“AI 按什么步骤生成”，质量上下文保存“依据和覆盖”，检查程序决定“是否允许生成 Excel”。

# 测试用例生成文件职责说明

## 1. `AGENTS.md`

`AGENTS.md` 是测试用例生成与交付的唯一规则来源，负责定义强制规范和质量门禁，主要包括：

- 需求、原型、代码和已有用例的证据优先级。
- 明确规则、合理扩展和需求待确认之间的边界。
- 测试用例与需求待确认的统一字段。
- 用例类型、优先级、编号和语义去重规则。
- 测试步骤、预期结果和来源备注的编写要求。
- Excel 工作表、列顺序、样式和数据校验标准。
- 交付前必须执行的完整性和质量检查。

它相当于测试用例工作的“制度和验收标准”。证据、同步、扫描、测试设计、字段语义、质量标准或交付格式发生变化时，只在 `AGENTS.md` 维护规则正文；只有任务输入或 JSON 输出接口也发生变化时，才同步修改 `Cem Kaner.txt`。

## 2. `Cem Kaner.txt`

`Cem Kaner.txt` 是提供给 AI 的 JSON 用例生成执行模板，只负责：

- 提供项目名称、端名、模块名、来源策略、扫描结果、同步结果、已有用例和证据等输入占位。
- 定义启动生成任务的最小执行顺序。
- 引用 `AGENTS.md` 对应章节，不重复解释证据、同步、扫描、测试设计、去重和质量规则。
- 约束正式用例和需求待确认的 JSON 字段、数据类型及初始默认值。
- 提供合法 JSON 示例。

它相当于测试用例生成时的“调用模板和输出接口”。输入占位、执行入口、JSON 字段接口或示例需要调整时修改 `Cem Kaner.txt`；测试设计方法和质量规则只修改 `AGENTS.md`。

## 3. `<项目>/需求来源策略.json`

项目来源策略文件负责声明该项目在需求、原型发生差异时采用哪一种处理方式：

- `prototype-primary`：原型优先，生成用例前自动同步派生需求清单。
- `requirement-primary`：正式需求优先，原型冲突进入需求待确认。
- `conflict-review`：不自动选择覆盖方，冲突进入需求待确认。

它还负责指定原型目录、允许修改的派生需求清单、公共证据清单、历史只读来源、同步结果位置和阻断条件。派生需求清单只控制自动写入范围，不控制证据读取范围；没有该文件时默认使用 `conflict-review`，不能自行假定原型优先。

## 4. `原型与需求清单同步指令.md`

该文件负责定义生成用例前的自动同步流程，主要包括：

- 固定原型文件路径、修改时间和 SHA-256，保证一次任务使用同一原型基线。
- 建立原型页面与端、模块、功能结构、需求文件和章节的映射。
- 将差异分类为新增、修改、明确删除、原型未覆盖、来源冲突或无法定位。
- 限定可以自动修改和删除的范围，保护原型没有表达的隐藏业务规则。
- 记录需求变化、评估已有用例影响，并在同步阻塞时停止生成 Excel。

它相当于“原型变更转换为派生需求”的执行流程。

## 5. `需求清单变更日志.md`

该文件是根目录唯一的需求清单同步日志，负责记录：

- 项目、来源策略和原型基线。
- 目标需求文件、章节和差异分类。
- 原型证据、修改前原文和修改后原文。
- 影响范围、受影响用例和处理状态。
- 可以直接执行的回退方法。

它只记录项目来源策略和 `context/` 派生需求的变化。`AGENTS.md`、`Cem Kaner.txt` 等规则变化仍记录在 `规则文件变更日志.md`。

## 5.1 `全局证据扫描指令.md` 与扫描结果

`全局证据扫描指令.md` 负责定义首次全量、后续增量、缓存失效、回退全量、项目指纹、依赖扩展、规则缺失判定复核和输出前证据复核规则。

- 项目级缓存保存在 `work/<项目目录名>-global-evidence-cache/latest.json`，用于后续任务比较项目指纹和安全复用未变化证据。
- 每次任务在自己的 `work/` 目录保存 `global-evidence-scan-result.json`，记录本次扫描模式、模式依据、规则基线、文件变化、读取和复用范围、依赖关系、关联映射、规则缺失复核、缺口、阻塞项及输出前复核。
- 项目级缓存只在任务通过全部交付门禁后更新；任务快照独立保留，不因后续缓存变化而改写。

它相当于“项目证据索引和增量失效控制”。它先确定本次需要读取和同步的范围，`原型与需求清单同步指令.md` 再处理该范围内的原型与派生需求差异。

## 6. `scripts/validate-testcase-json.mjs`

`scripts/validate-testcase-json.mjs` 负责执行所有项目共用的交付前阻断检查，主要包括：

- 校验测试用例和需求待确认的字段、编号、分类与数组结构。
- 识别明显包含多个独立观察点的预期结果。
- 检查存在数据驱动操作的 P0 用例是否提供 `测试数据：` 记录。
- 校验证据编号、来源类型以及正式用例是否具有可证明业务预期的来源。
- 校验覆盖清单是否追溯到正式用例、需求待确认或不适用依据。

检查失败时不得生成 Excel。它只负责可以稳定自动判断的质量问题，不能替代生成过程中的业务语义审查。

## 7. `*-质量上下文.json`

质量上下文文件保存在对应任务的 `work/` 目录，不进入正式 Excel，包含：

- `证据索引`：记录证据编号、来源类型、实际位置和能够证明的规则。
- `覆盖清单`：记录当前功能适用的业务对象、角色、状态、操作、渠道和终态，以及对应的用例、待确认问题或不适用依据。

它让证据资格和覆盖完整性能够被检查程序追溯，同时避免把某个项目的字段写成所有项目的固定要求。

## 8. 文件之间的关系

- `AGENTS.md` 是唯一规则来源和上层强制规范。
- 项目 `需求来源策略.json` 决定该项目采用原型优先、需求优先还是冲突待确认。
- `全局证据扫描指令.md` 通过项目指纹和依赖关系决定本次使用全量、增量或回退全量扫描；在声明规则缺失前复核全部关联来源，并保存可追溯的任务快照。
- `原型与需求清单同步指令.md` 在生成前把明确的原型变化同步到允许修改的派生需求清单。
- `需求清单变更日志.md` 保存需求同步的证据、前后内容和回退方法。
- `Cem Kaner.txt` 是遵守该规范的 JSON 生成执行模板，只保留输入、执行引用、输出契约和示例。
- `Cem Kaner.txt` 必须完整读取并引用当前 `AGENTS.md`，不得维护一套平行规则。
- 两个文件冲突时以 `AGENTS.md` 为准；治理规则只修改 `AGENTS.md`，输入或 JSON 输出接口变化时才修改 `Cem Kaner.txt`。
- 质量上下文为本次生成提供证据和覆盖追溯。
- 共用检查程序按照 `AGENTS.md` 校验最终 JSON 和质量上下文。

可以简单理解为：

> `AGENTS.md` 决定“必须遵守什么标准和怎样设计用例”，项目来源策略决定“以谁为准”，全局扫描指令决定“读取哪些证据及何时失效”，同步指令决定“怎样更新派生需求”，`Cem Kaner.txt` 负责“接收哪些输入并输出什么 JSON”，两个日志分别保证规则和需求可以回退，检查程序决定“是否允许生成 Excel”。

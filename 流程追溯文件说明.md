# 流程追溯接口

治理要求统一见 AGENTS.md 的 TRACE-01、BASIS-01、GATE-01。本文件定义共用脚本的输入输出，不定义业务规则。所有路径均相对 testcase 根目录；每轮使用新 work 目录。下面的命令由生成执行者依次调用，命令之间的需求分析不能省略。

## 本轮范围与计时

收到 MainBasis 生成请求后，先保存 `work/<本轮任务>/batch.json`，再执行 `node scripts/pipeline-metrics.mjs task-start work/<本轮任务> <请求的ISO时间>`，将原定范围保存至计时记录的 `任务.交付范围`。无法取得请求时间时省略时间参数并说明计时从工具启动开始。续办不重新 task-start；单端也保存批次，只登记一个端任务。按用户范围固定以下元数据，不从最终产物反推端清单：

```json
{
  "schemaVersion": "1.0",
  "项目目录": "liveshow-proto",
  "目标端": ["用户App", "公会App", "管理后台"],
  "交付要求": "完整覆盖",
  "端任务": [
    {"端名": "用户App", "模块名称": "全部模块", "任务目录": "work/<本轮任务>/user"},
    {"端名": "公会App", "模块名称": "全部模块", "任务目录": "work/<本轮任务>/guild"},
    {"端名": "管理后台", "模块名称": "全部模块", "任务目录": "work/<本轮任务>/admin"}
  ]
}
```

只有用户范围允许部分交付时才使用 `交付要求:允许部分交付`，不能因生成困难改写范围或要求。导出前为各端追加实际新文件的 `工作簿:outputs/<项目名称>-case/<文件名>.xlsx`。

`任务.交付范围` 保存项目目录、目标端、交付要求及各端的端名/任务目录/已登记模块名称，工作簿路径不进入该快照。启用原型目录的项目必须登记模块范围，且端侧输入清单与批次一致。批次检查逐字段比对快照。旧任务缺少快照时先按原始用户指令核对并补登记，同时在任务记录保存补登记依据；用户明确改变范围时保存原范围及本次用户决定后更新快照。两种情况均保留原开始时间及全部阶段记录，不补造过去的完成校验，不因此重读历史业务数据。`task-finish` 使用本接口的 MainBasis 批次校验；其他项目的阶段计时命令不变。

各阶段开始执行 `node scripts/pipeline-metrics.mjs start work/<本轮任务> <阶段名> 输入数量=...`，结束执行 `finish ... 输出数量=... 复用数量=...`。标准阶段名为 inventory-hash、semantic-read、sync、normalize-rules、generate-dedup、history-compare、json-validate、xlsx-build、xlsx-verify。语义读取包含实际阅读与业务分析，同步包含 context 回读与 MainBasis 两段核对，规则归一化包含共享状态及场景基线，生成阶段包含逐条复核、去重和缺口修正。

返工先 `finish ... 状态=失败 原因=...` 或 `状态=中断`，再 `start ... 原因=...`；再次运行内部自动计时的现有脚本时，使用 `PIPELINE_RETRY_REASON` 传入具体返工原因。工具只追加尝试，不覆盖旧记录。普通重跑不能重置已完成记录。异常退出遗留计时锁时先确认对应进程已退出，再移除该任务的确切锁文件；不得盲删活动锁。历史禁用执行 `node scripts/pipeline-metrics.mjs skip work/<本轮任务> history-compare 不适用：用户禁用`，其他质量阶段不可 skip。

整轮文件由主执行者顺序写入；按阶段包住三端的对应工作，端内工具另写各自子目录计时，禁止把端内耗时重复累加为整轮墙钟。完成全部交付检查后运行 `task-finish work/<本轮任务>`；该命令重新执行批次 `--workbooks` 检查，要求交付要求满足，随后原子写入 `任务.完成校验`、结束状态及时间，汇总每阶段全部尝试、整轮墙钟和未归属时间。检查失败不写结束状态。缺少记录时保持未完成并说明，不能补造已经过去的阶段时间。`status work/<本轮任务>` 可查看当前累计，不表示业务门禁已通过。

## 需求阶段

1. `node scripts/mainbasis.mjs verify liveshow-proto` 检查已有基线；旧版或失效则进入下一步。
2. `node scripts/mainbasis.mjs start liveshow-proto work/<本轮需求任务>` 保存上游哈希与同步前派生清单原文。
3. 按扫描和同步指令完成当前上游分析、图片查看、差异处理、context 同步和日志。保存 global-evidence-scan-result.json。历史生成物、竞品素材等不读正文的文件，在 `不读取内容` 中逐文件登记 `{路径, SHA-256, 说明}`；当前 context 不得整文件排除。
4. `node scripts/mainbasis.mjs prepare-context liveshow-proto work/<本轮需求任务>` 生成 context-units.json 与 context-transfer.json 草稿。只从当前文件构建完整单元，所有来源初始为未处理，不会自动认证完成。
5. 逐项解释并填写 context-transfer.json；完成真实语义复核后，运行 `node scripts/mainbasis.mjs context liveshow-proto work/<本轮需求任务>`。脚本重新从磁盘读取全部 context 并核对，保存 context-checkpoint.json。
6. 读取 checkpoint 绑定的当前 context，生成 MainBasis 两份文件。运行 `node scripts/mainbasis.mjs prepare-basis liveshow-proto work/<本轮需求任务>`，填写 mainbasis-transfer.json，核对每项 context 去向及 MainBasis 目标来源。
7. 保存原有同步报告、文件核对及 MainBasis复核字段，执行 `node scripts/mainbasis.mjs seal liveshow-proto work/<本轮需求任务>`。基线 schemaVersion 为 2.0，绑定六份过程记录；启用目录时另绑定两份目录传递记录及模块区块版本，原子更新 latest 指针。

prepare 命令不覆盖已有对照草稿；启用原型目录时，prepare-basis 在创建草稿前按下节接口更新正式需求的受管模块区块。来源发生改变，使用新的需求任务目录重新 start；不要修改旧 checkpoint 哈希掩盖变化。

## 原型目录接口（配置项目）

来源策略增加 `原型目录结构:{入口:"prototype/index.html",输出:"context/原型页面目录结构.md"}`，输出同时列入 `派生需求清单`。Luma Live 已登记。`mainbasis start` 自动执行目录更新；仅整理目录时可单独运行 `node scripts/prototype-directory.mjs liveshow-proto work/<本次目录任务>`，不因此发布 MainBasis 或生成用例。独立目录任务与需求 start 任务使用不同新目录，不能覆盖已有同步记录。

目录以端、模块、页面三级组织，包含页面名称、仓库相对页面路径及直接页面/关联详情页标识。受管区块从 `<!-- prototype-modules:v1:start -->` 到 `<!-- prototype-modules:v1:end -->`，包含来源路径及 SHA-256，不维护额外 JSON 模块台账。

context 回读通过后，`prepare-basis` 原样插入或更新正式需求的此区块，再建立原有两段传递草稿；业务正文和风险文档不由目录工具改写。目录变更保存为 `prototype-directory-sync.json`，MainBasis 区块变更保存为 `mainbasis-module-sync.json`，含前后哈希及可回退原文。实际变化必须由同步执行者登记到根目录需求清单变更日志。启用目录后基线绑定八份记录及 `模块目录SHA256`；未启用项目仍使用原六份记录。

用例输入继续只有 MainBasis 两文档；`validateMainBasisInput` 从正式需求区块返回 `moduleDirectory`，无需给输入清单增加目录文件。规则增加 `模块归属说明`，设计增加 `观察页面路径`（仓库相对路径）。两字段在生成复核之前填写；分类修改后按实际影响重新复核设计。模块名称从本端区块取，`全部模块` 展开为本端全部目录模块。需求问题跨模块时用顿号列出，未完成分类保留 `待映射`，由交付检查报告生成缺口。

生成器使用 `casesFromCatalog(catalog,{moduleDirectory,coverageV2})`。`coverageV2` 与当前覆盖展开策略一致；全局行序号连续，模块编号采用 `USER/GUILD/ADMIN-模块序号-模块内序号`，例如 `USER-01-0001`。模块序号由当前目录中本端的模块顺序确定，属于当前版本编号，不读取历史。输出仍使用既有 15/20 字段，模块归属字段留在规则设计中，不增加 Excel 列。

## 两段传递的相同字段

`*-units.json` 包含 `来源` 和 `目标` 数组，每项为 `{标识, 路径, 行, 原文}`；二进制文件使用 `行:0, 二进制:true`，排除文件仅保存 `排除说明`。同步前派生文本以 `同步前:路径` 标记，只用于保留与删除核对。

`context-transfer.json` 和 `mainbasis-transfer.json`：

```json
{
  "schemaVersion": "1.0",
  "来源指纹": "由 prepare 计算",
  "目标指纹": "由 prepare 计算",
  "逐项": [{
    "来源标识": "对应来源单元标识",
    "去向": "已同步",
    "说明": "原条件、执行角色及结果如何保留；差异采用理由",
    "目标标识": ["实际目标单元标识"]
  }],
  "目标排除": [{"标识": "非业务标题或结构单元标识", "说明": "具体非业务理由"}],
  "语义复核": {"说明": "本次逐项复核的范围、分支和冲突结论", "内容SHA256": "复核内容指纹"}
}
```

去向允许已同步、待确认、不适用、已替代。已同步/待确认必须有目标。图片等二进制来源被采用时，同一记录还需 `查看范围` 和 `视觉解读`；这两个字段必须源自实际查看，不得用自动模板填充。被排除的历史文件不得创建业务目标。目标排除仅解释非业务信息或有明确范围依据的内容，不能用于隐藏新增业务规则。

`reviewHash(report)` 从 scripts/requirement-traceability.mjs 导出，计算除语义复核字段以外的内容指纹。必须先执行语义复核再填写；函数本身不代表复核通过。

## 用例阶段

三端共同运行 `node scripts/mainbasis.mjs prepare-coverage liveshow-proto work/<本轮任务>/user work/<本轮任务>/guild work/<本轮任务>/admin`；单端仍可传一个目录。一次核对版本并读取两份 MainBasis，分别创建 requirement-units.json 和 requirement-coverage.json，全部从未覆盖开始；不读取旧用例，不自动生成场景或签复核。两份文档所有非空行均在核对范围内，包括未带特殊标记的正文与结构元数据；这些单元不是业务规则数量。三端先共同建立一个状态转换基线，再将同一份完整基线投影/复制到端目录，端侧场景去向单独填写；不能各自维护不同的共享转换。

在 generation-input-manifest.json 中增加三个路径：

```json
{
  "需求覆盖清单": "work/<本轮用例任务>/<端>/requirement-coverage.json",
  "独立场景库": "work/<本轮用例任务>/<端>/test-scenario-library.json",
  "状态转换基线": "work/<本轮用例任务>/<端>/state-transition-baseline.json"
}
```

三个文件均登记为当前任务的 `本次派生产物`、`允许定义业务规则:false`，保存实际 SHA-256。候选及最终文件继续使用现有 JSON 契约；不新增 Excel 列。

覆盖清单保留 prepare 生成的 schemaVersion、来源指纹和完整逐项列表。补充：

- `交付性质`：完整覆盖或部分覆盖，由实际缺口决定。
- 每个逐项记录：`来源标识、状态、说明、分支`；待确认额外填写问题编号；每个结构标题、注释、其他端条款也须有明确不适用理由，不能直接丢弃。
- `状态`：已覆盖、部分覆盖、未覆盖、待确认、不适用。未覆盖是设计未完成，不能伪装成产品问题。
- 每个分支：`标识、状态、说明、来源片段、执行角色、目标端、入口、场景标识、用例契约`；已设计分支最终补充用例编号。来源片段必须在该单元原文中；用例契约含前置条件、操作步骤、预期结果三个非空字符串数组。最终用例的对应三字段必须与契约逐项一致。待确认分支填写问题编号。
- 分支状态：已设计、未覆盖、待确认。部分覆盖必须同时列出已设计与尚未完成分支。
- `状态转换处理`：每个本端操作或观察的转换都填 `{状态转换标识, 状态, 说明, 场景标识:[]}`；状态为已映射、未覆盖或待确认。待确认必须填写 `问题编号` 并关联最终需求待确认清单，未映射不得声明完整覆盖。
- `语义复核`：说明和内容SHA256，生成执行者按 TRACE-01 实际复核后填写。

独立场景库包含 `场景` 数组：每项使用 `场景标识、执行角色、目标端、入口、用例契约`，与覆盖分支一致；存在生命周期时填状态转换标识。状态基线继续按 STATE-01 保存 `状态转换` 数组，无状态转换时显式为空。各端必须核对自己的操作与观察投影，不能以其他端场景替代。

为减少重复维护，新任务优先使用覆盖清单的 `契约模式:场景引用`。此时已设计分支只填写 `标识、状态、说明、来源片段、场景标识、用例编号`，不重复填写执行角色、目标端、入口和用例契约；这些内容从独立场景库取出后仍逐字段匹配实际候选。覆盖清单另填 `场景库SHA256`，值由 `fingerprint(解析后的场景库JSON)` 计算；输入清单仍登记文件字节哈希，两者用途不同。覆盖语义复核同时绑定这份场景库指纹，场景变化后须重新核对受影响分支并更新复核，不能仅改哈希。旧的内联契约格式继续支持，但同一份清单不能混合引用与重复字段。

生成中发现问题时，在本轮覆盖清单增加 `影响复核`：记录改变的规则/分支/场景、共同业务对象、关联端、受影响转换与场景、复用项及版本依据、未知依赖和处理结论。检查旧关系退出与新关系加入，不只顺着修改后的关系找影响。依赖不明则扩大复核；来源补证先回需求阶段。正文、场景库、候选及相应文件哈希变化后，旧语义复核即失效；不得直接修改最终 JSON 或 Excel。

正式内容稳定后、Excel 导出前运行 `node scripts/validate-testcase-delivery.mjs --batch work/<本轮任务>/batch.json`。整批入口逐端调用现有全部交付门禁，并额外核对原定端清单、当前同版 MainBasis 和同一共享状态基线；禁止因为各端单独通过就跳过整批核对。各端导出及内容/可读性检查通过后，再运行同一命令加 `--workbooks` 核对实际文件。返回码 0 表示本轮声明的交付要求满足，2 表示有效用例通过但完整覆盖仍未满足，1 表示校验错误。返回 2 时仍可保留有效的局部文件，继续补齐可依据当前需求完成的缺口；真正需要业务决定的缺口按 TRACE-01 披露，不得宣称完整。

批次返回的 `未完成项` 从当前覆盖记录投影，包含端名及条款、分支、转换或证据原子的定位，不能将条目数当作覆盖率。`后续动作` 区分继续补齐生成缺口、等待业务确认、复核交付性质声明、可完整交付和可按授权部分交付。返回码 1 时按实际错误定位影响范围，不能无修正地循环重试。

正式导出使用 `node scripts/build-testcase-workbook.mjs work/<本轮任务>/<端> <最终JSON文件名> outputs/<项目名称>-case/<新文件名>.xlsx`，入口自动执行批次要求检查，保存前再次核对。只保存阶段性成果时追加 `--stage`，输出必须为 `outputs/<项目名称>-case/阶段性-<新文件名>.xlsx`；仍执行本端所有正式用例质量检查。两种版本均按 EXCEL-01 直接交付到项目固定目录，Luma Live 使用 `outputs/Luma Live-case/`，用户交付链接不得指向 work 任务快照。阶段性文件在概览说明用途，`delivery-verification.json` 记录 `导出用途:阶段性`；正式导出记录 `导出用途:正式交付`。批次工作簿检查拒绝阶段性或未登记用途的旧检查记录，禁止去掉阶段性标识冒充完整交付；统一保存位置不改变覆盖结论或完成门禁。

依次执行现有 pre-generate、final 和 Excel 交付校验。有效用例可在缺口明确隔离时继续交付，但交付说明必须报告部分覆盖及未完成事项。脚本不理解任意自然语言的全部隐含分支，来源片段、范围排除和业务语义仍须逐条复核。

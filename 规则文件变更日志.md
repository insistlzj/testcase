# AGENTS.md 与 Cem Kaner.txt 规则文件变更日志

本文件是根目录唯一的规则变更日志，用于追溯和回退 `AGENTS.md` 与 `Cem Kaner.txt` 的全部后续改动。

## 日志规则

- 只追加新记录，不删除、覆盖或改写已有记录。
- 变更编号使用 `RCL-0001` 起的连续编号。
- 每次修改和每次回退都必须单独记录。
- 回退时以记录中的“修改前原文”恢复，并追加新的回退记录。

## 启用前基线

- 基线时间：2026-08-27 12:01:08 CST
- `AGENTS.md` SHA-256：`7f86427ac763ed476aefb7f65ff15c67a01819794f0a7f4314c5bbd6f03c3259`
- `Cem Kaner.txt` SHA-256：`3e1bb8ae41cf2735143d5ce1b796649b18335c8d6ebd708a9611657cba4b5304`
- 说明：该基线代表日志制度启用前的文件状态；后续变更均从此基线开始记录。

## RCL-0001 建立唯一规则变更日志制度

- 修改时间：2026-08-27 12:01:08 CST
- 用户要求：以后每一次改动 `Cem Kaner.txt` 和 `AGENTS.md` 都要记录日志，所有改动保存在根目录唯一一个日志文件中，以便内容回退。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`

### AGENTS.md

修改位置：第 1 节“适用范围与文件职责”及新增第 15 节“规则文件变更日志与回退”。

修改前原文：

```text
第 1 节没有“规则文件变更日志.md”的文件职责说明。
文件末尾没有“规则文件变更日志与回退”章节。
```

修改后原文：

```text
- `规则文件变更日志.md`：根目录唯一的规则变更日志，集中记录 `AGENTS.md` 和 `Cem Kaner.txt` 的每次修改及回退依据。

## 15. 规则文件变更日志与回退

对 `AGENTS.md` 或 `Cem Kaner.txt` 的任何新增、删除、修改或回退，必须在同一次任务中同步追加到根目录 `规则文件变更日志.md`。

- 根目录只维护这一个规则变更日志，不得按日期、文件或任务另建其他规则日志。
- 日志采用追加方式维护；已有记录不得删除、覆盖或改写。日志记录本身有误时，追加一条更正记录。
- 每条记录必须包含连续变更编号、修改时间、用户要求或修改原因、涉及文件、修改位置、修改前原文、修改后原文和明确的回退方法。
- 修改前先读取日志并确定下一个变更编号；修改后核对文件实际差异与日志记录一致。
- 执行回退也属于一次新改动，必须追加新的回退记录，并引用被回退的变更编号。
- 未同步写入日志的规则文件改动视为未完成，不得交付。
```

### Cem Kaner.txt

修改位置：文件开头的规则优先级说明之后。

修改前原文：

```text
本提示词必须遵守同目录 `AGENTS.md`。如有冲突，以 `AGENTS.md` 为准。
```

修改后原文：

```text
本提示词必须遵守同目录 `AGENTS.md`。如有冲突，以 `AGENTS.md` 为准。

对本文件或 `AGENTS.md` 的任何修改，必须按照 `AGENTS.md` 的规则文件变更要求，同步追加到根目录唯一日志 `规则文件变更日志.md`。
```

### 回退方法

1. 从 `AGENTS.md` 第 1 节删除 `规则文件变更日志.md` 的职责说明。
2. 从 `AGENTS.md` 删除完整的第 15 节“规则文件变更日志与回退”。
3. 从 `Cem Kaner.txt` 删除新增的日志同步要求行。
4. 不删除本条历史记录；追加一条引用 `RCL-0001` 的回退记录。

## RCL-0002 用项目名称作为测试用例目录名

- 修改时间：2026-08-27 14:14:43 CST
- 用户要求：把存放测试用例的文件夹目录名称改为项目名称。
- 修改原因：让用例目录可以直接识别项目归属，避免 UUID 目录不便查找和维护。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`work/liveshow-user-live-testcases/build-live-testcases.mjs`、`规则文件变更日志.md`
- 涉及目录：`outputs/01a040db-5c7a-7442-980a-beb6eb8b88d1`、`outputs/019fae8d-0fed-7f50-be8c-1b16f2b35120`

### AGENTS.md

修改位置：第 13 节“Excel 交付标准”。

修改前原文：

```text
第 13 节没有测试用例输出目录的命名规则。
```

修改后原文：

```text
- 测试用例统一保存到 `outputs/<项目名称>/`；`<项目名称>` 使用正式需求、原型说明或用户确认的实际项目名称，不得使用任务 ID、线程 ID、UUID、日期或“项目名称”等占位文本作为目录名。
- 项目名称无法从证据中确定时，必须先写入“需求待确认”或向用户确认，不得自行编造目录名。
- 同一项目的不同模块、版本和生成日期通过文件名区分，继续保存在同一个项目目录中。
```

### Cem Kaner.txt

修改位置：“输入材料”和“任务目标”。

修改前原文：

```text
输入材料没有项目名称字段。
任务目标没有 Excel 项目目录命名要求。
```

修改后原文：

```text
### 项目名称
<<PROJECT_NAME>>

6. Excel 交付时使用 `outputs/<项目名称>/` 作为项目目录；不得使用任务 ID、线程 ID、UUID、日期或占位文本作为目录名。同一项目的模块、版本和日期通过文件名区分。
```

### 目录及生成脚本

修改前：

```text
outputs/01a040db-5c7a-7442-980a-beb6eb8b88d1
outputs/019fae8d-0fed-7f50-be8c-1b16f2b35120
build-live-testcases.mjs 输出到 outputs/01a040db-5c7a-7442-980a-beb6eb8b88d1
```

修改后：

```text
outputs/Luma Live
outputs/UG 代理 App
build-live-testcases.mjs 输出到 outputs/Luma Live
```

### 回退方法

1. 从 `AGENTS.md` 第 13 节删除本记录新增的三条目录规则。
2. 从 `Cem Kaner.txt` 删除“项目名称”输入字段和任务目标第 6 条。
3. 将 `build-live-testcases.mjs` 的输出目录恢复为 `outputs/01a040db-5c7a-7442-980a-beb6eb8b88d1`。
4. 将 `outputs/Luma Live` 改回 `outputs/01a040db-5c7a-7442-980a-beb6eb8b88d1`，将 `outputs/UG 代理 App` 改回 `outputs/019fae8d-0fed-7f50-be8c-1b16f2b35120`。
5. 不删除本条历史记录；追加一条引用 `RCL-0002` 的回退记录。

## RCL-0003 为项目用例目录追加 -case 后缀

- 修改时间：2026-08-27 14:17:51 CST
- 用户要求：存放测试用例的项目目录名称需要追加 `-case` 后缀。
- 修改原因：通过统一后缀明确区分测试用例目录与项目源文件目录。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`work/liveshow-user-live-testcases/build-live-testcases.mjs`、`规则文件变更日志.md`
- 涉及目录：`outputs/Luma Live`、`outputs/UG 代理 App`

### AGENTS.md

修改位置：第 13 节“Excel 交付标准”的项目目录命名规则。

修改前原文：

```text
- 测试用例统一保存到 `outputs/<项目名称>/`；`<项目名称>` 使用正式需求、原型说明或用户确认的实际项目名称，不得使用任务 ID、线程 ID、UUID、日期或“项目名称”等占位文本作为目录名。
```

修改后原文：

```text
- 测试用例统一保存到 `outputs/<项目名称>-case/`；`<项目名称>` 使用正式需求、原型说明或用户确认的实际项目名称，目录名固定追加 `-case` 后缀，不得使用任务 ID、线程 ID、UUID、日期或“项目名称”等占位文本作为目录名。
```

### Cem Kaner.txt

修改位置：“任务目标”第 6 条。

修改前原文：

```text
6. Excel 交付时使用 `outputs/<项目名称>/` 作为项目目录；不得使用任务 ID、线程 ID、UUID、日期或占位文本作为目录名。同一项目的模块、版本和日期通过文件名区分。
```

修改后原文：

```text
6. Excel 交付时使用 `outputs/<项目名称>-case/` 作为项目目录，目录名固定追加 `-case` 后缀；不得使用任务 ID、线程 ID、UUID、日期或占位文本作为目录名。同一项目的模块、版本和日期通过文件名区分。
```

### 目录及生成脚本

修改前：

```text
outputs/Luma Live
outputs/UG 代理 App
build-live-testcases.mjs 输出到 outputs/Luma Live
```

修改后：

```text
outputs/Luma Live-case
outputs/UG 代理 App-case
build-live-testcases.mjs 输出到 outputs/Luma Live-case
```

### 回退方法

1. 将 `AGENTS.md` 第 13 节的目录格式恢复为 `outputs/<项目名称>/` 并移除固定 `-case` 后缀说明。
2. 将 `Cem Kaner.txt` 任务目标第 6 条的目录格式恢复为 `outputs/<项目名称>/` 并移除固定 `-case` 后缀说明。
3. 将 `build-live-testcases.mjs` 的输出目录恢复为 `outputs/Luma Live`。
4. 将 `outputs/Luma Live-case` 改回 `outputs/Luma Live`，将 `outputs/UG 代理 App-case` 改回 `outputs/UG 代理 App`。
5. 不删除本条历史记录；追加一条引用 `RCL-0003` 的回退记录。

## RCL-0004 统一测试用例 Excel 文件名称

- 修改时间：2026-08-27 14:23:13 CST
- 用户要求：具体测试用例 Excel 文件名称使用“端名-模块名-日期(yymmdd)-编号”。
- 修改原因：统一文件排序、项目端与模块识别以及同日版本追溯方式。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`work/liveshow-user-live-testcases/build-live-testcases.mjs`、`规则文件变更日志.md`
- 涉及目录：`outputs/Luma Live-case`、`outputs/UG 代理 App-case`

### AGENTS.md

修改位置：第 13 节“Excel 交付标准”的文件命名规则。

修改前原文：

```text
- 同一项目的不同模块、版本和生成日期通过文件名区分，继续保存在同一个项目目录中。
```

修改后原文：

```text
- Excel 测试用例文件名固定使用 `<端名>-<模块名>-<日期yymmdd>-<编号>.xlsx`，例如 `用户App-直播模块-260827-001.xlsx`。
- `端名` 和 `模块名` 使用正式需求、原型说明或用户确认的名称；日期使用文件生成日期的六位 `yymmdd` 格式。
- `编号` 固定为三位数字；同一项目目录内相同“端名 + 模块名 + 日期”组合从 `001` 连续递增。生成新文件前必须扫描已有编号并使用下一个编号，不得覆盖已有文件。
- 同一项目的不同端、模块、日期和版本通过文件名区分，继续保存在同一个项目目录中。
```

### Cem Kaner.txt

修改位置：“输入材料”和“任务目标”。

修改前原文：

```text
输入材料没有端名字段。
任务目标没有 Excel 文件名格式和编号规则。
```

修改后原文：

```text
### 端名
<<END_NAME>>

7. Excel 文件名固定使用 `<端名>-<模块名>-<日期yymmdd>-<三位编号>.xlsx`。同一项目目录内相同“端名 + 模块名 + 日期”组合从 `001` 连续递增；生成前扫描已有编号并使用下一个编号，不得覆盖已有文件。
```

### 现有 Excel 文件及生成脚本

修改前：

```text
Luma Live-case/用户App-直播模块-测试用例-20260827.xlsx
Luma Live-case/用户App-直播模块-测试用例-v2-20260827.xlsx
Luma Live-case/用户App-直播模块-测试用例-单预期结果版-20260827.xlsx
UG 代理 App-case/代理商流程与数据统计-测试用例-20260730-003433.xlsx
UG 代理 App-case/代理商流程与数据统计-CemKaner测试用例-20260730-014919.xlsx
build-live-testcases.mjs 输出：用户App-直播模块-测试用例-单预期结果版-20260827.xlsx
```

修改后：

```text
Luma Live-case/用户App-直播模块-260827-001.xlsx
Luma Live-case/用户App-直播模块-260827-002.xlsx
Luma Live-case/用户App-直播模块-260827-003.xlsx
UG 代理 App-case/UG代理App-代理商流程与数据统计-260730-001.xlsx
UG 代理 App-case/UG代理App-代理商流程与数据统计-260730-002.xlsx
build-live-testcases.mjs 输出：用户App-直播模块-260827-003.xlsx
```

配套的 `.xlsx.inspect.ndjson` 检查文件同步采用对应 Excel 文件名。

### 回退方法

1. 将 `AGENTS.md` 第 13 节新增的文件名格式、日期和编号规则恢复为修改前的一条说明。
2. 从 `Cem Kaner.txt` 删除“端名”输入字段和任务目标第 7 条。
3. 将 `build-live-testcases.mjs` 的输出文件名恢复为 `用户App-直播模块-测试用例-单预期结果版-20260827.xlsx`。
4. 按“修改前”清单恢复五个 Excel 文件名，并同步恢复三个 `.xlsx.inspect.ndjson` 文件名。
5. 不删除本条历史记录；追加一条引用 `RCL-0004` 的回退记录。

## RCL-0005 建立项目无关的四项生成门禁

- 修改时间：2026-08-27 17:44:46 CST
- 用户要求：将单一业务结果、P0 可执行性、同类覆盖完整性和证据充分性改成适用于所有项目的通用规则，并在以后生成用例时自动避免已发现的问题。
- 修改原因：原规则已有原则性说明，但项目生成脚本只检查数组长度、分隔符和少量连接词，无法阻止复合预期、模糊测试数据、同类状态漏测和只有代码依据的正式用例。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`scripts/validate-testcase-json.mjs`、`scripts/test-validate-testcase-json.mjs`、`规则文件变更日志.md`

### AGENTS.md

修改位置：第 1 节“适用范围与文件职责”。

修改前原文：

```text
- `AGENTS.md`：定义强制质量门禁、证据边界、统一字段和交付标准。
- `Cem Kaner.txt`：定义具体的 JSON 生成提示词，必须遵守本文件。
- `规则文件变更日志.md`：根目录唯一的规则变更日志，集中记录 `AGENTS.md` 和 `Cem Kaner.txt` 的每次修改及回退依据。
```

修改后原文：

```text
- `AGENTS.md`：定义强制质量门禁、证据边界、统一字段和交付标准。
- `Cem Kaner.txt`：定义具体的 JSON 生成提示词，必须遵守本文件。
- `scripts/validate-testcase-json.mjs`：执行所有项目共用的 JSON、证据、覆盖、单一结果和 P0 可执行性阻断检查。
- `<用例JSON文件名去扩展名>-质量上下文.json`：保存在对应 `work/` 目录，记录本次生成的证据索引和覆盖清单，不进入正式 Excel。
- `规则文件变更日志.md`：根目录唯一的规则变更日志，集中记录 `AGENTS.md` 和 `Cem Kaner.txt` 的每次修改及回退依据。
```

修改位置：第 3 节“标准工作流”以及新增第 3.1、3.2 节。

修改前原文：

```text
3. 建立证据索引，区分明确规则、来源冲突和证据缺口。
4. 建立覆盖清单：模块、功能结构、主流程、替代流程、状态、角色、数据校验、边界、异常、依赖和跨页面一致性。
5. 先生成有依据的可执行用例，再生成“需求待确认”记录。
6. 按业务验证点执行语义去重和来源合并。
7. 统一编号后生成 Excel。
8. 校验 JSON、工作表、字段、分类、重复项、可执行性、来源和工作簿格式。
```

修改后原文：

```text
3. 建立带唯一证据编号的证据索引，区分可证明业务预期的来源、辅助风险来源、来源冲突和证据缺口。
4. 根据当前功能识别适用的业务对象、角色、状态、操作方式、渠道、终态和数据校验维度，建立覆盖清单；每个覆盖项必须标记为已覆盖、不适用或待确认。
5. 将证据索引和覆盖清单写入对应 `work/` 目录的 `<用例JSON文件名去扩展名>-质量上下文.json`。
6. 先生成有依据的可执行用例，再生成“需求待确认”记录。
7. 对全部用例执行单一业务结果、可执行性、同类覆盖完整性和证据充分性四项通用门禁。
8. 按业务验证点执行语义去重和来源合并，统一编号。
9. 运行 `node scripts/validate-testcase-json.mjs <用例JSON> <质量上下文JSON>`；存在错误时必须修正并重新检查，不得生成 Excel。
10. 检查通过后生成 Excel，再校验工作表、字段、格式和最终文件。

新增第 3.1 节“四项通用生成门禁”：
- 使用独立失败判断识别复合预期，不按项目字段或连接词机械拆分。
- 根据实际操作反推所需数据；存在数据驱动动作的 P0 必须包含 `测试数据：` 记录。
- 对当前证据识别出的同类对象、角色、状态、操作、渠道和终态建立覆盖清单，不机械凑数。
- 每条正式用例必须至少引用一个可以证明业务预期的用户确认、正式需求、验收标准、接口契约或原型明确说明证据；只有代码、已有用例或测试启发式时转入需求待确认。

新增第 3.2 节“质量上下文格式”：
- `证据索引` 字段为 `证据编号, 来源类型, 位置, 可证明规则`。
- `覆盖清单` 字段为 `维度, 对象, 场景, 处理, 追溯`。
```

修改位置：第 4.1 节“备注”字段规则。

修改前原文：

```text
- `备注`：至少写明需求、原型、接口、代码、用户补充或已有用例中的实际来源；静态分析结论要说明未动态验证。
```

修改后原文：

```text
- `备注`：至少使用 `证据：E-xxx；来源类型：类型；位置：实际位置` 引用质量上下文中的来源；静态分析结论要说明未动态验证。
```

修改位置：第 14 节“交付前质量门禁”。

修改前原文：

```text
- 每条正式用例有来源；推断内容没有被写成无依据的确定性预期。
```

修改后原文：

```text
- 每条正式用例通过独立失败判断，只保留一个可独立判断的业务结果。
- 存在数据驱动动作的 P0 用例包含具体测试数据、生成规则或可定位引用，预期结果不是抽象的“成功”。
- 质量上下文中的覆盖项均有处理结论和追溯信息，没有无说明空白。
- 每条正式用例引用有效证据编号，并至少有一个可以证明业务预期的来源；代码、已有用例和测试启发式没有被单独写成确定性预期。
- 共用质量检查程序退出状态为成功；检查失败或缺少质量上下文时不得生成和交付 Excel。
```

### Cem Kaner.txt

修改位置：“输入材料”“任务目标”以及新增“生成前强制准备”。

修改前原文：

```text
### 需求、原型、接口、代码或用户补充证据
<<SOURCE_EVIDENCE_JSON>>

3. 只生成具有可执行步骤和有依据预期结果的正式用例。
```

修改后原文：

```text
### 需求、原型、接口、代码或用户补充证据
<<SOURCE_EVIDENCE_JSON>>

### 质量上下文 JSON
<<QUALITY_CONTEXT_JSON>>

3. 根据质量上下文中的证据索引和覆盖清单，只生成具有可执行步骤和有依据预期结果的正式用例。

新增“生成前强制准备”：生成正式用例前建立包含证据索引和覆盖清单的质量上下文；覆盖项只能为已覆盖、不适用或待确认，并提供追溯。
```

修改位置：“证据与推断规则”“用例粒度和语言”以及新增“可执行性门禁”“同类覆盖完整性门禁”。

修改前原文：

```text
- 需求、验收标准、接口契约、原型明确描述和用户确认可以作为预期依据。
- 每条用例只能有一个预期结果，该结果必须是一个可独立观察和判断的业务结果。
```

修改后原文：

```text
- 用户确认、正式需求、验收标准、正式接口契约和原型明确说明可以作为预期依据。
- 代码文件中的请求类、控制器和校验注解默认归类为代码；只有正式发布或明确确认为系统契约的材料才归类为接口契约。
- 已有用例和测试启发式只能用于发现风险、补充输入和识别覆盖缺口，不能单独证明业务预期。
- 每条正式用例至少引用一个能够证明其预期结果的证据编号；只有代码、已有用例或测试启发式时必须转入 `需求待确认`。
- 每条用例只能有一个预期结果，该结果必须是一个可独立观察和判断的业务结果。
- 对每条预期执行独立失败判断：如果其中一个观察点失败时其他观察点仍可能通过，必须拆成不同用例。

新增“可执行性门禁”：按实际步骤反推数据要求，P0 数据驱动用例必须包含 `测试数据：` 记录，模糊词只有在已有具体定义时才可保留。
新增“同类覆盖完整性门禁”：按当前业务识别同类对象和状态，逐项追溯到用例、问题或不适用依据，不机械凑数。
```

修改位置：“备注”规则、合法 JSON 示例以及新增“输出前强制自检”。

修改前原文：

```text
- `备注` 至少说明来源；静态代码分析内容注明“未动态验证”。
合法示例使用“有效账号”和普通来源文本。
```

修改后原文：

```text
- `备注` 至少使用 `证据：E-xxx；来源类型：类型；位置：实际位置` 引用质量上下文中的来源；静态代码分析内容注明“未动态验证”。
合法示例使用 `测试数据：账号引用数据集 TD-LOGIN-001` 和结构化证据引用。
新增七项输出前强制自检，并要求运行 `node scripts/validate-testcase-json.mjs <用例JSON> <质量上下文JSON>`；检查成功前不得生成 Excel。
```

### 共用质量检查程序

修改前原文：

```text
`scripts/validate-testcase-json.mjs` 和 `scripts/test-validate-testcase-json.mjs` 不存在。
```

修改后原文：

```text
新增 `scripts/validate-testcase-json.mjs`：
- 校验统一 JSON 字段、编号、分类和数组结构。
- 阻断明显复合预期和只描述“成功”的预期。
- 阻断存在数据驱动动作但没有 `测试数据：` 记录的 P0 用例。
- 校验证据编号、来源类型和业务预期证据资格。
- 校验覆盖清单的处理结论以及正式用例、待确认问题的双向追溯。
- 检查失败时返回非零退出状态，支持输出 JSON 检查报告。

新增 `scripts/test-validate-testcase-json.mjs`：验证正常用例、复合预期、P0 数据缺失、只有代码证据和覆盖追溯缺失五种行为。
```

### 回退方法

1. 从 `AGENTS.md` 第 1 节删除共用检查程序和质量上下文职责说明。
2. 将 `AGENTS.md` 第 3 节恢复为原 8 步工作流，删除第 3.1 节“四项通用生成门禁”和第 3.2 节“质量上下文格式”。
3. 将 `AGENTS.md` 第 4.1 节备注规则和第 14 节质量门禁恢复为本记录中的修改前原文。
4. 从 `Cem Kaner.txt` 删除质量上下文输入、生成前准备、四项门禁补充和输出前强制自检，恢复证据、备注及合法示例的修改前原文。
5. 删除 `scripts/validate-testcase-json.mjs` 和 `scripts/test-validate-testcase-json.mjs`。
6. 不删除本条历史记录；执行回退时追加一条引用 `RCL-0005` 的新记录。

## RCL-0006 增加测试层级隔离、证据适用性和独立覆盖门禁

- 修改时间：2026-08-27 19:51:56 CST
- 用户要求或修改原因：统一解决车辆 SKU 价格用例中功能测试混入接口地址、主流程缺少持久化验证、拆分用例无法独立执行、阻止类断言过弱、历史材料冲突未处理、测试数据不可重复以及覆盖清单从用例反向生成等问题，并将解决方案改为所有项目可复用的规则和自动门禁。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`scripts/validate-testcase-json.mjs`、`scripts/test-validate-testcase-json.mjs`

### AGENTS.md

修改位置：第 1 节质量上下文职责、第 2.1 节证据冲突、第 3 节标准工作流、第 3.1 节通用门禁、第 3.2 节质量上下文格式、第 6 节拆分规则、第 14 节质量门禁。

修改前原文：

```text
- `<用例JSON文件名去扩展名>-质量上下文.json`：保存在对应 `work/` 目录，记录本次生成的证据索引和覆盖清单，不进入正式 Excel。

代码当前实现不能单独证明业务预期。多个来源相互冲突时，不得自行选择其中一个，必须写入“需求待确认”。

4. 根据当前功能识别适用的业务对象、角色、状态、操作方式、渠道、终态和数据校验维度，建立覆盖清单；每个覆盖项必须标记为已覆盖、不适用或待确认。
5. 将证据索引和覆盖清单写入对应 `work/` 目录的 `<用例JSON文件名去扩展名>-质量上下文.json`。

- 操作步骤包含填写、输入、选择、上传、调用、扫描等数据驱动动作时，必须提供具体值、明确的数据生成规则或可定位的测试数据引用。
- P0 用例存在数据驱动动作时，前置条件中至少包含一项以 `测试数据：` 开头的记录，写明本次核心输入或测试数据引用。

- `证据索引`：对象数组，每项字段为 `证据编号, 来源类型, 位置, 可证明规则`。
- `覆盖清单`：对象数组，每项字段为 `维度, 对象, 场景, 处理, 追溯`。

- 拆分后的用例可以复用前置条件和操作步骤，但“验证用例子项”和“预期结果”必须对应本条唯一观察点。
```

修改后原文：

```text
- `<用例JSON文件名去扩展名>-质量上下文.json`：保存在对应 `work/` 目录，记录本次生成的测试范围、证据索引、需求清单和覆盖清单，不进入正式 Excel。

代码当前实现不能单独证明业务预期。多个来源相互冲突时，只有证据明确说明适用范围、生效版本或替代关系时，才能据此判定当前适用规则；不得仅按文件日期新旧自行选择。无法确定时必须写入“需求待确认”。

4. 先从证据独立提取需求、规则和风险，建立需求清单；不得从已生成用例反向拼接需求清单。
5. 根据测试范围和需求清单识别适用的业务对象、角色、状态、操作方式、渠道、终态和数据校验维度，建立覆盖清单；不得用逐条复制用例编号的方式伪造覆盖分析。
6. 将测试范围、证据索引、需求清单和覆盖清单写入对应 `work/` 目录的 `<用例JSON文件名去扩展名>-质量上下文.json`。

- 操作步骤包含填写、输入、选择、上传、调用、扫描、新增、创建、编辑、修改、关联或绑定等数据驱动动作时，不分优先级，前置条件中至少包含一项以 `测试数据：` 或 `复用数据：` 开头的记录，提供具体值、明确的数据生成规则或可定位的测试数据引用。
- 新增、创建、关联或绑定等会占用唯一数据的场景，必须写明时间戳、随机值、未占用数据池等可重复生成规则，或者写明清理、释放、恢复方法。
- 拆分预期结果后，每条用例仍必须独立可执行：前置条件和步骤要完整说明本条所需状态、数据和提交动作，不得依赖上一条用例已经执行或使用“按主流程提交”等不可定位表述。
- 对保存、提交、创建、编辑等改变业务数据的流程，至少应有一条独立用例通过刷新、重新进入、列表或详情回显验证持久化结果；只验证弹窗、提示或跳转不能替代持久化验证。
- 阻止类场景不能只用“成功弹窗未显示”“页面未跳转”作为唯一结果，应验证明确的字段提示、业务提示，或刷新后记录数量、目标值、唯一记录未发生变化等可判断结果。

- `测试范围`：对象，字段为 `项目名称, 端名, 模块名, 测试层级, 包含范围, 排除范围`。
- `证据索引`：对象数组，每项字段为 `证据编号, 来源类型, 位置, 可证明规则, 适用性, 适用说明`。
- `需求清单`：对象数组，每项字段为 `需求编号, 来源证据, 业务对象, 规则或风险, 处理, 追溯`。
- `覆盖清单`：对象数组，每项字段为 `维度, 对象, 场景, 处理, 追溯`。

- 拆分后的用例可以复用完整的前置条件和操作步骤，但不得依赖其他用例先执行；“验证用例子项”和“预期结果”必须对应本条唯一观察点。

新增“测试层级隔离门禁”：质量上下文声明测试层级；业务功能用例不得直接混入 HTTP 方法、接口地址、原始请求字段或原始响应字段；接口验证必须单独纳入接口测试范围。
```

### Cem Kaner.txt

修改位置：“任务目标”“生成前强制准备”“证据与推断规则”“用例粒度和语言”“可执行性门禁”“同类覆盖完整性门禁”“测试层级隔离门禁”“输出前强制自检”。

修改前原文：

```text
3. 根据质量上下文中的证据索引和覆盖清单，只生成具有可执行步骤和有依据预期结果的正式用例。

- `证据索引`：每项字段为 `证据编号, 来源类型, 位置, 可证明规则`。
- `覆盖清单`：每项字段为 `维度, 对象, 场景, 处理, 追溯`。

- 步骤包含填写、输入、选择、上传、调用、扫描等数据驱动动作时，提供具体值、明确生成规则或可定位的测试数据引用。
- P0 用例存在数据驱动动作时，前置条件至少包含一项以 `测试数据：` 开头的记录。

3. P0 数据判断：存在数据驱动动作时是否包含 `测试数据：` 记录，预期是否具有明确观察对象。
4. 覆盖追溯判断：质量上下文的每个覆盖项是否追溯到用例、问题或不适用依据。
```

修改后原文：

```text
3. 根据质量上下文中的测试范围、证据索引、需求清单和覆盖清单，只生成具有可执行步骤和有依据预期结果的正式用例。

- `测试范围`：字段为 `项目名称, 端名, 模块名, 测试层级, 包含范围, 排除范围`。
- `证据索引`：每项字段为 `证据编号, 来源类型, 位置, 可证明规则, 适用性, 适用说明`。
- `需求清单`：每项字段为 `需求编号, 来源证据, 业务对象, 规则或风险, 处理, 追溯`。
- `覆盖清单`：每项字段为 `维度, 对象, 场景, 处理, 追溯`。

- 步骤包含填写、输入、选择、上传、调用、扫描、新增、创建、编辑、修改、关联或绑定等数据驱动动作时，不分优先级，前置条件至少包含一项以 `测试数据：` 或 `复用数据：` 开头的记录，并提供具体值、明确生成规则或可定位的测试数据引用。
- 新增、创建、关联或绑定等会占用唯一数据的场景，写明时间戳、随机值、未占用数据池等可重复生成规则，或者写明清理、释放、恢复方法。
- 保存、提交、创建、编辑等改变业务数据的流程，至少设计一条通过刷新、重新进入、列表或详情回显验证持久化结果的独立用例。
- 阻止类场景不得只用“成功弹窗未显示”或“页面未跳转”作为唯一结果。

新增“测试层级隔离门禁”以及九项输出前强制自检，检查测试数据可重复性、持久化观察、阻止类强断言、独立需求清单、证据适用性和测试层级隔离。
```

### 共用质量检查程序

修改前原文：

```text
校验器仅检查两个质量上下文数组、P0 测试数据、证据来源资格、单一预期和覆盖双向追溯；测试脚本包含 5 项检查。
```

修改后原文：

```text
质量上下文固定为测试范围、证据索引、需求清单和覆盖清单。
校验器新增所有优先级的数据门禁、唯一数据可重复性、跨用例依赖、持久化观察、阻止类强断言、测试层级隔离、证据适用性、需求双向追溯和反向覆盖占位检查。
测试脚本扩展为 11 项检查，覆盖上述主要阻断行为。
```

### 回退方法

1. 将 `AGENTS.md` 的质量上下文恢复为仅包含证据索引和覆盖清单，并删除需求清单、证据适用性、可重复数据、独立执行、持久化观察、阻止类强断言和测试层级隔离规则。
2. 将 `AGENTS.md` 的来源冲突规则恢复为所有冲突直接进入“需求待确认”，将数据驱动门禁恢复为仅强制 P0 使用 `测试数据：`。
3. 将 `Cem Kaner.txt` 中对应的质量上下文、证据适用性、可执行性、覆盖和测试层级内容恢复为本记录中的修改前原文。
4. 将 `scripts/validate-testcase-json.mjs` 恢复为 RCL-0005 的两数组质量上下文、P0 数据和基础覆盖检查版本；将 `scripts/test-validate-testcase-json.mjs` 恢复为 5 项检查版本。
5. 不删除本条历史记录；执行回退时追加一条引用 `RCL-0006` 的新记录。

## RCL-0007 回退 RCL-0005 与 RCL-0006 规则升级

- 修改时间：2026-08-28 10:02:16 CST
- 用户要求：规则文件回退两个版本。
- 回退目标：恢复到 `RCL-0004` 完成后的状态。
- 被回退变更：`RCL-0005`、`RCL-0006`。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`scripts/validate-testcase-json.mjs`、`scripts/test-validate-testcase-json.mjs`
- 保留内容：`RCL-0001` 至 `RCL-0004` 的唯一日志制度、项目目录名、`-case` 后缀和 Excel 文件命名规则；历史日志和已生成 Excel 不删除。

### AGENTS.md

修改位置：第 1 节文件职责、第 2.1 节证据冲突、第 3 节工作流及原第 3.1/3.2 节、第 4.1 节备注规则、第 6 节拆分规则、第 14 节质量门禁。

修改前原文：

```text
- `scripts/validate-testcase-json.mjs`：执行所有项目共用的 JSON、证据、覆盖、单一结果和 P0 可执行性阻断检查。
- `<用例JSON文件名去扩展名>-质量上下文.json`：保存在对应 `work/` 目录，记录本次生成的测试范围、证据索引、需求清单和覆盖清单，不进入正式 Excel。

代码当前实现不能单独证明业务预期。多个来源相互冲突时，只有证据明确说明适用范围、生效版本或替代关系时，才能据此判定当前适用规则；不得仅按文件日期新旧自行选择。无法确定时必须写入“需求待确认”。

3. 建立带唯一证据编号和适用性结论的证据索引，区分当前适用、辅助风险、历史不适用、来源冲突和证据缺口。
4. 先从证据独立提取需求、规则和风险，建立需求清单；不得从已生成用例反向拼接需求清单。
5. 根据测试范围和需求清单识别适用的业务对象、角色、状态、操作方式、渠道、终态和数据校验维度，建立覆盖清单；不得用逐条复制用例编号的方式伪造覆盖分析。
6. 将测试范围、证据索引、需求清单和覆盖清单写入对应 `work/` 目录的 `<用例JSON文件名去扩展名>-质量上下文.json`。
7. 先生成有依据的可执行用例，再生成“需求待确认”记录。
8. 对全部用例执行单一业务结果、可执行性、同类覆盖完整性、证据充分性和测试层级隔离五项通用门禁。
9. 按业务验证点执行语义去重和来源合并，统一编号。
10. 运行 `node scripts/validate-testcase-json.mjs <用例JSON> <质量上下文JSON>`；存在错误时必须修正并重新检查，不得生成 Excel。
11. 检查通过后生成 Excel，再校验工作表、字段、格式和最终文件。

原第 3.1 节包含单一业务结果、可执行性、同类覆盖完整性、证据充分性和测试层级隔离五项门禁；原第 3.2 节要求质量上下文包含测试范围、证据索引、需求清单和覆盖清单。

- `备注`：至少使用 `证据：E-xxx；来源类型：类型；位置：实际位置` 引用质量上下文中的来源；静态分析结论要说明未动态验证。

- 拆分后的用例可以复用完整的前置条件和操作步骤，但不得依赖其他用例先执行；“验证用例子项”和“预期结果”必须对应本条唯一观察点。

第 14 节要求质量上下文、证据适用性、测试层级、可重复数据、持久化观察和共用质量检查程序全部通过。
```

修改后原文：

```text
第 1 节不再声明共用质量检查程序和质量上下文文件职责。

代码当前实现不能单独证明业务预期。多个来源相互冲突时，不得自行选择其中一个，必须写入“需求待确认”。

3. 建立证据索引，区分明确规则、来源冲突和证据缺口。
4. 建立覆盖清单：模块、功能结构、主流程、替代流程、状态、角色、数据校验、边界、异常、依赖和跨页面一致性。
5. 先生成有依据的可执行用例，再生成“需求待确认”记录。
6. 按业务验证点执行语义去重和来源合并。
7. 统一编号后生成 Excel。
8. 校验 JSON、工作表、字段、分类、重复项、可执行性、来源和工作簿格式。

删除原第 3.1 节“五项通用生成门禁”和第 3.2 节“质量上下文格式”。

- `备注`：至少写明需求、原型、接口、代码、用户补充或已有用例中的实际来源；静态分析结论要说明未动态验证。

- 拆分后的用例可以复用前置条件和操作步骤，但“验证用例子项”和“预期结果”必须对应本条唯一观察点。

第 14 节恢复为结构、编号、分类、单一预期、来源、待确认、去重、计算、默认排除纯视觉以及工作簿格式检查，不再要求质量上下文或共用检查程序。
```

### Cem Kaner.txt

修改位置：“输入材料”“任务目标”“生成前强制准备”“证据与推断规则”“用例粒度和语言”“可执行性门禁”“同类覆盖完整性门禁”“测试层级隔离门禁”“备注”“输出前强制自检”和合法 JSON 示例。

修改前原文：

```text
### 质量上下文 JSON
<<QUALITY_CONTEXT_JSON>>

3. 根据质量上下文中的测试范围、证据索引、需求清单和覆盖清单，只生成具有可执行步骤和有依据预期结果的正式用例。

包含“生成前强制准备”，要求测试范围、证据索引、需求清单和覆盖清单。
包含证据适用性、独立失败判断、可执行性、同类覆盖完整性和测试层级隔离门禁。

- `备注` 至少使用 `证据：E-xxx；来源类型：类型；位置：实际位置` 引用质量上下文中的来源；静态代码分析内容注明“未动态验证”。

包含九项“输出前强制自检”，并强制运行共用 JSON 校验程序。
合法示例使用 `测试数据：账号引用数据集 TD-LOGIN-001` 和结构化证据引用。
```

修改后原文：

```text
输入材料不再包含质量上下文 JSON。

3. 只生成具有可执行步骤和有依据预期结果的正式用例。

删除“生成前强制准备”、可执行性门禁、同类覆盖完整性门禁、测试层级隔离门禁和输出前强制自检。

- 需求、验收标准、接口契约、原型明确描述和用户确认可以作为预期依据。
- 跨端状态流转按观察端拆分用例；拆分后的用例可以复用前置条件和操作步骤。
- `备注` 至少说明来源；静态代码分析内容注明“未动态验证”。

合法示例恢复为“有效账号和密码”及普通来源文本。
```

### 共用质量检查程序

修改前原文：

```text
存在 `scripts/validate-testcase-json.mjs` 和 `scripts/test-validate-testcase-json.mjs`，用于执行 RCL-0005、RCL-0006 的自动门禁。
```

修改后原文：

```text
删除 `scripts/validate-testcase-json.mjs` 和 `scripts/test-validate-testcase-json.mjs`，恢复到 RCL-0004 时不存在共用质量检查程序的状态。
```

### 回退方法

1. 如需撤销本次回退，按照 `RCL-0005` 的修改后原文恢复四项通用门禁、两数组质量上下文和 5 项校验程序。
2. 再按照 `RCL-0006` 的修改后原文恢复测试范围、需求清单、证据适用性、测试层级隔离、可重复数据、持久化观察及 11 项校验程序。
3. 恢复时不得删除或改写 `RCL-0007`；必须追加引用 `RCL-0007` 的新变更记录。

## RCL-0008 按功能复杂度控制测试数据准备粒度

- 修改时间：2026-08-28 10:33:57 CST
- 用户要求或修改原因：简单新增功能不应堆叠时间戳、虚构文件名和完整字段清单；统计、报表和计算类用例需要准备充分、可复算的数据。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`
- 修改位置：`AGENTS.md` 第 8 节后新增“8.1 测试数据准备粒度”；`Cem Kaner.txt` 的“测试设计覆盖”后新增“测试数据准备粒度”。

### 修改前原文

```text
AGENTS.md：第 8 节后直接进入“第 9 节 计算与统计规则”，没有按功能复杂度区分测试数据准备粒度的规则。

Cem Kaner.txt：“测试设计覆盖”后直接进入“用例粒度和语言”，没有按功能复杂度区分测试数据准备粒度的规则。
```

### 修改后原文

```text
测试数据的详细程度必须与被测功能对数据的依赖程度匹配，不得对所有用例套用同一种详细模板。

- 简单新增、编辑、删除、入口和字段展示用例，只写完成操作所需的最小有效数据；不逐项罗列与本条验证无关的字段值。
- 字段级校验只具体说明目标字段，其他必填字段统一写“填写合法值”或“保持已有合法值”。
- 普通业务用例不得直接保留 yyyyMMddHHmmss 等技术格式串，也不得引用未实际提供的虚构文件名。确需避免重名时，写“使用一个未被占用的名称”；确需指定文件时，写明可准备的文件类型、大小或实际数据位置。
- 统计、报表、金额和其他计算类用例必须充分准备可复算的数据，包括统计周期、业务状态、统计维度、纳入数据、排除数据、边界数据、原始数值、计算公式和最终数值。
- 审批、状态流转和跨端一致性用例按验证需要写明角色、来源状态、目标状态、变更前数据、变更后数据和观察端，不扩展无关字段。
- 只有当具体数据能影响预期结果、边界判断、数据唯一性或执行可复现性时才展开描述；否则使用简洁的合法数据条件。
```

### 回退方法

1. 从 `AGENTS.md` 删除“8.1 测试数据准备粒度”及其全部六条规则，使第 8 节后直接进入第 9 节。
2. 从 `Cem Kaner.txt` 删除“测试数据准备粒度”及其全部六条规则，使“测试设计覆盖”后直接进入“用例粒度和语言”。
3. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0008` 的新记录。

## RCL-0009 补充跨端跨模块流程与证据适用性规则

- 修改时间：2026-08-28 11:17:02 CST
- 用户要求或修改原因：修复现有执行流程和生成规则中拆分用例缺少流程关联、异步结果缺少判断标准、报表数据规则覆盖过宽、关联字段使用“合法值”可能不可复现、来源冲突一律待确认、跨模块共同业务对象缺失、待确认事项只能同页面合并以及质量门禁不检查完整链路的问题。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 2.1、3、4.1、6、8.1、11、14 节；`Cem Kaner.txt` 的“证据与推断规则”“测试数据准备粒度”“用例粒度和语言”“语义去重”“输出 JSON 结构”，并新增“跨端与跨模块流程”和“输出前检查”。

### 修改前原文

```text
AGENTS.md：
代码当前实现不能单独证明业务预期。多个来源相互冲突时，不得自行选择其中一个，必须写入“需求待确认”。

标准工作流只有覆盖清单、生成、去重、编号、Excel 和结构格式检查，没有跨端或跨模块轻量流程清单。

- `备注`：至少写明需求、原型、接口、代码、用户补充或已有用例中的实际来源；静态分析结论要说明未动态验证。

- 跨端状态流转按观察端拆分用例；每条用例只保留该端的一个预期结果。
- 拆分后的用例可以复用前置条件和操作步骤，但“验证用例子项”和“预期结果”必须对应本条唯一观察点。

第 6 节没有流程编号、共同业务对象、阶段顺序、异步时限、终态判断和链路完整性规则。

- 字段级校验只具体说明目标字段，其他必填字段统一写“填写合法值”或“保持已有合法值”。
- 统计、报表、金额和其他计算类用例必须充分准备可复算的数据，包括统计周期、业务状态、统计维度、纳入数据、排除数据、边界数据、原始数值、计算公式和最终数值。
- 审批、状态流转和跨端一致性用例按验证需要写明角色、来源状态、目标状态、变更前数据、变更后数据和观察端，不扩展无关字段。

- 多条同页面、同一证据缺口的待确认记录应合并。

第 14 节没有来源适用性、报表分类、跨端链路完整性和异步时效依据检查。

Cem Kaner.txt：
证据与推断规则没有明确来源替代关系和“新原型不能自动推翻旧业务规则”的判断方式。
测试数据准备粒度仍将全部统计、报表、金额和计算类用例统一要求为完整可复算数据。
没有跨端与跨模块流程清单、流程编号、共同业务对象、阶段关联、异步判断和流程完整性规则。
- 跨端状态流转按观察端拆分用例；拆分后的用例可以复用前置条件和操作步骤。
- 同页面、同一证据缺口的待确认事项：合并为一条。
没有针对上述缺口的输出前检查。
```

### 修改后原文

```text
AGENTS.md：
多个来源相互冲突时，不得仅按文件日期新旧自行选择；只有来源明确说明适用版本、生效时间、废止范围或替代关系，并且可以确认适用于当前任务时，才能采用当前有效规则并备注依据。无法确认时进入“需求待确认”。新原型只能证明页面结构和交互线索，不能仅凭更新时间自动推翻旧需求中的权限、状态、金额、计算或生效范围等业务规则。

标准工作流新增轻量流程清单：涉及跨端或跨模块流程时，记录流程编号、共同业务对象、触发端与角色、模块阶段顺序、来源状态、目标状态、观察端、证据和计划用例；不涉及时跳过。

跨端或跨模块拆分用例在备注中写明流程编号和当前流程阶段，不新增 Excel 列；前置条件写明可识别的同一业务对象以及会影响数据范围的必要条件。

新增“6.1 跨端与跨模块流程”：
- 只有证据表明业务经过两个及以上端或模块时才建立流程。
- 使用稳定且唯一的流程编号关联拆分用例。
- 按证据覆盖触发动作、中间状态、目标端终态和适用的失败恢复，缺口进入“需求待确认”或记录不适用依据。
- 异步同步、回调或延迟生效必须使用有依据的等待时限或终态判断条件；没有依据时不得编造“立即同步”或具体秒数。
- 重试、补偿、重复消息和乱序只在证据或实际依赖风险存在时生成。

测试数据准备粒度调整为：
- 只有不影响本条结果的其他必填字段才可以统一写“合法值”；影响计算、权限、状态或数据范围的关联字段必须写明必要条件。
- 聚合统计、金额和计算类用例提供完整可复算数据。
- 报表筛选和数据范围用例仅准备最小纳入、排除数据集及预期集合或数量。
- 报表字段展示、导航、下载入口等非计算用例使用最小有效数据，不强制公式和完整原始数据。
- 审批、状态流转和跨端一致性用例补充共同业务对象。

同一业务规则或同一证据缺口的待确认记录跨页面、跨模块合并；角色、适用范围或规则不同的记录不强行合并。

第 14 节新增来源适用性、报表分类、流程编号、阶段关联、共同业务对象、链路完整性和异步时效依据检查。

Cem Kaner.txt：
同步增加以上证据冲突、数据粒度、跨端跨模块流程、用例关联和跨模块待确认合并规则。
新增“输出前检查”，强制检查来源适用性、单一预期、报表分类、跨端链路完整性、异步时效依据和待确认合并。
```

### 回退方法

1. 将 `AGENTS.md` 第 2.1 节来源冲突规则恢复为“多个来源相互冲突时，不得自行选择其中一个，必须写入需求待确认”。
2. 从 `AGENTS.md` 标准工作流删除轻量流程清单步骤并恢复原 8 步编号；将备注恢复为仅要求来源和静态分析说明；删除“6.1 跨端与跨模块流程”。
3. 将 `AGENTS.md` 第 8.1 节恢复为 RCL-0008 的六条原规则；将第 11 节恢复为仅合并同页面、同一证据缺口；删除第 14 节本次新增的四项检查。
4. 从 `Cem Kaner.txt` 删除本次新增的来源替代与新原型边界规则，恢复 RCL-0008 的测试数据六条规则，删除“跨端与跨模块流程”和“输出前检查”，并恢复原跨端拆分、待确认合并及备注文本。
5. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0009` 的新记录。

## RCL-0010 将流程编号改为独立字段和 Excel 列

- 修改时间：2026-08-28 11:54:54 CST
- 用户要求或修改原因：用户在车辆 SKU 价格用例中无法直观看到流程编号，要求将流程编号从备注文本中拆出，作为“功能测试用例”的独立列，方便筛选、排序和关联跨端、跨模块流程。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 4.1、6.1、12、13、14 节；`Cem Kaner.txt` 的“跨端与跨模块流程”“用例粒度和语言”“输出前检查”“输出 JSON 结构”和合法 JSON 示例。

### 修改前原文

```text
“功能测试用例”固定使用 14 列：
序号, 用例编号, 功能模块, 功能结构, 用例类型, 优先级, 用例描述, 验证用例子项, 前置条件, 操作步骤, 预期结果, 测试结果, 测试人员, 备注

跨端或跨模块拆分用例将流程编号和流程阶段写入备注，不新增 Excel 列。

旧字段转换器只补充序号、用例编号、测试结果和测试人员。
```

### 修改后原文

```text
“功能测试用例”固定使用 15 列：
序号, 用例编号, 流程编号, 功能模块, 功能结构, 用例类型, 优先级, 用例描述, 验证用例子项, 前置条件, 操作步骤, 预期结果, 测试结果, 测试人员, 备注

流程编号仅用于有证据的跨端或跨模块流程；同一业务流程的拆分用例填写相同稳定编号，普通单模块用例留空。备注保留当前流程阶段、共同业务对象和来源，不再承担流程编号字段职责。

旧字段转换器补充流程编号；旧备注已有明确流程编号时迁移到独立字段，没有明确流程证据时留空，不自行推断。
```

### 回退方法

1. 将 `AGENTS.md` 第 4.1 节恢复为 14 列，删除“流程编号”字段要求，并将跨端或跨模块的流程编号与阶段重新写入备注。
2. 将 `AGENTS.md` 第 6.1、12、13、14 节恢复为 RCL-0009 修改后的文本，删除独立流程编号列的兼容和质量门禁要求。
3. 将 `Cem Kaner.txt` 的测试用例对象恢复为 14 字段，从合法 JSON 示例删除 `流程编号`，并将跨端流程关联方式恢复为备注中的流程编号和阶段。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0010` 的新记录。

## RCL-0011 调整流程编号列的位置

- 修改时间：2026-08-28 12:04:02 CST
- 用户要求或修改原因：用户要求将独立的“流程编号”列从“用例编号”之后移动到“预期结果”之后，使流程关联信息与业务观察结果相邻。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 4.1 节的 15 列顺序和流程编号说明；`Cem Kaner.txt` 的测试用例对象字段顺序和合法 JSON 示例。

### 修改前原文

```text
序号, 用例编号, 流程编号, 功能模块, 功能结构, 用例类型, 优先级, 用例描述, 验证用例子项, 前置条件, 操作步骤, 预期结果, 测试结果, 测试人员, 备注

合法 JSON 示例中的“流程编号”位于“用例编号”之后。
```

### 修改后原文

```text
序号, 用例编号, 功能模块, 功能结构, 用例类型, 优先级, 用例描述, 验证用例子项, 前置条件, 操作步骤, 预期结果, 流程编号, 测试结果, 测试人员, 备注

合法 JSON 示例中的“流程编号”位于“预期结果”之后。
```

### 回退方法

1. 将 `AGENTS.md` 第 4.1 节中的“流程编号”恢复到“用例编号”之后，并删除“位于预期结果之后”的说明。
2. 将 `Cem Kaner.txt` 的字段清单和合法 JSON 示例中的“流程编号”恢复到“用例编号”之后。
3. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0011` 的新记录。

## RCL-0012 增加业务结果完整性扫描和待确认分类门禁

- 修改时间：2026-08-28 18:10:48 CST
- 用户要求或修改原因：用户指出“需求待确认”不能只针对单个场景补问题，需要通用识别每个操作可能遗漏的反馈、状态、生效范围、跨端影响和异常恢复，并区分已有依据但漏写用例的覆盖缺口与真正需要产品确认的需求缺口。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 3、4.2、13、14 节并新增第 3.1 节；`Cem Kaner.txt` 的“任务目标”“证据与推断规则”“输出前检查”“需求待确认对象字段”和合法 JSON 示例，并新增“业务结果完整性扫描”。

### 修改前原文

```text
AGENTS.md：
4. 建立覆盖清单：模块、功能结构、主流程、替代流程、状态、角色、数据校验、边界、异常、依赖和跨页面一致性。
5. 涉及跨端或跨模块流程时，先建立轻量流程清单。
6. 先生成有依据的可执行用例，再生成“需求待确认”记录。

没有逐操作检查操作反馈、业务状态、生效范围、角色/端影响、异常恢复和过程态的通用扫描，也没有区分“已有明确结果但漏写用例”和“缺少确定产品规则”。

“需求待确认”固定使用 8 列：
问题编号, 功能模块, 功能结构, 待确认事项, 已知依据, 缺失信息, 影响用例, 确认状态

Excel 交付规则要求“需求待确认”严格使用 8 列，没有“问题分类”单选列及分类合法性检查。

Cem Kaner.txt：
任务目标只要求生成有依据的正式用例并把证据不足内容放入需求待确认，没有业务结果完整性扫描。
静态截图没有展示瞬时提示或操作后状态时，没有强制继续检查动态交互脚本、状态、标注和提示文案。
需求待确认对象固定使用 8 个字段，没有“问题分类”。
```

### 修改后原文

```text
AGENTS.md：
标准工作流在覆盖清单中增加操作反馈、生效范围和历史数据影响，并对每个用户操作、系统动作和状态变更执行“业务结果完整性扫描”。

新增“3.1 业务结果完整性扫描”，按实际风险检查：
1. 操作反馈。
2. 业务状态和数据。
3. 生效时间和范围。
4. 角色、端和模块影响。
5. 异常和恢复。
6. 并发和过程态。

每个适用观察点执行三向判定：
- 证据已明确结果时生成或拆分正式用例；遗漏时属于用例覆盖缺口，不得写成需求待确认。
- 结果影响业务验收但证据缺少确定规则或来源冲突时，生成独立需求待确认，不得用常见行为补写规则。
- 与当前功能无关、没有实际风险或明确不适用时，不生成用例或待确认问题，不得为了填满分类制造问题。

静态截图未展示操作后的瞬时状态，不等于原型没有定义；判定缺口前继续检查原型交互脚本、动态状态、便签、标注和提示文案。

“需求待确认”固定使用 9 列：
问题编号, 功能模块, 功能结构, 问题分类, 待确认事项, 已知依据, 缺失信息, 影响用例, 确认状态

“问题分类”只允许以下 10 种单选值：需求范围、业务规则、角色与权限、流程与状态、字段与数据校验、计算与统计口径、异常处理、跨端与跨模块一致性、配置和历史数据影响、交互与文案规则。

Excel 为“问题分类”和“确认状态”配置单选数据验证；质量门禁检查结果完整性、覆盖缺口与需求缺口未混用、分类取值合法且没有为凑分类制造问题。

Cem Kaner.txt：
同步增加业务结果完整性扫描、三向判定、动态原型证据检查、输出前结果完整性检查和 9 字段需求待确认结构；合法 JSON 示例新增“问题分类”。
```

### 回退方法

1. 从 `AGENTS.md` 标准工作流删除业务结果完整性扫描步骤，将覆盖清单恢复为不包含操作反馈、生效范围和历史数据影响，并恢复原 9 步编号。
2. 删除 `AGENTS.md` 第 3.1 节全部内容。
3. 将 `AGENTS.md` 第 4.2 节恢复为 8 列，删除“问题分类”字段、10 种分类和分类规则；第 13 节恢复为需求待确认严格使用 8 列，并删除问题分类单选要求。
4. 从 `AGENTS.md` 第 14 节删除本次新增的结果完整性、覆盖缺口与需求缺口区分、问题分类合法性检查，并将最后一条恢复为原工作簿格式检查文本。
5. 从 `Cem Kaner.txt` 删除任务目标中的业务结果完整性扫描、证据规则中的动态原型补查规则、完整的“业务结果完整性扫描”章节及输出前对应检查。
6. 将 `Cem Kaner.txt` 的需求待确认对象恢复为 8 字段，删除“问题分类”字段、允许值规则和合法 JSON 示例中的“问题分类”。
7. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0012` 的新记录。

## RCL-0013 将需求待确认改为产品决策清单

- 修改时间：2026-08-28 18:44:21 CST
- 用户要求或修改原因：用户从产品经理视角指出原“需求待确认”把多个问题和分析工作集中交给产品，难以逐行回答；要求落实单决策问题、可选方案、测试建议、产品结论、阻塞等级、负责人、确认时点和默认排序等全部产品友好交付要求。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 3、3.1、4.2、11、12、13、14 节；`Cem Kaner.txt` 的“任务目标”“语义去重”“输出前检查”“需求待确认对象字段”和合法 JSON 示例，并新增“产品决策型需求待确认”。

### 修改前原文

```text
“需求待确认”固定使用 9 列：
问题编号, 功能模块, 功能结构, 问题分类, 待确认事项, 已知依据, 缺失信息, 影响用例, 确认状态

同一业务规则或同一证据缺口跨页面、跨模块合并，但没有限制一条记录只能要求做一个决定。

没有阻塞等级、具体场景、可选方案、测试建议、产品结论、影响范围、负责人和期望确认时间字段。

Excel 只要求“问题分类”和“确认状态”单选，没有默认阻塞排序、产品结论可编辑标识和旧 9 字段拆分转换规则。
```

### 修改后原文

```text
“需求待确认”固定使用 15 列：
问题编号, 阻塞等级, 功能模块, 具体场景, 问题分类, 待决策问题, 可选方案, 测试建议, 产品结论, 已知依据, 影响范围, 影响用例, 负责人, 期望确认时间, 确认状态

每条记录只要求做一个业务决定；一个原始问题包含多个决定时拆分为子编号，保留原编号前缀追溯。

可选方案使用 2 至 4 个互斥选项；测试必须给出有理由的推荐方案，但不得代替产品填写结论。产品结论初次交付留空，是产品主要填写列。

阻塞等级只允许阻塞测试、部分阻塞、不阻塞；负责人只允许产品、交互、技术、多方确认；确认状态增加确认中。期望确认时间使用有依据的日期或项目里程碑，不编造日期。

Excel 对阻塞等级、问题分类、负责人和确认状态配置单选，默认按阻塞等级排序并冻结左侧关键列，产品结论列使用可编辑底色。

旧 8 或 9 字段待确认记录必须转换为新结构，拆分独立决定并使用子编号追溯。
```

### 回退方法

1. 将 `AGENTS.md` 第 4.2 节恢复为 RCL-0012 的 9 列字段和原问题分类、确认状态规则，删除产品决策型字段、单决策拆分、阻塞排序和负责人规则。
2. 从 `AGENTS.md` 第 3、3.1、11、12、13、14 节删除本次新增的单决策、旧字段转换、四列单选、产品结论、排序和质量门禁要求，恢复 RCL-0012 修改后的文本。
3. 从 `Cem Kaner.txt` 删除“产品决策型需求待确认”，将待确认对象和合法 JSON 示例恢复为 RCL-0012 的 9 字段结构，并恢复原语义去重和输出前检查文本。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0013` 的新记录。

## RCL-0014 增加待确认问题的需求组和父子追问关系

- 修改时间：2026-08-29 09:49:47 CST
- 用户要求或修改原因：用户指出同一父需求下可能存在依赖父结论的追问子问题，现有按单行阻塞等级排序会导致子问题排在父问题前面，并且只有编号前缀无法准确表达问题关联；要求建立显式父子关系，保证父问题先于子问题且能够互相追溯。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 3、4.2、11、12.1、13、14 节；`Cem Kaner.txt` 的“产品决策型需求待确认”“语义去重”“输出前检查”“需求待确认对象字段”和合法 JSON 示例。

### 修改前原文

```text
“需求待确认”固定使用 15 列：
问题编号, 阻塞等级, 功能模块, 具体场景, 问题分类, 待决策问题, 可选方案, 测试建议, 产品结论, 已知依据, 影响范围, 影响用例, 负责人, 期望确认时间, 确认状态

一个原始问题拆成多个决策点时，仅通过 Q-003-01、Q-003-02 等相同编号前缀追溯。

确认状态默认待确认，允许待确认、确认中、已确认、无需处理。

需求待确认逐行按阻塞测试、部分阻塞、不阻塞排序；同级按功能模块、具体场景和问题编号排序。

旧 8 或 9 字段记录转换为 15 字段结构，没有需求组、直接父问题、追问触发条件、孤儿或循环依赖校验。
```

### 修改后原文

```text
“需求待确认”固定使用 18 列：
问题编号, 需求组编号, 父问题编号, 追问触发条件, 阻塞等级, 功能模块, 具体场景, 问题分类, 待决策问题, 可选方案, 测试建议, 产品结论, 已知依据, 影响范围, 影响用例, 负责人, 期望确认时间, 确认状态

同一父需求或证据缺口使用 RQ-003 格式的需求组编号；同组只表示业务关联，不自动表示回答依赖。真正依赖前置结论的子问题必须填写同组内存在的直接父问题编号和可判断的追问触发条件。

根问题使用 Q-003，一级追问使用 Q-003-01，确有连续条件链时允许 Q-003-01-01；默认只使用一级追问，超过二级时拆成新需求组。不得因编号相近、排列相邻或场景相关而虚构父子关系。

根问题和并列问题初始状态为待确认；存在未确认父问题的子问题为待前置结论。父结论触发追问时改为待确认，使追问不适用时改为无需处理。

排序先按组内最高阻塞等级排列需求组，再在组内执行父问题优先的树形前序排列。父问题必须先于并紧邻全部子问题，不得按单行阻塞等级打散问题树。

旧 8、9 或 15 字段记录转换时补充需求组关系；只有文字明确存在前置依赖时才建立父子关系。质量门禁检查同组引用、自引用、孤儿引用、循环依赖、追问触发条件、父子顺序和状态。
```

### 回退方法

1. 将 `AGENTS.md` 第 4.2 节恢复为 RCL-0013 的 15 列字段，删除“需求组编号”“父问题编号”“追问触发条件”及父子编号、触发状态和树形排序规则。
2. 将 `AGENTS.md` 第 3、11、12.1、13、14 节恢复为 RCL-0013 修改后的文本，删除需求组、父子依赖、`待前置结论`、关系视觉展示和父子合法性门禁。
3. 从 `Cem Kaner.txt` 删除需求组、父问题、追问触发条件、父子状态流转和树形排序规则，将待确认对象及 JSON 示例恢复为 RCL-0013 的 15 字段结构。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0014` 的新记录。

## RCL-0015 增加产品决策概览并简化填写视图

- 修改时间：2026-08-29 10:22:05 CST
- 用户要求或修改原因：用户从产品经理视角确认现有“需求待确认”信息过宽、追问过多且回答成本高，要求在保留测试追溯和父子关系的前提下，提供更适合产品集中决策的概览、默认折叠和标准化填写方式。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 4.2、12.1、13、14 节；`Cem Kaner.txt` 的“产品决策型需求待确认”“输出前检查”“需求待确认对象字段”和合法 JSON 示例。

### 修改前原文

```text
“需求待确认”固定使用 18 列，没有“结论补充”。产品结论允许填写选中的方案及必要补充，没有固定单选值。

工作簿固定包含“功能测试用例”和“需求待确认”两个工作表，产品需要在完整明细宽表中逐行判断和填写。

关系字段保持可见，子问题只有缩进或底色区分，没有默认折叠；测试追溯列没有默认隐藏规则。

没有独立的产品决策进度、阻塞等级和负责人汇总视图，也没有要求汇总结果通过公式与明细实时一致。
```

### 修改后原文

```text
“需求待确认”固定使用 19 列，在“产品结论”后增加“结论补充”。产品结论只能单选 A、B、C、D、其他；选择“其他”时必须填写结论补充，其他选项仅在需要限定适用范围或生效时间时补充。

工作簿固定包含“功能测试用例”“需求待确认”和“产品决策概览”三个工作表。“需求待确认”是唯一可编辑数据源，“产品决策概览”通过公式实时汇总并作为默认打开工作表。

“需求待确认”默认隐藏功能模块、问题分类、已知依据、影响范围和影响用例等测试追溯列，但保留并允许取消隐藏；子问题使用 Excel 分级显示默认折叠，父问题保持可见且展开后顺序不变。

“产品决策概览”汇总问题总数、当前可回答、待前置结论、确认中、已确认、无需处理，并按阻塞等级和负责人统计当前待确认数量。
```

### 回退方法

1. 将 `AGENTS.md` 第 4.2 节恢复为 RCL-0014 的 18 列字段，删除“结论补充”及产品结论固定单选规则；第 12.1 节删除旧 18 字段兼容说明。
2. 将 `AGENTS.md` 第 13、14 节恢复为两个工作表、四个单选字段、关系字段可见且不要求概览、默认隐藏列和子问题分级折叠的 RCL-0014 文本。
3. 从 `Cem Kaner.txt` 删除“结论补充”字段及示例，恢复产品结论可填写选中方案和必要补充的原规则。
4. 重建工作簿时删除“产品决策概览”，取消默认隐藏列和行分级显示，并将“需求待确认”恢复为 18 列及 RCL-0014 的数据验证。
5. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0015` 的新记录。

## RCL-0016 改良需求待确认的决策表达和用例影响追溯

- 修改时间：2026-08-29 12:33:18 CST
- 用户要求或修改原因：用户要求通用改良“需求待确认”中的抽象词、缺失参数、非互斥选项、逃生选项、过宽适用范围、伪父子关系、历史数据边界和影响用例混写问题，并要求以后生成的所有表格同样适用。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 4.2、12.1、13、14 节；`Cem Kaner.txt` 的“产品决策型需求待确认”“输出前检查”“需求待确认对象字段”和合法 JSON 示例。

### 修改前原文

```text
“需求待确认”固定使用 19 列：
问题编号, 需求组编号, 父问题编号, 追问触发条件, 阻塞等级, 功能模块, 具体场景, 问题分类, 待决策问题, 可选方案, 测试建议, 产品结论, 结论补充, 已知依据, 影响范围, 影响用例, 负责人, 期望确认时间, 确认状态

父问题编号用于依赖前置结论的问题；追问触发条件只要求写明由父问题的哪个结论或业务条件触发。

可选方案使用 2 至 4 个互斥方案，允许“其他：请补充规则”，但没有明确禁止占位参数、可并存控制措施和“各功能单独定义”等逃生选项。

已知依据、影响范围、影响用例使用字符串数组；影响用例优先填写准确编号，没有编号时写明待补用例范围。

旧 8、9、15 或 18 字段待确认记录允许转换为当前结构。
```

### 修改后原文

```text
“需求待确认”固定使用 20 列：
问题编号, 需求组编号, 父问题编号, 追问触发条件, 阻塞等级, 功能模块, 具体场景, 问题分类, 待决策问题, 可选方案, 测试建议, 产品结论, 结论补充, 已知依据, 影响范围, 已有用例编号, 确认后待补用例, 负责人, 期望确认时间, 确认状态

只有子问题是否适用或可选方案会随父结论变化时才建立父子关系；触发条件必须指向具体父选项或可观察业务条件，不能只写“确认后”“范围明确后”。

具体场景、问题和影响范围必须列明角色、状态、动作、端、操作或数据对象；未定义技术词和宽泛集合改为产品可理解的可观察业务结果。

同组选项必须处于同一决策维度并且不能同时成立。选项被选择后必须直接形成可测试规则，不得保留固定时间、统一上限、中间档、短暂时间窗等占位参数，也不得使用各功能单独定义、另行定义或视情况处理等逃生选项。

兼容或历史数据方案必须明确旧数据范围、新规则生效对象或时点和旧规则退出条件。

原“影响用例”拆成“已有用例编号”和“确认后待补用例”：前者只写当前工作簿可核对编号或区间，后者只写确认后需要新增或重审的范围，两列至少一列非空。

旧 8、9、15、18 或 19 字段待确认记录转换时同步拆分用例影响字段。
```

### 回退方法

1. 将 `AGENTS.md` 第 4.2 节恢复为 RCL-0015 的 19 列字段，将“已有用例编号”和“确认后待补用例”合并回“影响用例”，并删除本次新增的业务语言、参数完整性、选项互斥、逃生选项、兼容边界和真实父子依赖规则。
2. 将 `AGENTS.md` 第 12.1、13、14 节恢复为 RCL-0015 的 19 字段兼容、列数、默认隐藏列和质量门禁文本。
3. 从 `Cem Kaner.txt` 删除本次新增的八类表达门禁，将待确认对象及 JSON 示例恢复为 RCL-0015 的 19 字段结构，并把两个用例影响字段合并回“影响用例”。
4. 重建受影响工作簿时把“需求待确认”恢复为 19 列，并同步恢复生成脚本中的列位置、数据验证、概览公式和隐藏列范围。
5. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0016` 的新记录。

## RCL-0017 增加原型优先项目的需求清单自动同步门禁

- 修改时间：2026-08-29 18:00:28 CST
- 用户要求或修改原因：产品经理修改原型后可能遗漏同步需求清单。用户确认采用项目级来源策略：对声明为 `prototype-primary` 的项目，以审核通过的 `prototype/` 为当前有效需求源，在每次生成用例前自动同步 `context/` 派生需求清单，并加入原型基线、删除限制、隐藏规则保护、精确日志、旧用例影响和同步阻断等保护措施。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`liveshow-proto/AGENTS.md`、`liveshow-proto/需求来源策略.json`、`原型与需求清单同步指令.md`、`需求清单变更日志.md`、`测试用例生成文件职责说明.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 1、2.1、3、3.1、14、15 节；`Cem Kaner.txt` 的输入材料、任务目标、生成前同步门禁、证据规则和输出前检查；`liveshow-proto/AGENTS.md` 第 9 节；新增项目配置、同步指令和需求清单日志；补充文件职责说明。

### 修改前原文

```text
AGENTS.md 使用固定证据优先级：用户当前确认、正式需求、原型、代码、已有用例、测试启发式。新原型可以证明页面结构和交互线索，但不能仅凭更新时间自动推翻旧需求中的业务规则。

标准工作流直接读取需求、原型和已有用例后建立证据索引，没有项目来源策略、原型基线、prototype 与 context 自动同步、需求同步日志和同步阻断步骤。

Cem Kaner.txt 没有“项目需求来源策略”和“原型与需求清单同步结果”输入，也没有生成前需求同步门禁。

liveshow-proto/AGENTS.md 说明审核通过的原型是需求依据，但没有声明机器可读的 prototype-primary 策略，也没有规定怎样同步 context、怎样限制删除和怎样追踪旧用例影响。

根目录没有原型与需求清单同步指令、需求清单变更日志；文件职责说明没有相关文件职责。
```

### 修改后原文

```text
全局允许 prototype-primary、requirement-primary、conflict-review 三种项目来源策略；项目未声明时默认 conflict-review，不把一个项目的原型优先规则扩散到其他项目。

prototype-primary 项目在生成用例前强制执行：固定相关原型文件路径、修改时间和 SHA-256；按目标模块和依赖增量扫描；建立原型到 context 章节映射；把差异分类为新增、修改、明确删除、原型未覆盖、来源冲突或无法定位；只自动修改原型明确表达的内容。

原型未展示不得作为删除依据。只有明确的删除、取消、下线、废弃或不支持证据才能删除；权限、金额、计算、统计、异步时效、异常恢复、跨端影响、历史数据和兼容规则在原型未覆盖时保留或进入需求待确认。

每次实际修改 context 都写入根目录唯一需求清单变更日志，保存修改前后原文、证据、影响和回退方法；旧用例标记为继续有效、需要重审、已被替代或应当废弃，不自动覆盖或删除历史 Excel。

存在原型基线变化、无法定位、缺少直接证据、日志失败、无法安全回退或阻塞性冲突时，同步状态为阻塞，不得生成 JSON 或 Excel。同步结果保存在本次任务 work 目录并参与交付前门禁。

Luma Live 新增需求来源策略配置，声明 prototype 为当前有效需求源，三份 Markdown 项目需求清单为可自动同步的派生需求；原始需求 Excel 和历史沟通记录保持只读。
```

### 回退方法

1. 从 `AGENTS.md` 第 1 节删除新增三个文件职责；将第 2.1 节恢复为 RCL-0016 后的固定证据优先级；将第 3 节恢复为直接读取需求和原型的 10 步流程，删除“原型与需求清单同步门禁”，并把“业务结果完整性扫描”恢复为第 3.1 节；删除第 14 节六条同步质量门禁和第 15 节两类日志说明。
2. 从 `Cem Kaner.txt` 删除项目来源策略和同步结果输入、生成前同步门禁及三条同步输出检查；将任务目标恢复为 8 条，并将证据规则恢复为 RCL-0016 后的通用来源冲突规则。
3. 从 `liveshow-proto/AGENTS.md` 删除第 9 节“本项目测试需求来源策略”，删除 `liveshow-proto/需求来源策略.json`，项目恢复为没有机器可读来源策略并按全局默认 `conflict-review` 处理。
4. 删除 `原型与需求清单同步指令.md`；将 `测试用例生成文件职责说明.md` 删除新增的来源策略、同步指令和需求日志职责，并恢复原章节编号和文件关系说明。
5. `需求清单变更日志.md` 已包含历史记录，不直接删除；追加引用 `RSL-0001` 的回退记录，说明项目不再自动同步。若未来已有实际需求变更，先按各记录的回退方法恢复需求内容。
6. 不删除或改写本条历史记录；执行回退时在 `规则文件变更日志.md` 追加引用 `RCL-0017` 的新记录。

## RCL-0018 增加最小前置条件和单分支用例门禁

- 修改时间：2026-08-29 19:03:30 CST
- 用户要求或修改原因：用户发现不同房管限制用例复制了相同的“场景 A / 场景 B”前置条件，使直播间黑名单和账号拉黑两个独立业务关系混入同一条用例。用户确认采用通用方案，要求每条用例只保留当前验证所需的最小条件、拆分可替代分支、删除孤立条件并增加生成后检查。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 新增第 8.2 节并补充第 14 节；`Cem Kaner.txt` 新增“前置条件最小化与单分支”并补充“输出前检查”。

### 修改前原文

```text
AGENTS.md 第 8.1 节只规定测试数据详细程度与验证目标匹配，要求简单用例使用最小有效数据，但没有规定一条用例只能选择一个业务分支，也没有定义孤立条件、结果归因和跨用例复制检查。

AGENTS.md 第 14 节只检查前置条件、步骤和预期结果非空且可执行，没有检查“场景 A / B”、可替代页面或关系、与步骤和预期无关的条件，以及不同限制原因混在同一用例的问题。

Cem Kaner.txt 的“测试数据准备粒度”和“输出前检查”没有最小充分条件、单分支、孤立条件、原因隔离和复杂前置条件复用复核规则。
```

### 修改后原文

```text
新增“前置条件最小化与单分支”规则：
1. 每条用例的前置条件和操作步骤只描述一条确定路径；可替代角色、页面、入口、业务关系、状态、输入或操作必须拆分，不得用“场景 A / B”或模糊“或”合并。
2. 前置条件使用最小充分集；每个条件必须保证操作可执行、触发目标规则、排除其他独立原因或使结果可复现，删除后不影响用例的条件必须移除。
3. 条件必须与验证用例子项、步骤和预期使用同一业务对象、关系和状态；只在前置条件出现的无用途内容认定为孤立条件。
4. 验证单一限制时排除会产生相同结果的其他独立原因，例如分别隔离直播间黑名单和账号拉黑，保证结果可归因。
5. 必须同时存在的对照数据明确写成共同条件，不得写成任选的 A/B 分支。
6. 同一操作拆分观察结果时允许复用必要条件，但完全相同的复杂前置条件用于不同验证点时必须逐条复核，删除复制带入的无关内容。

交付前质量门禁同步检查单一执行路径、孤立条件、原因隔离、共同对照数据和复杂前置条件复用情况。
```

### 回退方法

1. 从 `AGENTS.md` 删除第 8.2 节“前置条件最小化与单分支规则”，并从第 14 节删除本次新增的五条相关门禁。
2. 从 `Cem Kaner.txt` 删除“前置条件最小化与单分支”章节，并从“输出前检查”删除本次新增的四条检查。
3. 回退规则后，重新生成的用例将仅继续遵守 RCL-0017 及之前的测试数据粒度规则；不自动恢复已经按 RCL-0018 修正的历史 Excel。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0018` 的新记录。

## RCL-0019 原型默认数据与批注不一致时批注优先

- 修改时间：2026-08-31 10:34:31 CST
- 用户要求或修改原因：用户确认当原型页面的默认展示数据与同一页面批注描述不一致时，批注的业务规则优先级更高。需要避免把展示日期、占位值或 Mock 示例记录误当成与批注对等的业务规则，从而产生无必要的需求待确认或阻断整个生成流程。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`原型与需求清单同步指令.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 2.1、3.1、14 节；`Cem Kaner.txt` 的生成前同步门禁、证据与推断规则、输出前检查；`原型与需求清单同步指令.md` 第 3.4、3.6 节。

### 修改前原文

```text
AGENTS.md 和 Cem Kaner.txt 只规定多个来源冲突时，必须依据适用版本、生效时间、废止范围或替代关系选择当前规则；无法确认时进入“需求待确认”。没有规定原型内部批注与页面默认展示值、占位值或 Mock 示例数据之间的优先级。

原型与需求清单同步指令.md：
原型内部的说明、批注、页面交互和 Mock 相互冲突时，不自行挑选；能够依据明确版本或替代关系解决时记录依据，否则阻断同步。
```

### 修改后原文

```text
原型内部出现特定差异时，页面初始化的默认展示值、占位值或 Mock 示例数据，与同一页面批注明确写明的默认规则、筛选规则、状态含义或计算口径不一致，以批注为准。

该差异不归入“来源冲突”，不单独写入“需求待确认”，也不阻断同步。需求清单按批注同步，测试用例按批注生成；证据索引、同步结果和用例备注必须记录批注位置及被覆盖的页面值或 Mock 示例差异。

批注未明确给出规则、无法判断页面值是否属于默认或示例数据，或者差异涉及真实可执行交互、字段结构、枚举、数据类型、状态切换或操作结果时，仍按来源冲突处理，不把“批注优先”扩大为无条件覆盖。
```

### 回退方法

1. 从 `AGENTS.md` 第 2.1 节删除原型内部批注优先例外段落，将第 3.1 节“分类差异”和第 14 节来源冲突门禁恢复为 RCL-0018 后的文本。
2. 从 `Cem Kaner.txt` 删除生成前同步门禁和证据规则中的批注优先条目，并将输出前来源冲突检查恢复为 RCL-0018 后的文本。
3. 从 `原型与需求清单同步指令.md` 第 3.4 节删除批注优先说明，将第 3.6 节恢复为“原型内部说明、批注、页面交互和 Mock 冲突时不自行选择，无法依据版本或替代关系解决则阻断同步”。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0019` 的新记录。

## RCL-0020 增加生成前全局证据扫描门禁

- 修改时间：2026-08-31 16:53:45 CST
- 用户要求或修改原因：用户指出仅参考目标端局部原型会使测试用例缺少跨端和跨模块关联，要求把“生成用例前必须参考项目全局文件数据”写成通用强制规则。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 新增第 2.3 节并修改第 3、14 节；`Cem Kaner.txt` 的输入材料、任务目标、生成前门禁和输出前检查。

### 修改前原文

```text
生成流程读取同步后的需求清单、原型、页面 JSON、接口、代码、已有用例和用户补充规则。

原型同步要求平时扫描目标模块、公共批注、公共 Mock、共享组件和已知跨模块依赖，发现关联后扩大范围，但没有独立的项目全局文件建档、其他端关联证据读取、排除原因记录和用例生成阻断规则。

跨端流程规则只要求在已有证据表明跨端关联时建立流程，没有禁止在未读取被提及端文件的情况下把后台配置、审核、消息、记录或状态链路写成确定规则。
```

### 修改后原文

```text
生成任一模块测试用例前，必须先扫描项目根目录并建立全局文件清单，再完整读取目标模块、公共规则以及所有已发现的跨端或跨模块关联来源。

全局扫描至少覆盖全部 context、原型入口和说明、公共批注、公共 Mock、共享配置与组件、各端页面，以及项目内存在的接口、代码、数据库约束、自动化测试和已有用例。允许不解析二进制、生成物、历史归档或明显无关文件，但必须记录未纳入原因。

需求、批注、Mock、共享配置、接口或已有用例指向管理后台、公会端、第三方或其他模块时，必须自动扩大证据范围，直至共同业务对象、上游配置或触发、当前模块行为和下游结果可以关联。其他端只作为当前范围的关联证据，不自动扩大用例生成范围。

每次任务在 work 目录保存 global-evidence-scan-result.json，记录全局文件清单、已读取文件、纳入证据及用途、未纳入文件及原因、跨端或跨模块依赖、关联映射、证据缺口和阻塞项。

缺少全局文件清单、目标或公共证据未读取、已发现依赖未展开、排除原因未记录或关联映射无法闭合时，不得生成最终 JSON 或 Excel。来源完整扫描后仍缺少业务决定的内容进入需求待确认，不能用待确认代替未完成的证据扫描。
```

### 回退方法

1. 从 `AGENTS.md` 删除第 2.3 节，将第 3 节第 4 步和第 14 节恢复为 RCL-0019 后的文本。
2. 从 `Cem Kaner.txt` 删除“项目全局文件清单与证据扫描结果”输入、“生成前全局证据扫描门禁”章节以及输出前对应三条检查，并将任务目标恢复为 RCL-0019 后的 9 项。
3. 停止要求生成 `global-evidence-scan-result.json`；后续任务恢复为仅按原型同步范围和已知跨模块依赖读取证据。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0020` 的新记录。

## RCL-0021 将全局证据扫描改为全量与增量组合方案

- 修改时间：2026-08-31 17:17:45 CST
- 用户要求或修改原因：用户比较“每次全量扫描”和“首次全量、后续增量”后选择组合方案，要求兼顾生成速度、跨端关联和项目文件替换后的数据一致性，并要求修改后按新规则重新生成用户 App 直播模块测试用例。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`全局证据扫描指令.md`、`测试用例生成文件职责说明.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 1、2.3、3、14 节；`Cem Kaner.txt` 的任务目标、全局证据扫描门禁和输出前检查；新增全局扫描执行指令；补充文件职责说明。

### 修改前原文

```text
每次生成任务都建立项目全局文件清单并完成关联证据扫描。规则没有区分首次全量与后续增量，没有项目级缓存、确定的全量失效阈值和输出前第二次证据哈希复核。

标准工作流先执行 prototype-primary 的原型同步，再完成全局文件建档和关联证据扫描，可能在同步完成后才发现其他端依赖。

global-evidence-scan-result.json 记录文件清单、读取与排除、依赖和关联映射，但没有扫描模式、模式依据、项目指纹、规则基线、文件变化、复用证据和缓存失效信息。
```

### 修改后原文

```text
全局证据扫描采用组合方案：每次重建包含相对路径、大小、修改时间和 SHA-256 的轻量文件清单；没有有效缓存时 full，全量缓存有效且变化可定位时 incremental，增量发现未知依赖或映射断裂时 fallback-full。

来源策略、AGENTS.md、Cem Kaner.txt、全局扫描指令、原型同步指令、扫描 Schema 或项目标识变化，候选证据变化达到 30 个或前后文件数较大值的 20%，以及用户说明整体替换项目时，必须废弃旧缓存并全量扫描。

项目级缓存保存到 work/<项目目录名>-global-evidence-cache/latest.json；任务快照继续保存为本次 work 目录的 global-evidence-scan-result.json，并增加扫描模式、项目指纹、变化文件、读取与复用证据、依赖关系和输出前复核。

标准工作流调整为先比较项目指纹、选择扫描模式和确定关联范围，再执行 prototype-primary 的原型同步；生成 JSON 和 Excel 前重新计算全部采用证据的 SHA-256，发现变化则整次结果失效并重新选择扫描模式。

全局扫描只扩大证据范围，不扩大用户指定的用例范围。管理后台、公会端和第三方文件只作为当前模块的配置来源、状态影响或下游结果证据，除非用户明确要求，不生成这些端自身的模块用例。
```

### 回退方法

1. 删除 `全局证据扫描指令.md`，并从 `测试用例生成文件职责说明.md` 删除其职责、缓存和任务快照说明。
2. 将 `AGENTS.md` 第 2.3、3、14 节恢复为 RCL-0020 的每次全局建档扫描流程，删除项目缓存、三种扫描模式、30 个或 20% 阈值及双重基线校验规则。
3. 将 `Cem Kaner.txt` 恢复为 RCL-0020 的 10 项任务目标、全局扫描门禁和输出前检查。
4. 停止复用并保留已经生成的项目级缓存和历史任务快照；后续任务不再读取缓存，但不删除历史证据记录。
5. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0021` 的新记录。

## RCL-0022 增加目标端可执行性与规则归属门禁

- 修改时间：2026-09-01 11:27:15 CST
- 用户要求或修改原因：用户确认全局规则扫描具有覆盖价值，但指出系统概要或管理后台存在规则时，目标用户 App 可能没有对应查看入口。需要保留规则价值，同时防止把其他端规则直接写成目标端不存在的可执行步骤。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 新增第 2.2.1 节并调整标准工作流；`Cem Kaner.txt` 调整任务目标、新增“目标端可执行性与规则归属”章节并补充输出前检查。

### 修改前原文

```text
全局证据扫描用于建立业务关联和证据边界，但只要求正式用例具有可执行操作和可观察预期，没有强制逐步证明页面、详情、说明、明细或跳转确实存在于本次目标端。

其他端只作为当前范围证据，不自动生成其他端用例，但没有进一步区分“系统规则存在”“目标端规则可见”和“目标端业务结果可观察”，可能把系统概要或后台配置直接改写成用户 App 的查看步骤。
```

### 修改后原文

```text
全局证据扫描发现业务规则后，继续建立“规则来源 -> 适用角色和端 -> 实际入口或触发动作 -> 可观察结果”映射。系统存在规则不能单独证明目标端存在查看或操作入口。

目标端没有规则查看入口但可以执行业务并观察余额、状态、消息或记录时，只生成真实可执行、可观察的结果用例，不补写不存在的查看规则、打开详情或查看明细步骤。

规则只能通过其他端、接口、数据库、日志或测试工具验证时，归入对应测试范围；用户未要求扩大范围时只记录覆盖去向。业务规则有价值但当前目标端缺少入口、路径或观察方式时，保留为需求待确认或覆盖清单，不直接删除。

prototype-primary 项目中，派生需求写明目标端展示规则而审核通过的原型没有入口、批注或交互时，标记为原型未覆盖并进入需求待确认；获得目标端入口证据前不生成展示类正式用例。
```

### 回退方法

1. 从 `AGENTS.md` 删除第 2.2.1 节，将标准工作流恢复为 RCL-0021 后的 13 步，并从最终校验项删除目标端入口和规则归属检查。
2. 从 `Cem Kaner.txt` 删除“目标端可执行性与规则归属”章节、对应任务目标和两条输出前检查，将任务目标编号恢复为 RCL-0021 后的 13 项。
3. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0022` 的新记录。

## RCL-0023 增加业务对象名称优先与数据别名解释规则

- 修改时间：2026-09-01 12:12:16 CST
- 用户要求或修改原因：用户指出礼物名称可以直接识别，而将礼物改成 `GFT-LIVE-03` 等编码后还需要查阅文件，降低测试执行效率；同时认可用例编号等结构编码和必要的数据追踪别名继续存在。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 新增第 8.1.1 节并补充最终校验；`Cem Kaner.txt` 补充测试数据准备粒度和输出前检查。

### 修改前原文

```text
规则要求测试数据可准备、流程使用共同业务对象，并保留稳定编号，但没有区分结构化追踪标识与业务对象数据别名，也没有要求业务对象编码首次出现时随可识别名称解释。

生成结果可能在前置条件、操作步骤和预期结果中直接写 GFT-LIVE-03、OP-LIVE-02、LIVE-SESSION-001 等裸编码，测试执行人员需要另查文件才能确认对象含义。
```

### 修改后原文

```text
用例编号、流程编号、问题编号、需求组编号和规则/同步日志编号继续作为结构化追踪标识保留。

礼物、账号、主播、直播场次、订单和交易记录等业务对象以可识别名称为主；数据别名只作为跨步骤、跨端或日志追踪的辅助信息，不得以裸编码代替名称。

同一条用例中，业务对象首次出现在前置条件、操作步骤或预期结果时写成“可识别名称（数据别名：CODE）”，后续只写名称。输出前扫描正式执行字段，禁止出现需要另查文件才能识别的业务对象裸编码。

可识别名称必须来自原型、需求、接口、Mock、现有可准备数据或本次明确的数据准备约定；实际系统 ID 与测试数据别名必须明确区分。
```

### 回退方法

1. 从 `AGENTS.md` 删除第 8.1.1 节和最终校验中的业务对象名称检查。
2. 从 `Cem Kaner.txt` 删除测试数据准备粒度中的名称优先规则和对应输出前检查。
3. 后续生成可以恢复使用裸数据别名；已生成工作簿保留为历史产物，不自动覆盖或删除。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0023` 的新记录。

## RCL-0024 增加无来源业务对象命名与显示质量门禁

- 修改时间：2026-09-01 14:07:21 CST
- 用户要求或修改原因：用户发现工作簿出现“免检账号B”等无法从输入材料直接确认的名称，并要求修复同类显示问题及增加通用规则约束。现有 RCL-0023 虽要求名称可识别，但“本次明确的数据准备约定”的表述仍可能被生成者误用为自行命名依据，且未覆盖备注裸编码、字段值误作对象名称和中文标点后多余空格。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 8.1.1 节和输出前检查；`Cem Kaner.txt` 测试数据准备粒度和输出前检查。

### 修改前原文

```text
可识别名称可以来自“本次明确的数据准备约定”，但没有规定该约定必须由用户、测试负责人或实际准备数据明确确认。

备注允许保留数据别名，但没有明确要求备注中的业务对象编码也必须随名称或对象描述解释。

规则没有明确禁止把对象状态、权限、预期行为或字段值改写成业务对象名称，也没有要求检查右引号、右全角括号后的多余空格。
```

### 修改后原文

```text
只有输入材料、现有可准备数据，或用户、测试负责人明确确认的数据准备约定可以作为业务对象名称依据；生成者不得自行拟名后登记为数据准备约定。

没有来源名称时，使用有证据的“对象类型/业务关系/关键状态（数据别名：CODE）”，后续使用“该对象”“该主播”“该运营账号”“该直播场次”等指代；不得把待验证结论写入对象描述。

禁止把对象状态、权限、预期行为或字段值包装成业务对象名称；标题等字段值必须与承载该字段的业务对象分开表达。

正式执行字段和备注中的业务对象数据别名都必须随来源名称或对象描述解释；输出前扫描中文右引号、右全角括号与后续中文之间的多余空格，并人工抽样复核名称、字段值和数据别名的显示关系。
```

### 回退方法

1. 从 `AGENTS.md` 第 8.1.1 节删除 RCL-0024 新增的名称来源、对象描述、字段值分离、备注解释和显示空格要求，并删除对应输出前检查项。
2. 从 `Cem Kaner.txt` 删除对应生成约束和输出前检查。
3. 已生成工作簿保留为历史产物，不自动覆盖或删除；需要恢复旧表达时重新生成新版本。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0024` 的新记录。

## RCL-0025 划分 AGENTS 与 Cem Kaner 文件职责并移除重复规则正文

- 修改时间：2026-09-01 14:40:41 CST
- 用户要求或修改原因：用户确认 `AGENTS.md` 应负责制定唯一规则和验收标准，`Cem Kaner.txt` 应只负责接收输入并执行 JSON 生成。检查发现两份文件在证据、原型同步、全局扫描、目标端入口、测试设计、待确认、数据准备、前置条件、跨端流程、优先级、去重和输出前检查等方面存在大量语义重复，容易造成双重维护和规则漂移；用户同时要求检查其他文件是否需要联动修改。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`测试用例生成文件职责说明.md`、`原型与需求清单同步指令.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 1 节；`Cem Kaner.txt` 全文职责重构；`测试用例生成文件职责说明.md` 第 1、2、8 节；`原型与需求清单同步指令.md` 第 1 节。

### 修改前原文

```text
AGENTS.md 定义强制质量门禁、证据边界、统一字段和交付标准；Cem Kaner.txt 定义具体的 JSON 生成提示词。

Cem Kaner.txt 同时完整维护了来源策略、原型同步、全局扫描、证据边界、目标端入口、测试设计、业务结果扫描、待确认、数据准备、前置条件、跨端流程、用例语言、类型、优先级、语义去重和输出前检查等规则正文。

测试用例生成文件职责说明.md 将测试设计方法和生成流程调整归为 Cem Kaner.txt 的修改职责，原型与需求清单同步指令.md 只写“调用 Cem Kaner.txt 生成用例”。
```

### 修改后原文

```text
AGENTS.md 是唯一规则来源，集中维护证据、同步、扫描、测试设计、字段语义、去重、Excel 交付、质量门禁和回退机制。

Cem Kaner.txt 只保留任务输入占位、最小执行入口、AGENTS.md 章节引用、JSON 字段与类型契约及合法示例；不再复制治理规则正文，也不负责 Excel 转换和交付规则。

治理规则变化只修改 AGENTS.md；只有任务输入或 JSON 输出接口变化时才同步修改 Cem Kaner.txt。Cem Kaner.txt 与 AGENTS.md 冲突时以 AGENTS.md 为准。

测试用例生成文件职责说明.md 已按该边界更新；原型同步通过后，使用 Cem Kaner.txt 的输入与 JSON 模板，并按 AGENTS.md 当前规则生成用例。
```

### 其他文件检查结论

1. `全局证据扫描指令.md` 继续把 `AGENTS.md` 和 `Cem Kaner.txt` 的哈希变化作为全量扫描触发条件；该职责用于保证规则和接口基线变化后缓存失效，不属于重复规则，无需修改。
2. `需求清单变更日志.md` 只区分规则变更日志和需求变更日志，现有表述仍然准确，无需修改。
3. `已确认需求转测试用例指令.md` 只描述已确认问题转换任务，不依赖两份文件的旧职责，无需修改。
4. `Cem Kaner_yuan.txt` 是历史原始提示词，不参与当前规则链路，本次不修改。

### 回退方法

1. 将 `AGENTS.md` 第 1 节恢复为 RCL-0024 后的文件职责表述。
2. 将 `Cem Kaner.txt` 恢复为 RCL-0024 后包含完整生成规则正文的版本。
3. 将 `测试用例生成文件职责说明.md` 第 1、2、8 节和 `原型与需求清单同步指令.md` 第 1 节恢复为本次修改前内容。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0025` 的新记录。

## RCL-0026 增加规则缺失判定复核门禁

- 修改时间：2026-09-01 15:25:40 CST
- 用户要求或修改原因：用户指出 `context/系统概要 .md` 已明确写出主播连麦邀请规则，但分析仍把“能否同时收到多个邀请”误判为缺失。根因是只确认章节或部分来源，没有在声明缺失前读取命中表格上下文，也存在把 `需求来源策略.json` 的派生需求清单误作证据读取范围的风险。用户确认该问题重要并要求修改为通用约束。
- 涉及文件：`AGENTS.md`、`全局证据扫描指令.md`、`原型与需求清单同步指令.md`、`测试用例生成文件职责说明.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 1、2.3、2.3.1、3、14 节；`全局证据扫描指令.md` 第 3、7、10、11 节；`原型与需求清单同步指令.md` 第 2、3.1 节；`测试用例生成文件职责说明.md` 第 3、5.1、8 节。

### 修改前原文

```text
派生需求清单用于限定自动同步文件，但规则没有明确禁止把该清单当作证据读取白名单。

证据缺口判定只要求继续检查原型交互、状态、便签、批注和提示文案，没有要求拆分业务对象、动作、状态和结果，没有要求使用同义词检索，也没有要求读取搜索命中的章节或表格上下文。

global-evidence-scan-result.json 只记录证据缺口，没有为每条拟判定缺失的规则保存检索词、已查文件、命中位置、采用结论和未采用原因。
```

### 修改后原文

```text
派生需求清单是 prototype-primary 同步的写入白名单，不是证据读取白名单。公共证据清单用于显式登记系统概要、业务对象、角色权限和页面架构；即使未登记，全部现存 context 文件、项目说明引用和扫描发现的关联来源仍须建档和读取。

声明“规则未定义”“需求不存在”“证据缺失”或生成需求待确认前，必须拆分业务对象、角色、动作、状态、限制和结果，补充同义词、旧称、页面名和字段名，并检索全部关联来源。命中后必须阅读所属章节、表格整行、相邻条件和直接引用。

每项拟判定缺失的规则必须在 global-evidence-scan-result.json 的“规则缺失复核”中记录检索对象、检索词、已查文件、命中位置、命中规则、采用结论、未采用原因和复核状态。已有合格来源明确结果时必须撤销缺失结论并生成正式用例。

项目说明引用缺失、应读文件无法读取、关联链路未展开或复核记录不完整属于扫描阻塞，不能用需求待确认代替。
```

### 联动说明

1. `全局证据扫描指令.md` 的扫描结果 Schema 从 `1.0` 更新为 `1.1`，新增 `规则缺失复核` 字段；旧缓存必须失效并重新全量扫描。
2. `原型与需求清单同步指令.md` 明确未列入派生需求清单的公共证据仍必须读取，但不得自动覆盖。
3. `Cem Kaner.txt` 已通过现有 `GLOBAL_EVIDENCE_SCAN_RESULT_JSON` 输入和 `AGENTS.md` 第 2.3 节引用承接本规则，JSON 用例输出接口没有变化，因此本次不修改。
4. Luma Live 项目公共证据登记属于项目来源策略变化，另记入 `需求清单变更日志.md` 的 `RSL-0006`。

### 回退方法

1. 从 `AGENTS.md` 删除第 2.3.1 节以及标准工作流和交付门禁中的对应复核要求，并恢复来源策略文件职责描述。
2. 从 `全局证据扫描指令.md` 删除规则缺失判定复核章节和 `规则缺失复核` 字段，将 Schema 恢复为 `1.0`。
3. 恢复 `原型与需求清单同步指令.md` 和 `测试用例生成文件职责说明.md` 本次增加的读取范围说明。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0026` 的新记录。

## RCL-0027 增加新项目首次接入初始化门禁

- 修改时间：2026-09-02 12:19:09 CST
- 用户要求或修改原因：用户计划在 `testcase` 根目录持续新增不同项目，不再通过替换现有项目目录工作。不同项目可能没有 Luma Live 使用的 `项目说明.md`、`需求来源策略.json`、`context/` 或原型目录；原规则只能在文件缺失后默认冲突审查或阻断扫描，没有独立的项目身份确认、草稿、登记和首次扫描流程，存在误用其他项目结构、证据或缓存的风险。用户确认采用“先接入初始化、确认后生成”的通用方案。
- 涉及文件：`AGENTS.md`、`新项目首次接入指令.md`、`全局证据扫描指令.md`、`原型与需求清单同步指令.md`、`测试用例生成文件职责说明.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 1、2.1、2.3、2.4、3、14 节；新增 `新项目首次接入指令.md`；`全局证据扫描指令.md` 第 3、4、10、11 节；`原型与需求清单同步指令.md` 第 2、9 节；`测试用例生成文件职责说明.md` 第 3、4、9 节。

### 修改前原文

```text
项目没有需求来源策略时默认使用 conflict-review；没有有效全局缓存时直接使用 full 扫描。

规则没有先区分“刚新增的新项目”“已有项目缓存丢失”和“同名目录实际换成另一个项目”，也没有要求确认项目稳定身份、Git 基线和实际资料入口。

缺少项目说明或来源策略时，没有统一的只读盘点、草稿位置、用户确认、正式登记和禁止生成规则；不同项目之间主要依赖目录名隔离缓存。
```

### 修改后原文

```text
新项目先执行首次接入：只读盘点稳定项目标识、Git 盘点基线和实际存在的需求、原型、代码及测试资料入口，在任务 work 目录保存 project-onboarding-result.json，并按需生成项目说明和来源策略草稿。

用户确认前不得修改项目目录，也不得生成正式测试用例 JSON 或 Excel；确认后才落地最小正式文件、保存独立接入记录，并以 full 执行第一次全局证据扫描。

稳定项目标识与分支、提交基线分离，普通代码更新不会把同一项目误判为新项目。项目接入记录、来源策略、规则或扫描 Schema 变化会使业务证据缓存失效。

规则生效前的旧项目可以用既有项目说明、来源策略，以及扫描状态为通过或有非阻塞待确认、目录一致、两个核心文件哈希一致的历史缓存证明项目身份；核心文件已合理修改时还需要用户明确确认这是原项目。该缓存只能作为兼容接入依据，不能复用其中的业务证据，通过首次全量扫描后补建正式接入记录。

每个项目独立维护接入记录、来源策略、全局缓存、任务目录和输出，禁止跨项目补齐资料或复用证据结论。
```

### 联动检查结论

1. `全局证据扫描指令.md` 的扫描结果 Schema 从 `1.1` 更新为 `1.2`，新增“项目接入基线”；旧业务证据缓存必须失效并重新全量扫描。
2. `原型与需求清单同步指令.md` 已在来源策略判断前增加接入门禁，避免新项目缺少配置时直接进入默认策略或同步流程。
3. `Cem Kaner.txt` 已通过 `AGENTS.md` 第 3 节完整工作流承接本规则，任务输入和 JSON 输出接口没有变化，因此按文件职责不修改。
4. `已确认需求转测试用例指令.md` 是特定历史工作簿的转换指令，不是新项目生成入口；其执行仍受当前 `AGENTS.md` 约束，无需修改。
5. `需求清单变更日志.md` 只记录项目来源策略和派生需求内容变化；本次没有修改任何项目的 `context/`、原型或正式来源策略，因此不追加需求变更记录。

### 回退方法

1. 从 `AGENTS.md` 删除第 2.4 节，并恢复第 2.1、2.3、3、14 节本次修改前的项目识别和扫描流程。
2. 删除 `新项目首次接入指令.md`，恢复 `全局证据扫描指令.md`、`原型与需求清单同步指令.md` 和 `测试用例生成文件职责说明.md` 本次修改前内容。
3. 将全局扫描结果 Schema 恢复为 `1.1`；已生成的接入记录和任务草稿保留为历史产物，不自动删除。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0027` 的新记录。

## RCL-0028 增加规则归一化与状态包含语义去重门禁

- 修改时间：2026-09-02 15:56:03 CST
- 用户要求或修改原因：用户发现 `LIVE-226` 将“普通房连麦中”作为独立条件验证房间密码入口置灰，但普通房未连麦时该入口已经置灰。进一步追溯确认，原流程按不同页面批注分别生成用例，单结果拆分后没有再次去重，后续版本又按易变化的用例编号继承；现有字符串去重因功能结构、验证点和状态措辞不同而错误报告“业务语义重复为 0”。用户确认先修改通用规则，不生成新用例。
- 涉及文件：`AGENTS.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 3、6、11、14 节。

### 修改前原文

```text
标准工作流只要求建立证据索引、覆盖清单，并在用例生成后按业务验证点执行语义去重和来源合并，没有要求先归一化业务规则、建立状态包含关系或保存可审计去重结果。

单结果规则允许拆分后的用例复用前置条件和操作步骤，但没有要求拆分后删除与唯一预期无关的步骤，也没有要求再次执行条件最小化和语义去重。

多来源合并与去重主要比较“功能模块 + 功能结构 + 验证用例子项 + 角色/前置状态 + 关键输入/操作 + 状态去向/预期结果”，没有定义条件删除测试、规则覆盖测试、状态子集、实现变体例外或无法判断时的阻断方式。

增量生成只要求读取已有用例摘要并重新去重，没有要求把已有用例绑定到稳定规则标识，也没有禁止把会随排序变化的 LIVE-xxx 编号黑名单作为唯一删除机制。

交付门禁只要求没有语义重复，没有要求提供独立去重复核产物。
```

### 修改后原文

```text
证据索引同时建立业务规则清单；同一规则的多来源命中使用稳定规则标识合并，记录业务对象、角色和端、动作、必要条件、状态关系、可观察结果及来源。页面数量、批注数量和来源数量只增加证据，不自动增加用例。

对房型、角色、权限、流程阶段、开关和业务状态建立有证据的包含、互斥、前置或无影响关系。候选用例依次执行条件删除测试、规则覆盖测试和差异保留测试：附加条件删除后操作和结果不变时不得形成独立用例；条件更严格但结果相同的状态子集默认合并到基础用例。

业务结果相同但存在有证据的独立入口、组件或处理分支，或者用户明确要求分别回归时，可以标记为实现变体或用户指定覆盖；证据不足时记录待人工复核，并在复核完成前阻断最终 JSON 和 Excel。

单结果拆分后必须删除与唯一预期无关的步骤，再次执行步骤与预期匹配、条件最小化、状态包含、规则覆盖和跨功能结构语义去重，禁止拆分动作放大重复用例。

增量生成和重建时，已有用例重新绑定当前稳定规则标识，再判断继续有效、合并、替代、废弃或待人工复核；不得仅按不同交付编号保留，也不得把 LIVE-xxx 等易变化编号黑名单作为唯一删除机制。

每次生成、增量修订或重审在任务 work 目录保存 semantic-dedup-review.json，记录稳定规则标识、候选追溯、归一化业务键、基础和附加条件、状态关系、操作、结果、判定、合并目标、理由和证据。只完成标题、验证点或完整字符串查重，不能声明业务语义去重已完成。
```

### 联动检查结论

1. `Cem Kaner.txt` 已要求完整读取 `AGENTS.md`，并明确引用第 11 节语义去重和第 14 节质量门禁；本次没有改变任务输入或正式 JSON 输出接口，因此按文件职责不修改。
2. `全局证据扫描指令.md` 已在规则缺失复核中使用稳定 `规则标识`，并保存证据索引和关联映射；本次新增的是生成阶段的规则归一化和去重复核产物，不需要改变扫描结果 Schema。
3. `原型与需求清单同步指令.md` 只负责同步需求事实，不负责生成或去重用例，无需修改。
4. `测试用例生成文件职责说明.md` 已把测试设计和质量门禁归属 `AGENTS.md`，无需重复规则正文。
5. 规则修改阶段没有修改项目 `context/`、原型、来源策略或已有历史 Excel，也不追加 `需求清单变更日志.md`；用户随后要求重新生成，用例另存为 `用户App-直播模块-260902-002.xlsx`，未覆盖 `260902-001`。

### 回退方法

1. 将 `AGENTS.md` 第 3 节恢复为 RCL-0027 后的工作流，删除业务规则清单、状态关系预处理和 `semantic-dedup-review.json` 要求。
2. 从第 6 节删除单结果拆分后的步骤清理和再次去重要求。
3. 将第 11 节恢复为修改前的简单字段比较和四条合并规则，删除第 11.1 至 11.4 节。
4. 从第 14 节删除规则清单、条件子集、拆分后匹配、稳定规则绑定和去重复核产物检查。
5. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0028` 的新记录。

## RCL-0029 隔离当前证据与历史用例输入

- 修改时间：2026-09-02 17:11:16 CST
- 用户要求或修改原因：用户确认采用“当前证据重新建立规则，历史用例只做差异比较和编号追溯”的方案。现场检查确认 `work/liveshow-user-live-testcases-260902-002/build-user-app-live-module.mjs` 第 22、257 至 259、412 至 416 行直接读取上一版 JSON，并从历史 `测试用例` 和 `需求待确认` 数组构造新结果；此前已经实际传播过嵌套数据别名、重复备注和重复待确认记录。仅靠生成后的清洗无法阻止未被检查规则覆盖的历史错误继续累积。
- 涉及文件：`AGENTS.md`、`Cem Kaner.txt`、`全局证据扫描指令.md`、`测试用例生成文件职责说明.md`、`已确认需求转测试用例指令.md`、`scripts/validate-generation-input.mjs`、`scripts/validate-generation-input.test.mjs`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 1、2.1、2.3、2.3.1、2.5、3、4.1、11、12、14 节；`Cem Kaner.txt` 输入材料、执行入口、规则引用和输出说明；全局扫描候选证据、流程、Schema 和阻断条件；文件职责说明第 2、6.2、6.3、7.1、9 节；已确认需求转换流程；新增共用校验器及测试。

### 修改前原文

```text
已有测试用例位于业务规则证据优先级第 5 位；Cem Kaner.txt 直接接收 <<PREVIOUS_CASES_JSON>>，执行入口要求“读取已有用例后生成正式用例和需求待确认”。

标准工作流在语义读取阶段同时读取当前需求、原型、代码、已有用例和用户补充规则，没有强制区分各输入的角色，也没有禁止从历史数组复制、映射或筛选出当前候选。

全局扫描把已有测试用例列入候选证据，并允许从已有用例直接识别其他端或模块依赖。

旧格式 JSON 允许继续作为输入，只说明不得作为新格式标准，没有规定转换后的历史记录不能进入当前正式用例数组。

没有 generation-input-manifest.json、business-rule-catalog.json、current-testcase-candidate.json 或 historical-case-comparison.json 的强制契约，也没有共用程序校验历史文件权限、生成脚本引用和候选内容是否被历史比较改写。
```

### 修改后原文

```text
已有测试用例、历史 Excel、历史任务 JSON、历史扫描缓存和测试启发式不再作为业务规则来源，只能提供检索词、覆盖候选、变化比较和编号追溯；任何业务结论必须回到当前需求、原型、接口、代码或用户确认复核。

每次任务建立 generation-input-manifest.json，把输入唯一分类为当前业务证据、本次派生产物、历史参照、样式参照或执行工具，并记录路径、SHA-256、内容类型和是否允许定义业务规则。work 文件默认不能作为当前业务证据，本次用户确认及受控的已确认决策摘录除外。

候选生成前先从当前证据建立 business-rule-catalog.json；Cem Kaner.txt 不再接收历史用例或历史比较，只输出 current-testcase-candidate.json。候选形成后才单独读取历史参照生成 historical-case-comparison.json；发现遗漏必须回到当前证据和规则清单重新生成，不能直接修补候选。

最终 JSON 与候选 JSON 必须分别保存且 SHA-256 完全一致。质量上下文必须保存正式用例到稳定规则标识的映射。

新增 scripts/validate-generation-input.mjs，在 pre-generate 和 final 两个阶段校验输入角色、文件哈希、规则证据、历史比较、生成脚本直接引用以及候选/最终 JSON 同哈希；任何失败阻断正式 JSON 和 Excel。

包含已确认产品结论和历史用例的混合工作簿不得整体进入当前规则。只提取状态已确认、当前仍适用且位置可追溯的结论到 confirmed-decision-extract.json；原工作簿和其中用例仍作为历史参照。
```

### 联动检查与验证

1. `全局证据扫描指令.md` Schema 从 `1.2` 更新为 `1.3`，增加输入角色候选并明确历史用例只能提出待复核线索；规则基线和扫描 Schema 已变化，下一次正式生成必须执行 `full`，现有缓存不能复用业务结论。
2. `Cem Kaner.txt` 删除 `<<PREVIOUS_CASES_JSON>>` 输入，新增输入清单和当前规则清单，只负责生成当前候选；正式 JSON 的字段接口保持不变。
3. `原型与需求清单同步指令.md` 已明确历史 Excel 和已有用例只读，并只负责评估影响，不负责生成候选；无需修改。
4. `新项目首次接入指令.md` 已禁止跨项目复用缓存、业务规则和已有用例；无需修改。
5. `已确认补充业务规则模板.md` 记录用户或产品已经明确确认的当前规则，并明确示例不能作为正式需求；无需修改。
6. `已确认需求转测试用例指令.md` 原先要求直接修订旧工作簿，已改为决策摘录、当前规则重建、历史比较和候选同哈希流程。
7. `scripts/validate-generation-input.test.mjs` 已验证正常预生成、正常最终校验、历史参照提权阻断、生成脚本直读历史文件阻断、候选与最终 JSON 不一致阻断，共 5 项通过；两个脚本均通过 `node --check`。
8. 本次只修改通用规则和工具，没有重新生成测试用例。`用户App-直播模块-260902-002.xlsx` 及其任务目录保留为 RCL-0029 生效前的历史交付，不追溯改写；下一版生成必须使用新流程。

### 回退方法

1. 将 `AGENTS.md` 删除第 2.5 节，并恢复第 2.1、2.3、2.3.1、3、4.1、11、12、14 节对已有用例和生成顺序的修改。
2. 将 `Cem Kaner.txt` 恢复 `<<PREVIOUS_CASES_JSON>>` 输入及原执行入口和输出说明。
3. 将 `全局证据扫描指令.md` Schema 恢复为 `1.2`，删除输入角色候选和历史参照限制。
4. 恢复 `测试用例生成文件职责说明.md` 和 `已确认需求转测试用例指令.md` 的本次修改。
5. 删除 `scripts/validate-generation-input.mjs` 和 `scripts/validate-generation-input.test.mjs`；历史任务文件和已有 Excel 不删除。
6. 不删除或改写本条记录；执行回退时追加一条引用 `RCL-0029` 的新记录。

## RCL-0030 建立系统概要优先的派生需求同步规则

- 修改时间：2026-09-04 12:24:55 CST
- 用户要求：系统概要、原型或批注存在而需求清单缺失的内容补入；与需求清单冲突时以系统概要为准；系统概要、原型和批注均不存在而只存在于需求清单的内容清除。
- 涉及文件：`AGENTS.md`、`原型与需求清单同步指令.md`、`测试用例生成文件职责说明.md`、`规则文件变更日志.md`
- 修改前 SHA-256：`AGENTS.md` 为 `e9ce59c8908daed603f1218c83ad7f16c42bfb68e253b64cd3d76226feec7a1c`；`原型与需求清单同步指令.md` 为 `a4300ebac548a2315df37e833f0608782837320e145b8a2b4205e1337b664e7c`；`测试用例生成文件职责说明.md` 为 `82d8069b1236e3869c3d24a6edc67519a65fb319c41674f2ef0710ba48ddda6f`。
- 修改后 SHA-256：`AGENTS.md` 为 `c4cf5ae7184333b7247f0871662d78325396dac8977449996405279b134e2931`；`原型与需求清单同步指令.md` 为 `d3f326fe0152a551d561177b04480f566c70c3f09303089e97e8983219e432ee`；`测试用例生成文件职责说明.md` 为 `c95e25d0367b587d83eb5a6fde150b90b346c24944556fda06bd3294c06a0a1d`。

### 修改前原文

```text
AGENTS.md：
- prototype-primary：审核通过的原型是当前有效需求源，context/ 需求清单是原型的派生文档。
- 原型未表达不等于需求已删除；隐藏业务内容在原型未表达时保留并标记“原型未覆盖”或进入“需求待确认”。
- 只有来源明确写出删除、取消、下线、废弃或不支持时才能删除；不得因原型未展示删除。
- 交付门禁要求不存在因原型未展示而执行的删除，并保护原型未覆盖的隐藏规则。

原型与需求清单同步指令.md：
- 对 prototype-primary 项目，原型是当前有效需求源。
- 新增和修改只比较原型与派生需求；原型没有表达的内容归为“原型未覆盖”。
- 只有明确删除证据才能删除，隐藏业务规则在原型未表达时继续保留。

测试用例生成文件职责说明.md：
- prototype-primary 表示原型优先。
- 同步流程负责保护原型没有表达的隐藏业务规则。
```

### 修改后原文

```text
AGENTS.md：
- prototype-primary 项目存在已登记系统概要时，系统概要是派生需求同步中的最高业务规则来源，原型和批注负责页面、入口与交互。
- 系统概要、审核通过的原型页面或结构化批注中存在而派生需求缺失的内容必须补入。
- 同一业务规则冲突时系统概要优先；系统概要未覆盖时采用明确原型或批注。
- 只存在于派生需求清单，并经规则缺失复核确认系统概要、全部关联原型和结构化批注均不存在的孤立条目必须删除。
- 删除前保存完整原文、检索范围和回退方法；来源不完整、无法定位或存在未解决实质冲突时阻断同步。
- 系统概要可以证明业务规则，但不能单独证明目标端存在页面、按钮或查看入口。

原型与需求清单同步指令.md：
- 映射起点扩展为系统概要规则、原型文件、页面节点和结构化批注。
- 新增、修改、明确删除、原型未覆盖和来源冲突均按系统概要优先的新规则重新定义。
- 孤立条目删除必须完成同义词、全关联来源和上下文复核；单页、静态截图或单一关键词未命中不能作为删除依据。
- 系统概要明确但原型未展示的隐藏规则保留，但不得虚构目标端入口。

测试用例生成文件职责说明.md：
- prototype-primary 改为系统概要确定最高业务规则、原型和批注补充页面与交互。
- 明确三类来源取并集、系统概要冲突优先和三类来源均无时删除孤立条目。
```

### 联动检查结论

1. `Cem Kaner.txt` 已引用 `AGENTS.md` 第 2.1、2.2、3.1 节和 `原型与需求清单同步指令.md`，本次没有改变任务输入、执行入口或 JSON 输出接口，因此不修改。
2. `全局证据扫描指令.md` 已要求扫描全部现存 `context/`、系统概要、原型、公共批注和关联来源，并在删除或声称缺失前执行规则缺失复核；无需重复新增同一规则。
3. `新项目首次接入指令.md` 仍负责确认项目身份和来源策略，不需要改变接入 Schema。
4. 本次规则基线和同步指令 SHA-256 已变化，后续正式生成测试用例时旧全局证据缓存失效，必须执行 `full` 全量扫描。

### 回退方法

1. 将 `AGENTS.md` 的 `prototype-primary` 说明、派生需求同步四条规则、目标端差异处理、第 3.1 节第 4 至 7 项和第 14 节对应门禁恢复为本条“修改前原文”描述的 RCL-0029 后状态。
2. 将 `原型与需求清单同步指令.md` 的目的、映射、差异分类、自动修改保护及生成门禁恢复为原型优先且孤立条目保留的旧规则。
3. 将 `测试用例生成文件职责说明.md` 恢复为“prototype-primary：原型优先”和“保护原型未表达的隐藏业务规则”。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0030` 的新记录。

## RCL-0031 增加过程态与并发变更覆盖约束

- 修改时间：2026-09-04 18:07:47 CST
- 用户要求或修改原因：用户发现测试用例虽然覆盖“礼物下架后不能继续赠送”和“已完成赠送记录保留”，但没有系统覆盖用户已经发起操作、后台在请求处理中变更状态的中间态，例如用户赠送礼物时后台下架礼物。原有“并发和过程态”要求过于概括，生成时容易被“刷新后再操作”的稳定终态替代。
- 涉及文件：`AGENTS.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 3.2 节第 6 项；新增第 3.2.1 节“过程态与并发变更覆盖”。
- 修改前 SHA-256：`AGENTS.md` 为 `c4cf5ae7184333b7247f0871662d78325396dac8977449996405279b134e2931`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `be35f60e971c64f3fb56e208f35060ef4c24991a36fd2cc8a80dbe0cfceb66df`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
6. 并发和过程态：操作发生时仍在进行的会话、审批、支付、编辑或读取请求如何处理；仅在功能存在相应风险时检查。

规则没有定义哪些操作必须做过程态检查，也没有区分操作前、请求处理中、操作完成后及刷新重进后的时间点。只明确稳定终态而没有定义请求边界时，生成过程态用例可能擅自假设扣款、成功、失败或提示结果。
```

### 修改后原文

```text
高风险的扣款、结算、后台配置或商品状态、进房资格、角色权限、邀请和状态切换，不能只检查变更后刷新再操作的稳定终态。

过程态与并发变更覆盖要求按操作开始前已变化、客户端已发起或服务端处理中发生变化、本次业务完成后发生变化、刷新重进或重试后的状态分别判断；只有条件、处理边界或可观察结果不同才拆分用例。

请求完成边界必须由来源明确，不能把打开面板、点击、发起请求、服务端受理、扣款、落库或回显擅自视为同一时点。只定义前后稳定结果时，处理中边界进入独立需求待确认；不得默认失败不扣款、继续成功或编造提示。

后台、接口或测试工具可以用于制造过程态变化，但目标端用例只写实际入口和可观察结果。稳定终态、处理中边界和历史保留之间按时间点及业务结果执行语义去重。
```

### 联动检查结论

1. `Cem Kaner.txt` 已完整引用 `AGENTS.md` 第 3.2 节，任务输入和 JSON 输出接口均未变化，因此按文件职责不复制或修改同一规则正文。
2. `全局证据扫描指令.md` 已负责识别跨端配置、状态和共同业务对象；本次只细化候选生成时的测试设计与待确认边界，不改变扫描 Schema。
3. `原型与需求清单同步指令.md` 只同步来源中的明确业务事实，不能凭本规则补写未定义的处理中结果，因此无需修改。
4. 本次变更影响后续所有业务功能测试生成；当前任务按新规则重新构建用户 App 直播模块候选、历史比较和 Excel，不覆盖历史交付。

### 回退方法

1. 将 `AGENTS.md` 第 3.2 节第 6 项恢复为本条“修改前原文”的单句要求。
2. 删除 `AGENTS.md` 第 3.2.1 节“过程态与并发变更覆盖”。
3. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0031` 的新记录。

## RCL-0032 增加移动 App 生命周期与系统中断覆盖约束

- 修改时间：2026-09-04 18:56:15 CST
- 用户要求或修改原因：用户指出直播模块用例缺少移动 App 专项场景，尤其是切换后台、关闭 App、来电等系统中断。现有规则只有一般异常恢复和过程态要求，无法保证直播、连麦、交易和实时消息在 App 生命周期变化后得到系统性覆盖。
- 涉及文件：`AGENTS.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 3 节标准工作流第 10 项；新增第 3.2.2 节“移动 App 生命周期与系统中断覆盖”；第 6 节“风险驱动测试设计”。
- 修改前 SHA-256：`AGENTS.md` 为 `be35f60e971c64f3fb56e208f35060ef4c24991a36fd2cc8a80dbe0cfceb66df`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `7b95c8756083ff21de96eb03be8833a76a77c5ec4a2371cb1e59d2a939e63d1e`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
生成前覆盖清单没有要求识别移动 App 生命周期或系统中断。

业务结果完整性扫描和风险驱动测试设计没有规定何时区分退到后台、关闭 App、崩溃、来电、网络变化、运行中权限撤销，以及主播、观众、连麦参与者各自恢复后的可观察结果。

没有规则禁止把“退到后台”“关闭 App”“系统终止”混写，或在资料未定义时自行假设后台关播、来电静音、自动恢复、重连时限或提示文案。
```

### 修改后原文

```text
移动 App 且实际依赖持续会话、直播或实时音视频、摄像头或麦克风、网络连接、系统权限或交易回调的功能，必须建立生命周期与系统中断覆盖映射。

映射至少区分前后台、锁屏、显式关闭、系统回收或崩溃重启、来电或音频焦点中断、网络中断或网络切换、运行中相机或麦克风权限撤销；主播、观众和连麦参与者分别观察本端和必要的对端状态。

中断前、中断期间和恢复后需核对会话、实时状态、交易和消息的一致性。明确结果生成正式用例；资料未定义后台、来电、锁屏、进程终止或恢复边界时，逐项进入需求待确认，不得自行设定业务结果。

相同条件、结果和观察点的系统平台、设备或网络变体合并；只有平台能力、来源规则或可观察结果不同才拆分。
```

### 联动检查结论

1. `Cem Kaner.txt` 已将测试设计规则委托给 `AGENTS.md`，本次没有改变任务输入、执行入口、JSON 或 Excel 字段接口，因此不复制或修改同一规则正文。
2. `全局证据扫描指令.md` 和 `原型与需求清单同步指令.md` 分别负责证据读取和来源同步；本次只新增测试设计门禁，不改变扫描或同步 Schema。
3. 本次按用户要求只修改通用规则，未生成或修改任何测试用例 JSON、Excel、项目需求清单或历史交付。
4. 后续生成移动 App 相关模块时，规则基线已变化，应按现有全局扫描门禁重新选择扫描模式并建立过程态与生命周期覆盖映射。

### 回退方法

1. 将 `AGENTS.md` 第 3 节第 10 项恢复为不含“移动 App 生命周期与系统中断”的原文。
2. 删除 `AGENTS.md` 第 3.2.2 节“移动 App 生命周期与系统中断覆盖”。
3. 从 `AGENTS.md` 第 6 节删除移动 App 生命周期与系统中断的风险维度。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0032` 的新记录。

## RCL-0033 全量扫描前强制清除当前项目旧缓存

- 修改时间：2026-09-07 10:29:14 CST
- 用户要求或修改原因：用户要求每次全量扫描清除旧缓存文件。原规则只规定旧缓存“失效”且不得复用，没有要求从项目级缓存目录实际删除，可能导致执行者在全量扫描中误读旧缓存或继续保留多个过期缓存文件。
- 涉及文件：`AGENTS.md`、`全局证据扫描指令.md`、`测试用例生成文件职责说明.md`、`新项目首次接入指令.md`、`规则文件变更日志.md`。
- 修改位置：`AGENTS.md` 第 2.3 节、标准工作流第 4 步和第 14 节；`全局证据扫描指令.md` 第 2、4.4、5、10、11 节；`测试用例生成文件职责说明.md` 第 6.1、9 节；`新项目首次接入指令.md` 第 5、7 节。
- 修改前 SHA-256：`AGENTS.md` 为 `7b95c8756083ff21de96eb03be8833a76a77c5ec4a2371cb1e59d2a939e63d1e`；`全局证据扫描指令.md` 为 `aeb001570b0444b5446682a647129abbba9b591d4547ed384fb0c50c0778f633`；`测试用例生成文件职责说明.md` 为 `c95e25d0367b587d83eb5a6fde150b90b346c24944556fda06bd3294c06a0a1d`；`新项目首次接入指令.md` 为 `2c19c558724605e1f29f72ab8999a230f84e4c87f10d789d9ed1673b0e8220e6`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `19c88a087bbf7fee14b77892dcfa288db63161dcad18bfaa4ce4707e71d17e12`；`全局证据扫描指令.md` 为 `a17d0140eb810d51c18c517fc9438d561a462d4e2d6498e085f7b7a0d27bfabd`；`测试用例生成文件职责说明.md` 为 `cbc5126c3cf32102fca9813aba043101d70546596ae46e03d8628b5d8723fa05`；`新项目首次接入指令.md` 为 `277957ca9727e989a56ae668fc97a68c3e807ca691c4a4d666cd9a79f3bb1ca2`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
全量失效条件只要求旧缓存失效，不要求实际删除。

项目级缓存只在任务通过全部交付门禁后更新；任务快照独立保留。

global-evidence-scan-result.json Schema 为 1.3，没有“缓存清理”字段，也没有记录删除前文件、哈希、删除时间或残留校验。
```

### 修改后原文

```text
项目接入和来源策略前置门禁通过、扫描模式已确定后，full 和 fallback-full 必须在语义读取前清除当前项目 work/<项目目录名>-global-evidence-cache/ 内全部旧缓存文件。

清除前在本次 global-evidence-scan-result.json 记录缓存目录、旧文件相对路径、SHA-256、清除时间和清除结果；删除后检查残留。目录不存在或为空时记录清除数量为 0。

清除失败、存在残留或缺少记录时阻断扫描。旧缓存只可用于模式判断和清除审计，不得把业务结论带入本次全量索引。

清除范围不包含其他项目缓存、项目接入记录、历史任务快照、历史用例或测试输出。新项目级缓存只能在本次全量扫描和全部交付门禁通过后创建。
```

### 时机与范围说明

1. 先完成项目身份、正式入口和来源策略检查，再选择扫描模式；只有正式进入 `full` 或切换为 `fallback-full` 时才删除缓存，避免前置门禁尚未通过就破坏可审计信息。
2. `fallback-full` 与 `full` 使用同一清除规则；切换后必须丢弃此前复用的缓存结论并从当前证据重建索引。
3. 本次只新增后续扫描规则，没有执行一次新的正式全量扫描，因此没有删除当前 `work/liveshow-proto-global-evidence-cache/`。该项目当前接入和来源策略仍处于阻塞状态。
4. `Cem Kaner.txt` 已引用 `AGENTS.md` 第 2.3 节和 `全局证据扫描指令.md`，本次没有改变任务输入或 JSON 用例输出接口，因此不修改。

### 回退方法

1. 从 `AGENTS.md` 第 2.3 节删除“全量扫描清除旧缓存”条目，恢复后续条目编号，并移除标准工作流第 4 步和第 14 节中的缓存清除校验。
2. 从 `全局证据扫描指令.md` 删除第 4.4 节及固定产物中的清除范围说明；恢复第 5 节流程、任务快照 Schema 1.3 和第 11 节阻断条件。
3. 从 `测试用例生成文件职责说明.md` 和 `新项目首次接入指令.md` 删除本条新增的缓存清除说明。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0033` 的新记录。

## RCL-0034 增加角色与查看视角拆分门禁

- 修改时间：2026-09-07 16:25:00 CST
- 用户要求或修改原因：用户指出同一“在线观众”功能包含主播视角和普通观众视角，已有规则清单把多个角色合并后生成了未写明执行角色的用例，导致主播专属排序能力看起来像普通观众也可使用。用户要求将修复提升为通用规则，适用于所有项目和模块的同名多角色功能。
- 涉及文件：`AGENTS.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 2.2.1 节第 6 项、标准工作流第 13 步、第 14 节交付前质量门禁。
- 修改前 SHA-256：`AGENTS.md` 为 `19c88a087bbf7fee14b77892dcfa288db63161dcad18bfaa4ce4707e71d17e12`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `2ba4fac18aa40d5ae7bad47fee23d8afb7fb1f2c00b45b03245644713376cca9`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
目标端可执行性门禁只要求核对端、入口和观察位置，没有要求同一端内继续区分执行角色和查看视角。

规则清单可以把普通用户、主播、房管等多个角色写在同一“适用角色和端”中；当入口、字段、权限或结果不同，生成用例仍可能使用“用户”“查看列表”“打开详情”等未指明执行者的表述。

交付前质量门禁没有检查每条用例是否只包含一个执行角色和一个查看视角。
```

### 修改后原文

```text
同一端、同一功能名称被多个角色或身份使用时，先建立“执行角色/账号身份 -> 查看视角 -> 实际页面或入口 -> 可见字段和可执行动作 -> 可观察结果”映射。

入口、字段、权限、操作或结果任一不同，必须拆成不同稳定规则和不同用例。正式用例只能包含一个执行角色和一个查看视角；功能模块和功能结构仍不能唯一识别角色时，功能结构使用“功能名称（角色视角）”，并在前置条件或操作步骤中写明执行角色。

只有当前证据明确多个角色使用相同入口、动作和结果时才允许合并，但每条用例仍选择一个可执行角色；不得由其他角色视角推定缺少证据的本角色行为。交付前必须复核角色与视角拆分结果。
```

### 联动检查结论

1. `Cem Kaner.txt` 已将证据边界、标准工作流和测试设计规则委托给 `AGENTS.md`；本次没有改变任务输入、执行入口、JSON 根结构、字段或 Excel 接口，因此不复制或修改同一规则正文。
2. `全局证据扫描指令.md` 负责发现关联来源，`原型与需求清单同步指令.md` 负责同步来源中的明确业务事实；本次新增的是候选生成前的通用角色归属门禁，不改变扫描或同步 Schema，因此无需修改。
3. 当前用户 App 用例需要按新规则重建业务规则清单，拆分主播视角和普通观众视角，并重新执行去重、历史比较、输入隔离和 Excel 交付校验。

### 回退方法

1. 从 `AGENTS.md` 第 2.2.1 节删除第 6 项“按角色与查看视角拆分”。
2. 将标准工作流第 13 步恢复为不包含执行角色、查看视角和多角色拆分要求的原文。
3. 从第 14 节删除“每条正式用例只包含一个执行角色和一个查看视角”的质量检查项。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0034` 的新记录。

## RCL-0035 增加需求清单角色与动作拆分门禁

- 修改时间：2026-09-07 18:54:21 CST
- 用户要求或修改原因：用户指出修改或补充需求清单时可能把不同角色的操作合并成一句话。例如“当前直播场次已关闭，不能继续观看、评论、送礼或进入连麦”没有说明各动作属于哪个角色，并会误写成观众具有连麦能力。用户要求需求表述通过角色区分操作。
- 涉及文件：`AGENTS.md`、`规则文件变更日志.md`
- 修改位置：`AGENTS.md` 第 3.1 节第 6 项及第 14 节交付前质量门禁。
- 修改前 SHA-256：`AGENTS.md` 为 `2ba4fac18aa40d5ae7bad47fee23d8afb7fb1f2c00b45b03245644713376cca9`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `9fb6ce6da88852962daaa3d35238f81ae16e8991174928017f2793173e291832`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
原型与需求清单同步门禁只限制证据、差异类型和自动修改范围，没有明确要求在写入需求清单前逐项核对“角色或身份 -> 可执行动作”。

当多个角色受同一业务状态影响但操作权限不同时，需求表述可能使用“用户”“参与者”等泛称，把某一角色无权执行的动作并入该角色规则。

交付前质量门禁只检查正式用例的执行角色和查看视角，没有检查同步新增或修改的需求表述是否正确区分角色与动作。
```

### 修改后原文

```text
修改或补充派生需求清单前，必须将每个功能点拆成“适用角色或身份 -> 可执行动作 -> 必要状态 -> 可观察结果”，逐项核对动作归属。

不同角色受到同一状态限制，但可执行动作不同时，应分角色列出或拆成独立条目；不得使用“用户”“参与者”等泛称，把某一角色无权执行的动作与其可执行动作并列。直播关闭后，若观众只能观看、评论或送礼且没有连麦权限，观众规则不得包含“不能进入连麦”；连麦限制单独归入具备发起或参与连麦权限的角色。

只有证据明确多个角色的动作和结果完全相同时才允许合并。角色、权限或动作归属证据不足时，记录“来源冲突”或“需求待确认”，不得写入确定性需求。交付前必须复核同步新增或修改内容中的角色与动作归属。
```

### 联动检查结论

1. 本规则扩展 `RCL-0034`：`RCL-0034` 约束规则清单和正式用例按角色与查看视角拆分，本次进一步约束派生需求清单在同步写入时按角色与动作拆分。
2. `Cem Kaner.txt` 已将同步和证据治理规则委托给 `AGENTS.md`；本次没有改变任务输入、执行入口、JSON 根结构、字段或 Excel 接口，因此不修改。
3. 本次没有修改任何项目 `context/` 需求清单、测试用例 JSON、Excel 或历史交付；后续同步任务按新门禁执行。

### 回退方法

1. 从 `AGENTS.md` 第 3.1 节删除第 6 项“按角色拆分需求表述”，并将后续条目编号恢复为修改前编号。
2. 从 `AGENTS.md` 第 14 节删除同步需求表述的角色与动作归属检查项。
3. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0035` 的新记录。

## RCL-0036 分层缓存并优化规则比较流程

- 修改时间：2026-09-07 19:24:54 CST
- 用户要求或修改原因：用户要求检查测试用例生成流程的主要耗时并按建议优化。现有流程把文件建档与语义读取混为同一层，规则文件变化会重复读取全部证据；候选去重和历史比较还会对全部规则反复比较，无法稳定复用，也缺少分阶段耗时记录。
- 涉及文件：`AGENTS.md`、`全局证据扫描指令.md`、`原型与需求清单同步指令.md`、`测试用例生成文件职责说明.md`、`scripts/validate-generation-input.mjs`、`scripts/validate-generation-input.test.mjs`、`规则文件变更日志.md`。
- 修改前 SHA-256：`AGENTS.md` 为 `9fb6ce6da88852962daaa3d35238f81ae16e8991174928017f2793173e291832`；`全局证据扫描指令.md` 为 `a17d0140eb810d51c18c517fc9438d561a462d4e2d6498e085f7b7a0d27bfabd`；`原型与需求清单同步指令.md` 为 `d3f326fe0152a551d561177b04480f566c70c3f09303089e97e8983219e432ee`；`测试用例生成文件职责说明.md` 为 `cbc5126c3cf32102fca9813aba043101d70546596ae46e03d8628b5d8723fa05`。
- 修改后 SHA-256：`AGENTS.md` 为 `9c9b8fabd7e1c4bfe3f34bc463bebcfe3fe6d220cb24e6a1fa23bfdcf065fd5c`；`全局证据扫描指令.md` 为 `d429eaafc885addb4b1321a36879e1a15c18b69a085dd551998ab67e3c0a2958`；`原型与需求清单同步指令.md` 为 `ecbbb8d1bd7680d0f8967ec0cbff93a961a22a285fe9b5ffcc6bd6f4381802a3`；`测试用例生成文件职责说明.md` 为 `50133aa5845351e9aed402b71a023dd8db9e81b8d25750982d682b2e02f5ac8b`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改内容

1. 项目缓存拆为文件清单、证据原子、归一化规则和依赖、候选与历史比较、工作簿五层；上游变化只使受影响层及下游失效。
2. 全量哈希检查继续保留，但语义读取只覆盖变化文件和依赖闭包；规则文件变化不再无条件重读全部未变化证据。
3. 依赖索引细化为“文件 -> 页面或章节 -> 端 -> 模块 -> 角色 -> 动作 -> 稳定规则标识 -> 用例编号”。
4. 规则清单增加单角色、单动作、单结果的结构化约束；稳定规则标识由归一化业务键生成或沿用，不依赖数组顺序。
5. 历史比较优先使用备注中的稳定规则标识精确匹配，只对未匹配记录在相同业务键桶内回退比较；语义去重也限制在同类桶内。
6. 每次任务新增 `pipeline-metrics.json`，记录各阶段耗时、输入输出数量、复用数量和耗时最长阶段。
7. 输入隔离校验增加执行角色唯一性、角色与端一致性、原子动作数量和原子结果数量检查。

### 联动检查结论

1. 本次没有改变任务输入方式、正式 JSON 根结构、测试用例字段或 Excel 字段，因此不修改 `Cem Kaner.txt`。
2. 本次改变扫描 Schema 和证据原子提取约束；下一次用户 App 生成按新规则执行一次 `full`，通过后才发布新的分层缓存。
3. 性能优化不能绕过证据哈希、来源优先级、缺失复核、角色拆分、历史隔离或工作簿质量门禁。

### 回退方法

1. 将上述四份治理文档恢复为本条记录的修改前 SHA-256 对应内容。
2. 从输入隔离校验中移除执行角色、角色与端、原子动作和原子结果检查，并同步恢复测试夹具。
3. 删除本次新增但尚未被后续任务采用的分层缓存产物；不得删除历史任务快照、历史 Excel 或交付文件。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0036` 的新记录。

## RCL-0037 合并常用操作动词清单

- 修改时间：2026-09-08 10:51:36 CST
- 用户要求或修改原因：用户要求读取桌面 `测试用例常用操作动词整理.md`，将其中的动词加入测试用例生成规则，并去除重复词。
- 来源文件：`/Users/geekonup/Desktop/测试用例常用操作动词整理.md`，SHA-256 为 `6da98aadf5340d3ae2db5eaff79586e22fe6e6b43af3183efe3604c2f355038a`。
- 涉及文件：`AGENTS.md`、`规则文件变更日志.md`。
- 修改位置：`AGENTS.md` 第 10 节“语言和步骤规范”。
- 修改前 SHA-256：`AGENTS.md` 为 `9c9b8fabd7e1c4bfe3f34bc463bebcfe3fe6d220cb24e6a1fa23bfdcf065fd5c`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `a13753c2b3e65b2e8050f0a45590f8426189fe0b70951f85e36e528b177bf297`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
- 使用符合实际操作的动作词，如进入、点击、填写、选择、切换、刷新、返回、上传、下载、拖拽、扫描、复制、查看、登录和退出。
```

### 修改后原文

```text
- 操作步骤使用符合实际操作的动作词；与现有规则合并并按词面去重后，可根据场景选用：
  - 页面与控件：打开、进入、点击、单击、双击、长按、滑动、滚动、拖动、拖拽、展开、收起、切换、返回、刷新、关闭、退出、扫描、复制、查看。
  - 输入与填写：输入、填写、录入、编辑、修改、清空、删除、选择、勾选、取消勾选、上传、下载。
  - 数据与接口：调用、请求、发送、接收、校验、验证、检查、对比、查询、新增、创建、更新、插入。
  - 业务流程：登录、注册、提交、保存、确认、支付、取消、审核、审批、驳回、通过、拒绝、冲正、退款、撤销。
  - 状态操作：启用、禁用、激活、冻结、解冻、锁定、解锁、发布、下架。
- 预期结果可使用显示、展示、提示、生成、跳转、保持等可观察、可判断的结果词；其他同时可表示结果的词沿用上述清单，不重复列出。
- 词表只用于规范已有证据支持的动作和结果表达，不得据此新增页面、入口、权限、状态或业务行为。
```

### 去重结果与联动检查

1. 桌面文件共提取 71 个不重复动词；与原规则已有词合并后共保留 75 个不重复词。
2. 去重按词面执行；`点击/单击`、`拖动/拖拽`、`显示/展示` 等来源中分别列出的近义词仍保留，便于匹配不同实际操作和原文。
3. 操作步骤动词按页面、输入、数据、业务和状态用途分组；只用于预期结果且此前未列出的词单独补充，避免将结果描述误写为执行动作。
4. 词表只规范表达，不构成页面、入口、权限、状态或业务行为证据。
5. `Cem Kaner.txt` 已引用 `AGENTS.md` 的测试设计规则；本次未改变任务输入或 JSON 输出接口，因此不修改。

### 回退方法

1. 删除 `AGENTS.md` 第 10 节本次新增的分组词表、预期结果补充词和证据限制。
2. 恢复“修改前原文”中的单行动作词规则。
3. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0037` 的新记录。

## RCL-0038 禁止验证用例子项重复描述或预期结果

- 修改时间：2026-09-08 11:19:59 CST
- 用户要求或修改原因：用户发现 `UAPP-0963` 的“验证用例子项”和“预期结果”均为“仅普通房支持双主播连麦”。追查确认候选生成器对普通业务规则直接复用了完整结果句，且现有质量门禁没有检查这两个字段是否相同。用户确认按“生成逻辑 + 质量门禁 + 规则约束”修复，但本次不重新生成测试用例。
- 涉及文件：`AGENTS.md`、`scripts/validate-testcase-json.mjs`、`work/liveshow-user-app-all-260908-002/build-current-candidate.mjs`、`规则文件变更日志.md`。
- 修改位置：`AGENTS.md` 第 4.1 节及第 14 节；全局 JSON 校验器；当前用户 App 候选生成器的 `verificationPoint`。
- 修改前 SHA-256：`AGENTS.md` 为 `a13753c2b3e65b2e8050f0a45590f8426189fe0b70951f85e36e528b177bf297`；`scripts/validate-testcase-json.mjs` 为 `ac8ed8642825c59604036915d89bc243769c89cb24acf36a58de833595ede000`；`build-current-candidate.mjs` 为 `3976cec547cb90ad3a5fbc6e4f972022c68f9c52f7e473c2f2f6d5a8822ec840`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `26bd243905416113bdbcbe6f24a4a85fdbb32a8aba3202ffe25cac9f0d09d396`；`scripts/validate-testcase-json.mjs` 为 `2bb07e14e3b26a202564521f9506b5d9afc59c940edaf2ba6fa44f74d9c764bc`；`build-current-candidate.mjs` 为 `3d9cd707db439db771f4043d67897d3cefaa88ae9da88db7600a106a80c3ad39`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
AGENTS.md：
- `验证用例子项`：写本条用例唯一的业务验证点，不重复抄写整个用例描述。

第 14 节没有检查“验证用例子项”是否与用例描述正文或预期结果完全相同。

build-current-candidate.mjs：
普通业务规则未命中状态转换、限制或隐藏规则时，verificationPoint 直接返回原始规则文本；compactExpected 同时保留目标状态或可观察结果，导致两个字段可以完全相同。

scripts/validate-testcase-json.mjs：
只检查用例描述格式、单一预期结果、稳定规则标识和跨用例完全重复，没有检查同一用例内“验证用例子项”与描述正文或预期结果重复。
```

### 修改后原文

```text
AGENTS.md：
- `验证用例子项`：使用简短名词短语写本条用例唯一的验证维度或对象，不重复抄写整个用例描述。去除序号、空格和标点后，不得与用例描述正文或预期结果完全相同；它可以与预期结果指向同一观察点，但不能直接复制完整结果句。

- 每条正式用例的“验证用例子项”均为简短验证维度或对象；去除序号、空格和标点后，没有与用例描述正文或预期结果完全相同。

build-current-candidate.mjs：
verificationPoint 先保留已有的状态转换、限制和隐藏规则提取；若提取结果与用例描述正文或预期结果相同，再按适用范围、入口、提示、展示内容、计算规则、状态变化、字段或业务规则生成简短验证维度。`仅普通房支持双主播连麦` 转换为 `双主播连麦适用范围`。

scripts/validate-testcase-json.mjs：
去除序号、空格和标点后，分别比较“验证用例子项”与用例描述正文、唯一预期结果；完全相同时将 JSON 校验结果标记为失败。
```

### 验证与影响

1. 两个 JavaScript 文件均通过语法检查，文件差异检查通过。
2. 仅调用文本生成函数静态检查当前 1137 条可生成规则，没有写入候选 JSON；子项重复预期结果为 0，子项重复描述正文为 0，`UAPP-0963` 的未来子项为“双方主播连麦适用范围”。
3. 使用新全局校验器只读检查现有 `260908-002` JSON，识别出 976 条子项与预期结果完全相同，其中包含 `UAPP-0963`。这证明门禁生效，不表示已修改旧 JSON。
4. 本次没有运行候选生成、历史比较、Excel 生成或 Excel 检查，也没有修改任何已有 JSON、Excel、规则清单或历史交付。
5. `Cem Kaner.txt` 已引用 `AGENTS.md` 的字段和质量门禁，本次未改变任务输入或 JSON 输出字段，因此不修改。

### 回退方法

1. 将 `AGENTS.md` 第 4.1 节的“验证用例子项”要求恢复为本条修改前原文，并删除第 14 节新增的字段重复检查。
2. 从 `build-current-candidate.mjs` 删除 `comparableText` 和重复字段改写逻辑，恢复 `verificationPoint` 的原返回方式。
3. 从 `scripts/validate-testcase-json.mjs` 删除 `comparableText` 及两条同一用例字段重复检查。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0038` 的新记录。

## RCL-0039 更正 RCL-0038 的 UAPP-0963 子项文字

- 修改时间：2026-09-08 11:21:31 CST
- 用户要求或修改原因：记录 `RCL-0038` 时，将静态检查得到的“未来子项”误写为“双方主播连麦适用范围”；实际生成函数返回“双方”中的“方”不存在，应更正为“双主播连麦适用范围”。按照日志只追加不改写规则，保留原记录并追加本条更正。
- 涉及文件：`规则文件变更日志.md`。
- 修改位置：对 `RCL-0038`“验证与影响”第 2 项的更正说明。

### 修改前原文

```text
`UAPP-0963` 的未来子项为“双方主播连麦适用范围”。
```

### 修改后原文

```text
`UAPP-0963` 的未来子项为“双主播连麦适用范围”。
```

### 回退方法

1. 本条用于纠正已确认的记录笔误，不应回退为错误文字。
2. 如后续生成逻辑再次变化，应追加新的规则变更记录，不删除或改写 `RCL-0038` 和本条记录。

## RCL-0040 合并测试用例含糊词汇清单

- 修改时间：2026-09-08 11:29:47 CST
- 用户要求或修改原因：用户要求读取桌面 `测试用例含糊词汇整理.md`，将其中的模糊词加入测试用例规则，并去除重复词。
- 来源文件：`/Users/geekonup/Desktop/测试用例含糊词汇整理.md`，SHA-256 为 `d2871632ddd5e38f4a2c11984538f2b080ff10499d9f5a4d55994c58b0d380cc`。
- 涉及文件：`AGENTS.md`、`规则文件变更日志.md`。
- 修改位置：`AGENTS.md` 第 10 节“语言和步骤规范”及第 14 节质量门禁。
- 修改前 SHA-256：`AGENTS.md` 为 `26bd243905416113bdbcbe6f24a4a85fdbb32a8aba3202ffe25cac9f0d09d396`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `15fc25bc97b88d855b321b7f306e2b45139a20e05c8d294b87226ba2220d8791`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
- 禁止使用 `功能正常`、`结果正确`、`有合理提示`、`无异常`、`符合预期`、`同步正常` 等无法直接判断的结果。

第 14 节没有含糊表达专项质量检查。
```

### 修改后原文

```text
- 以下含糊表达在缺少明确对象、操作位置、测试数据、数量、时限、状态变化或可观察结果时不得单独使用；与现有禁用表达按完整词组去重后包括：
  - 操作不明确：点击一下、点进去、看一下、测试一下、随便输入、输入正确密码、填写信息、操作一下、试试、刷新一下、验证一下。
  - 时间不明确：很快、稍等、一段时间后、立即、最后。
  - 结果或状态不明确：功能正常、正常显示、页面没问题、结果正确、数据正确、成功了、可以使用、没有异常、有合理提示、更新成功、数据同步、自动处理、检查是否正确、看是否成功、无异常、符合预期、同步正常、正常。
  - 数量不明确：多次、少量数据、大量数据、几个。
- 改写含糊表达时，必须补充可执行和可判断的信息，例如具体对象、控件、数据、数量、时限、状态变化或观察标准。参考文件中的示例数值和账号数据仅用于说明表达方式，不是业务证据。

- 前置条件、操作步骤、预期结果和需求待确认内容没有单独使用第 10 节列出的含糊表达；保留来源原文时已在备注中注明，正式执行字段已补充明确对象、位置、数据、数量、时限、状态变化或观察标准。
```

### 去重结果与联动检查

1. 桌面文件共提取 34 个不重复词组；与原规则已有禁用表达合并后，共保留 38 个不重复词组。
2. 自动检查结果为重复 0 个、桌面来源漏项 0 个；按完整词组去重，含义相近但文字不同的表达分别保留。
3. `立即`、`更新成功`、`数据同步` 等表达只有在缺少明确时限、对象、状态变化或观察标准时才属于含糊表达，不作无条件禁用。
4. 桌面文件中的示例数值和账号数据不是当前项目业务证据，不得直接转写为业务阈值、账号或测试数据。
5. 新增第 14 节交付门禁，检查正式执行字段和需求待确认内容不得单独使用含糊表达。
6. 本次没有改变任务输入或 JSON 输出接口，因此不修改 `Cem Kaner.txt`；没有修改生成脚本或重新生成测试用例。

### 回退方法

1. 删除 `AGENTS.md` 第 10 节本次新增的含糊词分类和改写要求，恢复“修改前原文”中的单行规则。
2. 删除第 14 节本次新增的含糊表达专项质量检查。
3. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0040` 的新记录。

## RCL-0041 单文件分层重构并集中语言执行规则

- 修改时间：2026-09-08 12:47:17 CST
- 用户要求或修改原因：用户确认在不拆分 `AGENTS.md` 的前提下优化可读性，要求增加快速门禁、稳定规则编号、按执行顺序组织流程、缩短重复内容、集中机器语言规则并让校验程序直接读取。
- 涉及文件：`AGENTS.md`、`scripts/validate-testcase-json.mjs`、`scripts/validate-testcase-json.test.mjs`、`规则文件变更日志.md`。
- 修改位置：`AGENTS.md` 新增第 0 节，重构第 3、10、14 节并为主要章节增加稳定规则编号；JSON 校验器新增机器规则加载和语言门禁；新增最小回归测试。
- 修改前 SHA-256：`AGENTS.md` 为 `15fc25bc97b88d855b321b7f306e2b45139a20e05c8d294b87226ba2220d8791`；`scripts/validate-testcase-json.mjs` 为 `2bb07e14e3b26a202564521f9506b5d9afc59c940edaf2ba6fa44f74d9c764bc`；`scripts/validate-testcase-json.test.mjs` 不存在；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `107527f2ee5640cce252d268ab7a0d92b5df2b583ffe4c95c618afc754edc359`；`scripts/validate-testcase-json.mjs` 为 `30f7698c2b2a03e032d836cfcef9a746e022c8089802c5d84e5810150ff6e026`；`scripts/validate-testcase-json.test.mjs` 为 `88abf5a5994dbe84a1358f9e7db00851cabd00668ebb9cbca3d341b34af934e5`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
AGENTS.md 没有快速执行区和稳定规则编号；第 3 节使用 18 个步骤重复展开各门禁，第 14 节再次逐条复述前文检查内容。

第 10 节分别以正文列表维护动作词和含糊词，没有集中禁止“业务规则”“操作结果”“执行目标操作”等通用占位内容，也没有供程序直接读取的固定格式区域。

scripts/validate-testcase-json.mjs 只校验字段结构、单结果、规则标识、完全重复，以及验证子项与描述或预期完全相同；不会读取 AGENTS.md 中的语言规则。
```

### 修改后原文

```text
AGENTS.md 第 0 节集中说明规则用语、16 个核心稳定规则编号、8 个固定执行阶段和统一停止条件；第 3 节只保留阶段、产物和失败处理，第 14 节改为按规则编号分组的交付检查。

第 10 节明确功能结构、用例描述、验证用例子项、操作步骤和预期结果的独立职责，并在同一文件中使用 TESTCASE_LANGUAGE_RULES 标记保存唯一机器规则区。机器规则区集中维护动作词、结果词、含糊词和通用占位词，业务证据门禁仍由 AGENTS.md 正文负责。

scripts/validate-testcase-json.mjs 直接解析 AGENTS.md 机器规则区，规则区缺失、JSON 结构错误或版本错误时停止校验；同时检查通用描述尾词、通用验证子项、通用步骤、单独含糊表达和带序号的预期结果。词表不在脚本中重复维护。
```

### 验证与影响

1. `node --check scripts/validate-testcase-json.mjs` 通过；`node --test scripts/validate-testcase-json.test.mjs` 共 2 项测试，全部通过。
2. 机器规则区可以解析，合并后的动作词重复数量为 0；具体字段样例通过，通用占位内容和带序号预期均被拦截。
3. 使用新校验器只读检查 `260908-003` 当前 JSON，共识别 2206 个语言问题：1234 条通用描述、457 条通用验证子项、515 条通用步骤；`UAPP-0954` 的三个已知问题均被命中。
4. 本次规则变化最早影响生成与质量校验层；当前证据原子和来源哈希仍兼容，不要求仅因本次重构重复读取未变化证据。
5. 本次没有修改或重新生成任何测试用例 JSON、Excel、业务规则清单、需求清单、原型或历史交付。
6. `Cem Kaner.txt` 已引用 `AGENTS.md` 的生成和质量规则；本次没有改变任务输入、JSON 根结构、字段或 Excel 接口，因此不修改。

### 回退方法

1. 删除 `AGENTS.md` 第 0 节和主要章节的稳定规则编号，恢复第 3、10、14 节为本条修改前内容。
2. 从 `scripts/validate-testcase-json.mjs` 删除机器规则读取、通用占位词、含糊词和预期序号检查，并恢复原直接执行入口。
3. 删除 `scripts/validate-testcase-json.test.mjs`；不得修改任何已有测试用例或历史交付。
4. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0041` 的新记录。

## RCL-0042 增加资深 QA 证据分析总则

- 修改时间：2026-09-08 18:31:54 CST
- 用户要求或修改原因：用户确认增加资深 QA 分析前提，要求在提取业务规则和生成测试用例前理解页面结构、交互流程及批注表达的功能逻辑，不得只描述页面元素；同时保持现有多来源策略和证据边界。
- 涉及文件：`AGENTS.md`、`规则文件变更日志.md`。
- 修改位置：`AGENTS.md` 第 2.1 节 `EVID-01`，三种来源策略之后。
- 修改前 SHA-256：`AGENTS.md` 为 `107527f2ee5640cce252d268ab7a0d92b5df2b583ffe4c95c618afc754edc359`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `5894577472466de270746b989562e48c98fad181a9396f002d8c61518c8d8de1`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
第 2.1 节在三种来源策略后直接说明旧项目默认策略，没有统一规定提取业务规则前应如何理解页面结构、交互、动态状态和批注上下文。
```

### 修改后原文

```text
在任一来源策略下，必须以资深 QA 视角执行证据分析。提取业务规则前，必须按照当前项目的来源策略，结合页面结构、可执行交互、动态状态、批注上下文及其他当前证据，建立“执行角色 -> 页面入口 -> 触发动作 -> 必要条件或状态 -> 可观察结果”映射；不得将页面元素清单、静态展示值或 Mock 示例直接当作业务规则。完成业务规则归一化后，再按 `RESULT-01`、`ROLE-01` 和测试设计章节建立测试覆盖并生成测试用例；证据不足或冲突的内容按 `EVID-01`、`GAP-01` 进入“需求待确认”。
```

### 验证与影响

1. 新总则适用于全部来源策略，不会把原型提升为 `requirement-primary` 或 `conflict-review` 项目的唯一证据。
2. 总则引用现有 `RESULT-01`、`ROLE-01`、`EVID-01` 和 `GAP-01`，未重复定义对应门禁。
3. 本次规则变化最早影响证据语义读取和业务规则归一化层；后续生成任务应从受影响层重建覆盖、候选、历史比较及交付产物。
4. 本次没有修改或重新生成测试用例 JSON、Excel、业务规则清单、需求清单、原型或历史缓存。
5. 本次没有改变任务输入或 JSON 输出接口，因此不修改 `Cem Kaner.txt`。

### 回退方法

1. 删除 `AGENTS.md` 第 2.1 节本次新增的资深 QA 证据分析总则段落，恢复为三种来源策略后直接说明旧项目默认策略。
2. 不删除或改写本条历史记录；执行回退时追加一条引用 `RCL-0042` 的新记录。

## RCL-0043 落实结构化执行设计、条件去重和强制交付门禁

- 修改时间：2026-09-09（Asia/Shanghai）。
- 用户要求：按照生成逻辑与门禁审查方案修正问题，避免再次放行不可执行用例或误拦合法边界用例。
- 涉及文件：`AGENTS.md`、生成及校验脚本；`Cem Kaner.txt` 的正式 JSON/Excel 接口未变，不重复维护规则正文。
- 最早影响层：证据原子与规则归一化。旧任务作为历史保留，不得以旧“通过”状态复用受影响结果。

### 修改位置、修改前原文与修改后原文

1. `CASE-01`“缺少确定性预期时，不生成可执行用例；将问题写入‘需求待确认’。”之后，原无以下段落，新增：

```text
解析失败、角色映射未完成或测试数据尚未设计属于“生成待复核”，不得自动判定为业务规则缺失。复核必须回到当前证据；只有按 GAP-01 完成缺失复核后，才允许转入“需求待确认”。规则清单须保留全部覆盖点及处理去向，不得通过丢弃未解析内容使门禁通过。
```

2. 第 4.1 节子项定义后，原无以下条目，新增：

```text
同一验证维度在不同输入、必要条件或边界下可以使用相同子项；不得为满足字符串唯一性而截断名称或追加无业务含义的尾词。冲突和重复判断必须包含必要条件、关键输入、执行角色、操作及观察结果。
```

3. 第 9 节末，原无以下段落，新增：

```text
计算要求按本条观察点是否涉及数值运算判断，不按“逻辑校验”类型一律套用。计算测试数据必须明确业务变量、原始值、单位、数据范围及纳入条件，按有来源的公式独立复算；禁止在未匹配公式时补写通用算式。只验证展示上限、省略或字段可见性的用例不得附加无关计算结果。
```

4. `LANG-01` 机器规则区，修改前：

```json
"stepPatterns": ["^执行.+操作$", "^尝试(?:继续)?(?:该|对应)?受限操作$"]
```

修改后：

```json
"stepPatterns": ["^执行.+操作$", "^尝试(?:继续)?(?:该|对应)?受限操作$", "查看页面(?:状态|反馈)[。；]?$", "^.*输入符合“.+”的(?:密码|价格|关键词).*$"],
"resultAsActionPatterns": ["^(?:提交|保存|发送|上传|处理|支付|审核|加载|请求)(?:成功|失败|完成)[。]?$", "^.*完成“.+”后查看.*$", "^.*触发“.+”[。]?$" ]
```

5. `DELIVERY-01` 的“还必须完成以下核对”原有 1 至 6 项，在其后新增：

```text
7. 生成器只能将已复核的结构化用例设计渲染为正式字段；不得从未复核的自然语言片段猜测动作、补写观察位置或套用计算结果。规则清单中的设计须包含场景、子项、逐步角色与对象映射、观察位置和证据引用；复核结果绑定设计内容哈希，任何修改后须重新复核。这里的复核由生成执行者完成，不是要求用户逐条批准。
8. 自动语言检查不能代替证据语义复核。生成、最终 JSON、Excel 导出和缓存发布入口均必须执行适用门禁；检查记录须绑定本次输入、规则、候选及工具版本的哈希。扫描、同步、历史比较和交付状态由实际检查生成，不得写死“通过”、空复核列表或历史变更结论。未关闭的生成复核、未映射覆盖点或失效报告均阻断交付。
```

### 回退方法

逐项删除本记录新增的 CASE-01、4.1、9、DELIVERY-01 段落，并将机器规则区恢复为上述修改前原文。生成和校验脚本须配套回退，不能只撤除门禁继续使用未复核产物。保留其他已有改动、历史用例与本条记录；实际回退另追加引用 RCL-0043 的记录。

### 2026-09-09 实施与验证补记

- 共用生成器已移除从自然语言片段猜动作、拼子项、补通用算式及自动写入“通过”的旧路径，正式输出改由已复核设计渲染。候选文件采用排他创建，避免覆盖已用于历史比较的候选。
- 回归覆盖：描述与子项重复、逐步动作、角色及端/模块归属、同子项不同输入边界、正负号、多个独立结果、当前证据变更、空去重复核、失效报告、历史漏评及把变更误判为继续有效。
- 修复合法输入误拦：一个支付渠道对象可同时受端、地区与账号限制；操作步骤中填写的具体数值也计入测试数据。展示 RTP 或省略号不会被强加计算公式。
- 数值检查独立复算命名变量，校验算式末项而非数字子串；统计四舍五入使用十进制格式化，覆盖 1.005 保留两位为 1.01 的边界。仅由实现推导的用例会在备注明确标记。
- 运行四个回归文件，Node 测试报告 14 项通过、0 项失败。该结果证明已覆盖的程序检查通过，不代表全部业务规则、三端草稿或 Excel 已完成复核。正式输出仍须经过本次真实证据、设计、历史比较和工作簿检查。

### 2026-09-09 输入空格与发布复核补记

- 后续草稿复核发现：去重键删除所有空白字符，会把带首尾空格的测试输入误判成无空格输入；同一输入的保存值含空格与不含空格也可能被当作相同预期。已在 `scripts/validate-testcase-json.mjs` 的同条件冲突键和完全重复键中保留完整字段值。文字相似性仍只作为语义复核线索，不得删除测试数据中的空格。
- 增加一项对应回归检查后，四个回归文件共 15 项通过、0 项失败；原有正负数、边界、角色、证据、空复核报告和历史隔离检查仍通过。
- 合并缓存发布以实际执行的交付检查为准，并记录发布前检查结果及时间；移除对旧扫描结果中可写状态标记的重复依赖，避免为了发布而修改已经固定哈希的扫描报告。
- 本补记未修改治理正文或 JSON 输出接口；回退实现时保留该记录，并与 RCL-0043 的配套生成和门禁代码一起核对。业务草稿及 Excel 仍须完成各自复核，不能凭上述代码测试结果交付。

### 2026-09-09 本次抽查授权与交付收尾补记

- 用户明确要求“不用再一条一条的检查复核了，差不多就行了”。本次三端指定模块按当前任务内的 `DECISION-REVIEW-20260909-03` 执行自动检查及抽查；没有这条当前授权时，仍执行默认覆盖门禁。未逐条复核的证据保留原文、去向与限制，不伪装成需求缺失或已完成逐条审核。
- `validate-testcase-delivery.mjs` 校验授权文件角色、哈希、问题编号和原答；抽查例外只允许真实记录“未逐条复核”，不放宽证据哈希、角色、结构化设计、候选一致性、历史隔离及 Excel 内容检查。回归已验证无授权、原答不符仍阻断，有效授权后篡改候选仍阻断；四个回归文件合计 15 项通过。
- 生成范围补回已指定直播模块内的“举报直播间”子页面；用例备注保留唯一来源位置，完整证明原文留在规则清单。表格按内容调整行高，并补齐负责人、阻塞等级的实时汇总；测试结果使用精确匹配，避免“不通过”误命中“通过”的配色。
- 回退时仅撤回上述配套实现，保留已有需求决策、历史工作簿及日志；不能将本次抽查记录当成后续逐条语义复核完成的依据。该补记不改变全局治理正文或 JSON 字段契约。

## RCL-0044 将整批停止改为候选、用例和批次三级处理

- 修改时间：2026-09-09 16:24:06 CST（Asia/Shanghai）。
- 用户要求：质量校验用于保证用例质量，不得因局部信息缺失停止整个任务；候选问题继续生成并标记“需求待确认”，单条问题用例局部隔离，只有输入完整性、核心规则冲突或交付结果无法保证时才阻断批次。
- 涉及文件：`AGENTS.md`、`全局证据扫描指令.md`、`原型与需求清单同步指令.md`、`scripts/validate-generation-input.mjs`、`scripts/validate-generation-input.test.mjs`；`Cem Kaner.txt` 的任务输入和 JSON 输出接口未变化，不修改。
- 最早影响层：归一化规则层。证据文件清单和证据原子 Schema 未变化，不要求重新读取未变化证据；采用本规则的后续任务应从规则状态、候选隔离、历史比较和交付检查向下重建。
- 修改前 SHA-256：`AGENTS.md` 为 `3294c860f8edf79051fa6d577b2076e3f33015665bd07a2b7399ca05faaf9620`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `68de9e3a8b05897dd40d6114dff8ef747c30323c39517abb93096e026c663f69`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改位置、修改前原文与修改后原文

1. `AGENTS.md` 第 0.2 至 0.4 节，修改前：

```text
核心规则索引没有 GATE-01；扫描、输入、同步、语言、去重和交付失败主要写为“停止生成”或“停止交付”。

出现以下任一情况时，不得生成或交付正式 JSON、Excel：项目未完成接入；来源策略无法确定；证据扫描或同步阻塞；目标端入口、执行角色、具体动作或可观察结果缺少依据；输入角色、规则映射或哈希校验失败；历史比较仍有“待人工复核”；候选与最终 JSON 不一致；语言、语义去重或工作簿门禁失败。业务规则确实缺失时进入“需求待确认”；扫描未完成、文件无法读取或依赖未展开属于流程阻塞，不得伪装成需求缺口。
```

修改后新增 `GATE-01`，并将处理规则统一为：

```text
质量校验用于保证正式用例可靠，同时保留不受局部缺口影响的有效覆盖。发现问题后必须先确定最小影响范围，再区分可继续处理、需要隔离和必须阻断；禁止使用“一处问题导致整个流程停止”的粗粒度策略。

候选层：需求解析、角色映射、原型批注或业务规则不完整时，允许在“需求待确认”中保留候选测试点，说明已知依据、缺失信息、影响范围和确认后待补用例；不得混入正式用例，不影响其他明确规则继续生成。

用例层：单条用例缺少角色、入口、结果、数据或存在歧义时，只隔离该用例及直接依赖，设置“可生成正式用例=false”；只重建受影响规则、用例及下游产物，不重新生成全部用例。正式用例仍须满足 CASE-01、ROLE-01、LANG-01 和 DEDUP-01。

批次层：只有输入完整性问题、无法限定影响范围的核心规则冲突、交付结果无法保证时才停止当前批次。能够限定到具体规则、角色、场景或模块的问题必须局部隔离。

信息不足时不得编造业务规则或把推断当作确定需求。对于开启、关闭、删除、修改、切换和状态变化，分析当前模块、上下游模块、不同角色、数据状态和后续流程；无依据的影响关系标记为“待确认影响范围”。

正式交付允许包含已隔离问题、需求待确认和候选测试场景，但必须与正式用例清楚分离并可追溯。执行优先级为：业务理解正确 -> 保留有效覆盖 -> 隔离局部不确定因素 -> 仅在系统性风险出现时阻断。
```

2. `AGENTS.md` 的 `CASE-01`、`SCAN-01`、`INPUT-01`、标准工作流、`SYNC-01`、`RESULT-01`、`DEDUP-01` 和 `DELIVERY-01`，修改前把“生成待复核”、局部扫描或同步问题、历史待复核、去重待复核及任一交付失败直接写成停止生成或停止交付。修改后统一引用 `GATE-01`：能定位到规则、角色、用例或模块时局部隔离，保留其他正式用例；只有输入、公共证据、核心冲突或结果一致性无法限定影响范围时批次阻断。

3. `INPUT-01` 当前规则清单，修改前：

```text
一条规则只能包含一个执行角色、一个动作和一个可观察结果；来源冲突或证据缺口的规则不得标记为可生成正式用例。
```

修改后：

```text
正式规则只能包含一个执行角色、一个动作和一个可观察结果。候选层或用例层隔离项必须设置“可生成正式用例=false”；尚未确认的执行角色、动作或结果允许为空或记录待选范围，但必须在覆盖清单和“需求待确认”中说明缺失信息和处理去向。来源冲突、证据缺口或生成待复核的规则不得标记为可生成正式用例。
```

4. `全局证据扫描指令.md` 和 `原型与需求清单同步指令.md`，修改前把扫描、同步条件中的局部失败直接归入阻断。修改后要求先限定影响范围：具体规则、角色或模块问题进入隔离和“需求待确认”；系统性基线、输入完整性、核心冲突或结果一致性失效才阻断当前批次。

5. `scripts/validate-generation-input.mjs`，修改前：

```js
requireString(rule.执行角色, `${label}.执行角色`);
if (rule.适用角色和端.length !== 1 || !rule.适用角色和端[0].includes(rule.执行角色)) fail(...);
requireString(rule.触发动作, `${label}.触发动作`);
if (rule.规则状态 === "生成待复核") fail(...);
if (item.判定 === "待人工复核") fail(...);
```

修改后：

```text
执行角色和触发动作只有在“可生成正式用例=true”时才必须完整；“生成待复核”且不可生成正式用例的规则允许保留为空并继续候选流程。历史比较为“待人工复核”时，只有绑定非正式隔离规则且没有引用正式候选才允许继续；仍关联正式候选时继续阻断。
```

### 验证与影响

1. `node scripts/validate-generation-input.test.mjs` 通过，覆盖待复核规则局部隔离、待复核历史记录与正式候选隔离、输入哈希、范围、历史来源和候选一致性门禁。
2. `node --test scripts/testcase-design.test.mjs scripts/validate-testcase-json.test.mjs` 共 13 项通过、0 项失败；正式用例的角色、入口、动作、结果、计算、语言、重复和输入空格门槛未放宽。
3. `git diff --check` 对本次涉及文件通过。
4. 本次没有生成或修改测试用例 JSON、Excel、业务规则清单、需求清单、原型或历史缓存。
5. 本次没有改变 `Cem Kaner.txt` 的任务输入或 JSON 输出接口，因此不修改该文件。

### 回退方法

1. 从 `AGENTS.md` 核心规则索引删除 `GATE-01`，将第 0.4 节恢复为上述“出现以下任一情况时”统一停止段落，并按本记录逐项恢复 `CASE-01`、`SCAN-01`、`INPUT-01`、标准工作流、`SYNC-01`、`RESULT-01`、`DEDUP-01` 和 `DELIVERY-01` 的原阻断表述。
2. 恢复两份执行指令中本次修改的扫描、同步阻断原文。
3. 恢复 `validate-generation-input.mjs` 对执行角色、触发动作、“生成待复核”和历史“待人工复核”的原整批失败判断，并删除对应新增回归检查。
4. 不删除或改写本条历史记录；实际回退时追加一条引用 `RCL-0044` 的新记录。

## RCL-0045 按字段职责精简测试用例内容

- 修改时间：2026-09-10 12:18:40 CST。
- 用户要求或修改原因：用户要求精简用例描述、验证用例子项、前置条件、操作步骤、预期结果和备注；精简必须提升人工阅读效率，使执行人员一眼看懂场景和判断标准，不得机械删词或截断内容。
- 涉及文件：`AGENTS.md`、`scripts/testcase-design.mjs`、`scripts/testcase-design.test.mjs`、`scripts/validate-testcase-json.mjs`、`scripts/validate-testcase-json.test.mjs`、`规则文件变更日志.md`。
- 修改位置：`AGENTS.md` 第 4.1、10 节及机器语言规则区；结构化用例设计校验、正式用例备注生成、JSON 语言校验和对应回归检查。
- 修改前 SHA-256：`AGENTS.md` 为 `68de9e3a8b05897dd40d6114dff8ef747c30323c39517abb93096e026c663f69`；`scripts/testcase-design.mjs` 为 `669cc819cd9d965f5ddb56f52bf4ae16e19a0b53984585c3974bef6df87c41e0`；`scripts/testcase-design.test.mjs` 为 `0bb5c1110b724bde347b6ef5884ed42dab5a2f782c045203d87f8e75954b6058`；`scripts/validate-testcase-json.mjs` 为 `46697b5004eca159f67c4330d234135ed7cafee15a10349f5714f75d07bc97b2`；`scripts/validate-testcase-json.test.mjs` 为 `48da7df401f218152192f4d0c7a44857a169d0827afa2e7856ac4f208f04045f`；`Cem Kaner.txt` 为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后 SHA-256：`AGENTS.md` 为 `4d2ecea1deb81bf7711ec54e9ba3337f853adbd3c0e001e1e2d19ba7e992097c`；`scripts/testcase-design.mjs` 为 `1476c5212ed78c049f5061d44bab1db59ee44c350f9dcf218a3f496fe4deda85`；`scripts/testcase-design.test.mjs` 为 `854aeef4b404609d1d10032ed3daa3b2b6ac3cd535f50d0d5e9588fcb4027782`；`scripts/validate-testcase-json.mjs` 为 `a441260924499787f4619d0be82dd3ba0573d651d09f7ead95a410ef580d92f6`；`scripts/validate-testcase-json.test.mjs` 为 `30d0838cd2a1a325e1f22a52871ac2ed4f42e5789a133a0e76cb28f3b0a0304b`；`Cem Kaner.txt` 未修改，仍为 `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。

### 修改前原文

```text
用例描述、验证用例子项、操作步骤和预期结果已有基本职责，但没有规定六个执行字段连读时必须让人员一眼识别场景；前置条件仍允许写页面路径和进入动作，备注使用“说明：依据当前资料静态设计，未动态验证产品行为”的固定长句。

校验器只拦截通用占位、含糊表达和完全重复，不识别前置条件中的本次操作、写成“检查是否”的预期结果、冗长固定备注及同一来源文件的重复记录。
```

### 修改后原文

```text
六个字段按问题分工：用例描述写场景和测试意图；验证用例子项写唯一验证维度；前置条件只保留角色、初始业务状态和必要测试数据；操作步骤写本次具体动作和对象；预期结果直接写唯一可观察结果；备注只保留按文件合并的来源、稳定规则标识和必要边界。

精简以删除字段间重复、套话和非必要条件为准，不以机械删词、截断句子或统一字数为标准。校验器新增前置条件动作、检查式预期、冗长固定备注和重复来源检查；共用生成器把固定说明缩短为“未动态验证”。
```

### 验证与影响

1. `node --test scripts/testcase-design.test.mjs scripts/validate-testcase-json.test.mjs` 共 16 项通过、0 项失败。
2. 可读样例“验证场次结束后的评论入口 / 评论入口 / 用户、当前场次已结束 / 进入直播结束页 / 评论入口不显示”通过；把进入动作写入前置条件、把预期写成“检查是否”或使用旧固定长备注时被拦截。“确认按钮保持禁用”这类以控件名开头的合法结果不会被误拦。
3. 本次规则变化最早影响结构化用例设计和语言校验层；证据原子及业务规则内容不因文案精简自动改变，后续生成应重建设计、候选、历史比较和交付产物。
4. 本次没有修改项目需求、原型或业务证据，也没有使用机械后处理改写历史用例。
5. 本次没有改变任务输入、JSON 根结构、字段或 Excel 接口，因此不修改 `Cem Kaner.txt`。

### 回退方法

1. 将 `AGENTS.md` 第 4.1、10 节恢复为本记录“修改前原文”描述的字段职责，删除机器规则区的 `preconditionActionPatterns`、`expectedCheckPatterns` 和 `noteExact`。
2. 从 `scripts/testcase-design.mjs` 删除前置条件动作检查，并将固定备注恢复为“说明：依据当前资料静态设计，未动态验证产品行为”。
3. 从 `scripts/validate-testcase-json.mjs` 删除本次新增的前置条件动作、检查式预期、字段重复、冗长固定备注和重复来源检查，并删除对应回归检查。
4. 不删除或改写本条历史记录；实际回退时追加一条引用 `RCL-0045` 的新记录。

## RCL-0046 从原始证据独立发现规则和测试场景

- 修改时间：2026-09-10 18:17 CST。
- 用户要求：修复先写用例、再反向建立覆盖分母的机制；独立发现后映射已有用例，只生成真实缺口。禁止最低数量、关键词未命中即不适用及虚假覆盖声明。
- 涉及规则：`AGENTS.md` 快速顺序、INPUT-01、新增 DISC-01、标准工作流；`Cem Kaner.txt` 输入接口与执行入口。
- 修改前哈希：AGENTS.md `4d2ecea1deb81bf7711ec54e9ba3337f853adbd3c0e001e1e2d19ba7e992097c`；Cem Kaner.txt `42c3a7e2b7fcc3d3a56263fc76c0853e618d20cf9f4063ddbdc106156ce365c1`。
- 修改后哈希：AGENTS.md `89bb3a8e2d7c74fd36ef05c512383efaca1cc9f7b959cc2aae4c0b197b669f52`；Cem Kaner.txt `96b37048c7cc3ea04e0c6b86d8bec9371540a003872d62fa034ad9e9f11a1916`。

### 修改前后原文与回退依据

修改前顺序：`当前证据 -> 当前规则清单 -> 当前候选 JSON -> 历史比较 -> 当前证据缺口复核 -> 最终 JSON/Excel`。模板要求：`本文件不接收历史用例或历史比较结果。外层工作流在候选形成后生成 historical-case-comparison.json`。

修改后顺序：`原始证据 -> 独立规则库 -> 条件/状态/边界/异常/结果 -> 独立场景库 -> 已有用例映射 -> 真实缺口 -> 缺失用例 -> 覆盖验证 -> 质量门禁`。模板新增独立场景库和既有覆盖映射两个输入；用例 JSON 与 Excel 字段不变。

两份规则文件的完整修改前原文保存在 `work/liveshow-user-live-discovery-260910-005/governance-before/`。回退时仅恢复本次顺序、INPUT-01 和 DISC-01 相关段落，不覆盖用户其他修改；追加引用 RCL-0046 的回退记录。禁止恢复旧的虚假覆盖声明后继续使用本次覆盖结果。

### 实现与验证

- 新增 `scripts/evidence-discovery.mjs`、`scripts/liveshow-evidence.mjs`、`scripts/scenario-design.mjs`、`scripts/build-live-discovery.mjs`、`scripts/validate-discovery.mjs` 及来源变更/错误覆盖回归检查。
- 两项共享输入/交付校验器支持 Schema 3.0，校验原文重提取、规则/场景重演、覆盖条件和断言；保留旧 Schema 校验兼容。旧 work 生成脚本仅作历史复现，不作为发现入口。
- 共用依赖展开修正共享脚本中页面相对导航的解析。复用原工作簿生成器，增加任务路径参数及导出前校验，继续验证全表网格、公式和 JSON 单元格一致性。
- 最早失效层为证据提取层；旧缓存四个下游层移动到本次任务的 `cache-quarantine/`，不删除历史任务或输出。当前未完成语义解释的条款和实现分支单独列为局部复核，不发布为已完成语义缓存。
- 回归包含无既有用例时改变排序名额/字段、改变输入范围、自动提取邀请状态及关系清理，以及错误条件、错误断言、无依据不适用被拒绝。最终实测记录位于本次任务报告。
- 最终复核补充维度和元素关联重算，拒绝把无关的已覆盖场景挂为证明；每条交付用例须有场景证明，或为经当前来源复核且内容不变的历史用例。增加错挂维度、错挂元素、插入无来源用例三项产物篡改验证，全部重算哈希仍拒绝通过。
- 通过“声明真实性”检查不等于全量业务覆盖完成；规则数、场景数、用例数、未建模条款及未覆盖场景分开报告。

### RCL-0046 实现修订补记：Schema 3.1（2026-09-10）

- 用户授权：修复“原文待建模、场景未证明、元素未绑定”问题并重新生成直播模块用例。本轮修改生成器实现，未修改 AGENTS.md 或 Cem Kaner.txt 的治理正文。
- 修正多列表格原文拆分、引用文案中的时间词、并列动作主语、虚拟视图读取、物理控件重复归属及查看对象误作执行角色；HTML 使用 Python 标准库解析，动态模板单独记录。
- 补充当前原文参数驱动的长度、容量、选择保存/取消、时间限制、数据过滤、权限决策表及处置状态设计。等价设计合并时同步合并直接来源并重新验证，不以追加场景编号替代覆盖证明。
- 覆盖判断保留页面、角色、条件、动作、数值及否定；验证未完成台账和统计不得被删减或篡改。没有完整模型的业务约束保持生成待复核，不因命中关键词而计为完成。
- 本轮正式交付 252 条用例、4 项需求待确认；新增 38 条，6 条原用例仅更新来源追溯。当前仍有 261 条待建模原文和 609 个无覆盖证明的场景记录，不能宣称根本覆盖问题已全部解决。
- 本次证据、输入哈希、独立发现基线、回归结果及覆盖说明位于 `work/liveshow-user-live-discovery-260910-006/`。保留旧输出和旧任务快照；若退回旧实现，必须重新生成其对应版本的发现基线，不能混用 Schema 3.1 的覆盖声明。

## RCL-0047 采用业务对象状态机串联三端

- 修改时间：2026-09-11 15:50:30 CST。
- 用户要求：将用户 App、公会 App、管理后台三端用例生成流程改为方案 2，即先按共同业务对象建立状态转换和跨端流程，再投影为各端场景；不得从已有用例反推覆盖。
- 涉及规则：`AGENTS.md` 快速顺序、文件职责、`INPUT-01`、`DISC-01`、新增 `STATE-01`、标准工作流、跨端设计、去重和交付门禁；`Cem Kaner.txt` 新增状态转换基线输入并调整执行顺序。
- 配套实现：`scripts/scope-scenario-design.mjs`、`scripts/scope-scenario-design.test.mjs`、`scripts/validate-discovery.mjs`、`scripts/validate-generation-input.mjs`、`work/liveshow-three-end-all-260911-001/build-three-end.mjs`。
- 最早影响层：归一化规则层。原始证据及证据原子在来源哈希和 Schema 兼容时可以复用；状态转换、场景、历史映射、候选和交付层必须向下重建。
- 修改前 SHA-256：`AGENTS.md` 为 `89bb3a8e2d7c74fd36ef05c512383efaca1cc9f7b959cc2aae4c0b197b669f52`；`Cem Kaner.txt` 为 `96b37048c7cc3ea04e0c6b86d8bec9371540a003872d62fa034ad9e9f11a1916`。
- 修改后 SHA-256：`AGENTS.md` 为 `3b16a93fc5c4d1bbdb24e6c47bac02fe2c238217361b6314c69b66f8afe41c33`；`Cem Kaner.txt` 为 `0edaeb495dbc8f7c36854d69eb0ca71ba63f74421a051d64e3d7407019f6e75e`。

### 修改前原文

```text
固定流程以“独立规则库 -> 独立场景库 -> 已有用例映射 -> 真实缺口”为核心。三端关系只在场景和用例层通过相似页面、动作、结果及流程编号表达；没有独立的共同业务对象、来源状态、目标状态、跨端连续性和闭环真实性门禁。

Cem Kaner.txt 只接收独立场景库和既有覆盖映射，没有两端及以上任务必填的状态转换基线输入。
```

### 修改后原文

```text
固定流程改为“独立规则库 -> 业务对象状态转换基线 -> 目标端场景投影 -> 已有用例映射 -> 真实缺口”。

STATE-01 要求先按共同业务对象建立“来源状态 -> 触发动作 -> 目标状态”，记录执行角色、操作端、观察端、证据、稳定状态转换标识和流程编号；完成状态连续性检查后，才投影用户 App、公会 App、管理后台场景。缺少来源状态或目标状态的内容进入待补转换，不得补写确定结果；只有状态链闭合且各操作端、观察端均有场景投影时才能声明跨端闭环。

state-transition-baseline.json 是由当前规则清单派生的输入隔离产物，禁止独立定义业务事实。两端及以上任务使用 Schema 3.2，并在发现基线、覆盖统计和最终门禁中校验其哈希、状态连续性、三端投影和闭环数量。已有用例仅在该基线及场景库固定后参与覆盖映射。
```

### 验证与影响

1. `node --test scripts/scope-scenario-design.test.mjs scripts/evidence-discovery.test.mjs scripts/validate-generation-input.test.mjs` 共 14 项：13 项通过、0 项失败、1 项因旧发现产物项目指纹变化而按预期跳过。新增回归验证三端连续状态链可形成闭环、伪造中间状态会被拒绝、缺少公会 App 投影会被拒绝。
2. 语法检查和 `git diff --check` 通过。
3. 对当前三端正式证据进行了只读试算：识别 12 条已确认转换、122 条待补转换；待补项均缺少明确来源状态，因此没有把它们猜成跨端闭环。当前结果为 0 个已确认跨端闭环，这表示证据仍需补充或确认，不表示状态机门禁失效。
4. 本次只修改规则和生成流程，没有生成或修改测试用例 JSON、Excel，也没有用已有用例建立业务规则、状态或覆盖分母。

### 回退方法

1. 将 `AGENTS.md` 恢复为本记录“修改前原文”描述的两级独立发现流程，删除 `STATE-01`、状态转换文件职责及各处状态机门禁。
2. 从 `Cem Kaner.txt` 删除状态转换基线输入，将执行顺序恢复为规则库后直接建立独立场景库。
3. 回退配套实现中的状态转换构建、三端投影、Schema 3.2 校验和回归检查；重新生成受影响的状态转换及其全部下游产物，禁止混用 Schema 3.2 的闭环声明。
4. 不删除或改写本条历史记录；实际回退时追加一条引用 `RCL-0047` 的新记录。

## RCL-0048 衔接 MainBasis 更新与两文档生成输入

- 修改时间：2026-09-14 10:15 CST。
- 用户要求：将上游变化检查、需求同步、MainBasis 更新和只从两份文档生成三端用例衔接为默认流程；本次不生成用例。
- 涉及文件：AGENTS.md、Cem Kaner.txt；配套更新全局证据扫描指令、原型与需求清单同步指令、测试用例生成文件职责说明、项目来源策略和共用校验器，新增 scripts/mainbasis.mjs 及其回归检查。
- 修改位置：快速流程、INPUT-01、DISC-01、BASIS-01、标准流程、交付检查及日志职责；模板增加 MainBasis 版本和两份文档输入。
- 阶段边界：上游仍采用系统概要优先的 prototype-primary；生成阶段只使用项目 MainBasis 两文档。风险输入不定义确定预期。首次缺少同步记录不能认证旧文档为最新，后续上游或文档变化返回同步。三端共享需求版本，保留对象状态转换和独立场景设计。
- 最早影响层：本项目生成证据入口和来源阶段切换，下一次首次衔接须全量核对并建立需求版本；不把历史语义缓存或旧场景直接作为当前依据。已交付工作簿和历史任务不改写。
- 实现边界：脚本验证文件版本、同步记录及输入角色，生成执行者负责语义比较和文档更新；没有新增一个自动猜测业务规则的解析器，没有把版本通过等同全部覆盖。
- 验证：node --test scripts/mainbasis.test.mjs scripts/validate-generation-input.test.mjs 通过；覆盖首次无基线、同步中变化、上游修改/新增/重命名、单文档修改、旧基线、风险冒充规则、原型混入业务输入及记录篡改。实际项目 verify 按预期提示首次缺少同步基线，未发布虚假的最新版本。

### 修改前后原文

以下是本次两份规则文件的精确统一差异；减号行为修改前原文，加号行为修改后原文，其余为定位上下文。

```diff
diff --git a/AGENTS.md b/AGENTS.md
index cc5d9e0..56f7bd0 100644
--- a/AGENTS.md
+++ b/AGENTS.md
@@ -2,7 +2,7 @@

 ## 0. 快速执行区

-本节用于快速定位，不替代后文细则。执行时按“范围 -> 接入 -> 证据 -> 同步 -> 输入隔离 -> 独立规则库 -> 业务对象状态转换基线 -> 目标端场景投影 -> 已有用例映射 -> 缺口生成 -> 覆盖验证 -> 最终校验 -> Excel”顺序处理；同一要求只在对应稳定规则编号下定义，其他章节只引用编号。
+本节用于快速定位，不替代后文细则。执行时按“范围 -> 接入 -> 证据 -> 同步 -> MainBasis 更新与版本核对（已配置项目） -> 输入隔离 -> 独立规则库 -> 业务对象状态转换基线 -> 目标端场景投影 -> 已有用例映射 -> 缺口生成 -> 覆盖验证 -> 最终校验 -> Excel”顺序处理；同一要求只在对应稳定规则编号下定义，其他章节只引用编号。

 ### 0.1 规则用语

@@ -24,6 +24,7 @@
 | `GAP-01` | 规则缺失复核 | 不得声称缺失 |
 | `ONBOARD-01` | 新项目首次接入和项目隔离 | 停止正式生成 |
 | `INPUT-01` | 输入角色、规则清单、历史隔离和哈希 | 局部隔离或批次阻断 |
+| `BASIS-01` | 上游同步、MainBasis 两文档更新和用例输入版本衔接 | 返回需求同步；不得使用过期基线 |
 | `DISC-01` | 从原始证据独立发现规则、场景和真实覆盖缺口 | 未解释内容局部复核；虚假覆盖声明不得通过 |
 | `STATE-01` | 业务对象生命周期、状态转换、三端投影和流程闭环 | 缺失转换局部待确认；虚假闭环不得通过 |
 | `SYNC-01` | 原型与派生需求同步 | 进入需求待确认或批次阻断 |
@@ -39,7 +40,7 @@

 1. 按 `SCOPE-01` 确认范围、目标端、角色、交付格式和视觉测试要求。
 2. 按 `ONBOARD-01` 确认项目身份，再按 `EVID-01` 读取来源策略。
-3. 按 `SCAN-01` 选择扫描模式并建立证据闭环；`prototype-primary` 项目同时执行 `SYNC-01`。
+3. 按 `SCAN-01` 选择扫描模式并建立证据闭环；`prototype-primary` 项目同时执行 `SYNC-01`。配置 MainBasis 的项目先执行 `BASIS-01` 的版本检查，有变化时完成需求更新，再进入用例阶段。
 4. 按 `GAP-01` 完成缺失复核，再按 `INPUT-01` 建立输入清单和当前规则清单。
 5. 按 `STATE-01` 以共同业务对象建立来源状态、动作、目标状态和观察端的状态转换基线；跨端流程先完成状态连续性检查。
 6. 按 `RESULT-01`、`ROLE-01` 和测试设计章节，将状态转换及非状态规则投影为各目标端场景；不满足 `CASE-01` 的内容按 `GATE-01` 进入候选层或用例层隔离。
@@ -218,7 +219,7 @@

 每次生成、增量修订或重审测试用例时，必须在本次任务 `work/` 目录先建立 `generation-input-manifest.json`。当前规则清单完成后、候选用例生成前执行 `scripts/validate-generation-input.mjs --phase pre-generate`；历史比较和最终 JSON 完成后执行 `--phase final`。清单是各阶段生成输入的完整白名单；未登记文件不得被相应脚本读取后用于业务规则、步骤或预期结果。

-1. **固定项目与输入角色**：清单必须记录本次唯一的 `项目目录`；除本次任务内允许的用户确认记录和已确认决策摘录外，`当前业务证据` 必须位于该项目目录，不得读取相邻项目作为当前规则来源。每个输入只能标记为 `当前业务证据`、`本次派生产物`、`历史参照`、`样式参照` 或 `执行工具`。清单至少记录 `路径、SHA-256、角色、内容类型、允许定义业务规则`；同一路径不得登记多个角色。
+1. **固定项目与输入角色**：清单必须记录本次唯一的 `项目目录`；除本次任务内允许的用户确认记录和已确认决策摘录外，`当前业务证据` 必须位于该项目目录，不得读取相邻项目作为当前规则来源。每个输入只能标记为 `当前业务证据`、`风险与缺口`、`本次派生产物`、`历史参照`、`样式参照` 或 `执行工具`。`风险与缺口` 禁止定义正式业务规则。清单至少记录 `路径、SHA-256、角色、内容类型、允许定义业务规则`；同一路径不得登记多个角色。MainBasis 项目的阶段与角色边界另按 `BASIS-01` 执行。
 2. **限制规则来源**：只有 `当前业务证据` 可以作为最终业务事实来源。`本次派生产物` 中仅 `business-rule-catalog.json` 可以承载由当前证据归一化后的规则；其每条规则必须反向引用清单中哈希一致的当前业务证据。`state-transition-baseline.json` 只能组合规则清单中已有的业务对象、状态、动作、角色、端和结果，不得独立定义业务事实；每项转换必须反向引用规则标识和当前证据。扫描结果、同步结果和质量报告本身不能替代原始证据。
 3. **隔离历史输入**：旧 JSON、旧 Excel、旧任务目录、历史扫描结果和历史缓存统一标记为 `历史参照`，`允许定义业务规则` 固定为 `false`。独立规则库和场景库固定前不得读取已有用例内容；固定后可按 `DISC-01` 映射、复核并保留当前证据仍支持的既有用例。既有用例不能反向定义规则、场景或覆盖分母；历史待确认结论不能直接视为当前需求缺失。
 4. **限制 work 目录资格**：`work/` 下的文件默认不是当前业务证据。仅本次任务保存的当前用户明确确认记录，或从正式决策载体中提取并完成当前适用性复核的 `confirmed-decision-extract.json`，可以标记为 `当前业务证据`；其余文件只能是本次派生产物、历史参照、样式参照或执行工具。决策摘录必须记录原文件路径和哈希、精确位置、已确认结论、适用范围、当前适用性和判断依据；原文件仍作为历史参照，测试用例区域不得随摘录进入当前证据。其他项目和其他任务目录中的文件不得标记为当前业务证据。
@@ -230,13 +231,27 @@

 ### 2.6 [DISC-01] 规则和场景独立发现

+已配置 MainBasis 的项目按 `BASIS-01` 划分上下游阶段：本节所指当前源证据在用例阶段只指已发布的两份 MainBasis 文档；全局原型和关联证据的发现、补漏及缺失复核在需求更新阶段完成。
+
 1. **独立输入与三级模型**：发现阶段只接收当前原型、批注、需求、页面及递归关联证据，不接收已有用例或预先编写的场景清单。先保存原始证据单元及定位，再提取业务规则；对存在生命周期的共同业务对象，按 `STATE-01` 建立状态转换；最后将转换和非状态规则投影为目标端场景。业务规则允许包含多个条件和结果分支；第 2.5 节第 5 项的单动作、单结果约束施加于状态转换投影、场景和正式用例，不得据此退回“一规则一用例”。
 2. **独立状态与场景库**：保存 `business-rule-catalog.json`、`state-transition-baseline.json`、`test-scenario-library.json` 和发现基线。规则、转换和场景标识来自语义条件，不依赖已有用例数或 `add(...)` 调用。只有会改变状态或可观察结果的有效条件才展开，不做全维度笛卡尔积。不支持解析的条款或缺失状态记录为“生成待复核”或“待补转换”，不得算作已完成建模的场景，也不得伪装成需求待确认。
 3. **先映射再补缺口**：固定场景及覆盖契约后，按规则、页面、角色、操作、状态、分支映射已有用例，逐项记录已覆盖、未覆盖、待确认或不适用。来源仍有效不等于场景已覆盖；只有必要条件、实际动作和明确断言均满足场景契约才能声明已覆盖。随后只对未覆盖且具备执行依据的场景生成用例；保留既有用例的源证据复核和新旧编号对应。
 4. **维度与元素覆盖**：15 维判断以结构化条件、边界、状态和结果为依据，不能通过用例文本关键词命中数量计算覆盖率。不适用须引用明确依据；没有建立适用性或未完成建模时记录未覆盖/生成待复核。每个页面元素单独绑定其状态、操作、结果与场景；页面名称已映射、按钮数或字段数一致均不足以声明元素已覆盖。
 5. **证据门禁**：重新读取当前源文件，核对哈希及原文，重演完整条款提取和场景发现，再核对每个已覆盖声明的条件、动作、断言、角色和页面来源。只有编号、关键词、格式或关系完整不能通过。必须用来源变更和伪造覆盖的回归样例证明门禁会拒绝错误条件、错误结果和无依据不适用。设计覆盖与产品执行结果分开；未执行的用例保持“未测”。
 6. **局部隔离与真实统计**：原始条款数、已结构化规则数、场景数、已覆盖数、未覆盖数、待建模条款及历史待复核问题分别报告。只放行可用部分时必须披露其余缺口，不能把“声明真实性校验通过”写成“全部语义覆盖完成”。未关闭的局部缺口不得删除，也不阻断其他明确分支。
-7. **当前直播执行入口**：单端直播模块使用 `scripts/build-live-discovery.mjs discover` 固定独立模型，再使用同脚本的 `generate <既有用例JSON> <既有规则清单>` 进行映射和补缺；两端及以上范围必须先建立共享状态转换基线，再按端投影场景。`work/` 下旧的固定 `add(...)` 生成脚本仅保留历史复现用途，不得作为新任务发现入口。Schema 3.2 由两项共用校验器转入 `scripts/validate-discovery.mjs`；旧 Schema 校验保留兼容，不代表允许旧发现策略。
+7. **当前执行入口**：已配置 MainBasis 的项目使用 `BASIS-01` 和同步指令第 3.9 节进入本次新任务，由生成执行者只从两份文档建立规则、共享状态和场景，复用结构化设计渲染及共用质量校验；不得调用仍直接读取原型的旧发现入口。未配置 MainBasis 的单端直播模块继续使用 `scripts/build-live-discovery.mjs discover` 和 `generate <既有用例JSON> <既有规则清单>`。两端及以上范围先建立共享状态转换基线，再按端投影。`work/` 下旧生成和直接导出脚本仅供历史追溯，不是新任务入口；旧任务中“直接生成/跳过复核”的授权不得跨任务继承为默认规则。Schema 3.2 继续由共用校验器转入 `scripts/validate-discovery.mjs`，但不得以 Schema 版本绕过 MainBasis 输入边界；手工复核的结构化设计可使用现有 Schema 1.0 接口，仍须独立保存并验证状态和场景基线。
+
+### 2.6.1 [BASIS-01] MainBasis 更新与生成衔接
+
+本节适用于来源策略中配置 `MainBasis` 的项目。用户只说“生成用例”时自动执行，无需重复要求同步；这是由生成执行者衔接的流程，不是后台定时任务或纯脚本语义转换。
+
+1. **先核对新鲜度**：按同步指令第 3.9 节执行版本检查。上游全项目文件（包括新增、删除、未跟踪文件）、流程规则和两份文档均核对内容哈希；不按文件日期判断。缺少同步基线视为首次衔接，禁止直接认证旧 MainBasis 为最新。上游或文档改变时自动回到扫描和同步，未变化且有效的证据按 `SCAN-01` 复用。
+2. **需求更新阶段**：保留项目 `prototype-primary` 策略；系统概要优先，审核通过的原型及明确批注补充系统概要未覆盖事项。执行 `SCAN-01`、`GAP-01`、`SYNC-01` 对比全部 context 文件，但只修改配置白名单中的派生清单。然后汇总更新项目 MainBasis 下配置的两份文档。这两份是单独的输出白名单，不能再当作上游来源参与自我证明；其他 context 文件的差异只记录，不擅自修改系统概要或正式证据。
+3. **保留业务与缺口**：统一需求文档包含有来源的规则、角色、入口、状态、端侧观察结果及稳定编号；未确认条目必须明确标记并关联风险问题。风险清单只说明缺口、冲突、影响和待决策事项，不能提供确定预期。保留既有编号和用户已确认结论；按适用性复核更新，不能覆盖或清空产品决策。来源缺失不能通过删除整个功能掩盖。
+4. **两文档一起发布**：完成真实语义对比和日志后，保存同步前后哈希、来源映射、隔离问题及复核说明，再发布 MainBasis 基线。脚本只验证版本与记录一致性，不代替业务判断。中途中断或只更新一份文档不得放行。需求基线发布不表示测试覆盖、Excel 检查或项目证据缓存已通过。
+5. **用例输入隔离**：生成阶段只有统一需求文档登记为 `当前业务证据/允许定义业务规则=true`；风险清单登记为 `风险与缺口/允许定义业务规则=false`，仅支持隔离规则与待确认问题。两文档内的来源链接仅用于追溯，不在用例阶段沿链接读取原型补写步骤或预期。需要补证据时返回需求更新阶段，更新文档和版本后重建受影响设计。当前用户新确认的业务结论也先落入这两份文档，再进入正式用例。本条是 `INPUT-01`、`ROLE-01`、`GAP-01` 的阶段限定，不降低可执行性和缺失复核要求。
+6. **绑定同一版本**：三端共享同一个 MainBasis 基线，分别建立端侧输入清单，记录 `MainBasis基线.路径` 和 `SHA-256`。规则和状态转换从当前文档独立建立后才读取历史用例；不得沿用旧任务的规则/场景分母。候选前、最终 JSON、Excel 导出及缓存发布通过共用校验重验版本；任一变化按影响范围回到同步，不只重新导出旧 JSON。
+7. **不修改历史交付**：旧用例、工作簿、历史任务和旧基线保留追溯。新一轮输出使用新编号。历史脚本里写死的旧路径不得替换为当前文档后冒充重新设计；没有完成本节时，不得声称已按最新需求生成。

 ### 2.7 [STATE-01] 业务对象状态转换与三端投影

@@ -256,6 +271,7 @@
 | --- | --- | --- | --- |
 | 1. 定范围 | 执行 `SCOPE-01`、`ONBOARD-01`、`EVID-01` | 范围和来源策略 | 输入完整性失败时批次阻断 |
 | 2. 建证据 | 执行 `SCAN-01`、`GAP-01`，适用时执行 `SYNC-01` | 扫描、同步和缺失复核记录 | 局部隔离；无法限定影响时批次阻断 |
+| 2a. 更新生成依据 | 已配置项目执行 `BASIS-01` | 两份 MainBasis 文档、同步记录、需求版本基线 | 返回需求更新，不使用旧输入 |
 | 3. 隔离输入 | 执行 `INPUT-01` 的清单、规则目录和 `pre-generate` 校验 | `generation-input-manifest.json`、`business-rule-catalog.json` | 局部规则隔离；输入失效时批次阻断 |
 | 4. 建状态转换 | 执行 `STATE-01`，先按共同业务对象建立生命周期，再检查跨端连续性 | `state-transition-baseline.json` | 缺失转换局部待复核或待确认；虚假闭环阻断声明 |
 | 5. 独立发现 | 执行 `DISC-01`、`RESULT-01`、`ROLE-01` 和第 5 至 9 节，将规则和转换投影为目标端场景 | 独立规则库、场景库、发现基线 | 局部待复核或待确认 |
@@ -632,6 +648,7 @@ Excel 的“需求待确认”工作表固定使用以下 20 列：
 | 项目与范围 | `SCOPE-01`、`ONBOARD-01`、`EVID-01` | 项目身份、范围、目标端、角色和来源策略明确；未跨项目或扩大范围 |
 | 扫描与同步 | `SCAN-01`、`GAP-01`、`SYNC-01` | 扫描、缓存、依赖、缺失复核和适用的同步记录完整；证据基线未变化 |
 | 输入与历史隔离 | `INPUT-01` | 输入清单在两个阶段校验通过；规则、候选、历史比较和最终 JSON 的来源、顺序及哈希一致 |
+| MainBasis 衔接 | `BASIS-01` | 已配置项目的上游、两文档及生成输入版本一致；风险未作为正式预期 |
 | 状态转换与跨端闭环 | `STATE-01` | 共同业务对象、状态、动作、角色和端有依据；状态连续且各操作端、观察端均有场景去向；未虚报闭环 |
 | 用例可执行性 | `CASE-01`、`ROLE-01`、`RESULT-01`、第 5 至 9 节 | 每条用例只有一个角色、一条路径和一个结果；入口、动作、条件及观察点均有依据 |
 | 语言与去重 | `LANG-01`、`DEDUP-01` | 没有含糊词、通用占位词、字段整句重复、条件子集重复或未关闭的人工复核 |
@@ -664,4 +681,4 @@ Excel 的“需求待确认”工作表固定使用以下 20 列：
 - 执行回退也属于一次新改动，必须追加新的回退记录，并引用被回退的变更编号。
 - 未同步写入日志的规则文件改动视为未完成，不得交付。

-`需求清单变更日志.md` 与本节的规则日志职责不同：前者只记录项目 `context/` 需求清单及来源策略的同步变化，后者只记录规则文件变化。两类日志均在根目录按各自职责保持唯一，不得按项目、日期或任务另建同类日志。
+`需求清单变更日志.md` 与本节的规则日志职责不同：前者记录项目 `context/`、MainBasis 需求文档及来源策略的同步变化，后者记录规则文件变化。两类日志均在根目录按各自职责保持唯一，不得按项目、日期或任务另建同类日志。
diff --git a/Cem Kaner.txt b/Cem Kaner.txt
index 24b1166..dbaca6d 100644
--- a/Cem Kaner.txt
+++ b/Cem Kaner.txt
@@ -28,6 +28,11 @@
 ### 项目全局文件清单与证据扫描结果
 <<GLOBAL_EVIDENCE_SCAN_RESULT_JSON>>

+### MainBasis 需求版本（项目配置 MainBasis 时必填）
+<<MAINBASIS_BASELINE_JSON>>
+<<MAINBASIS_FORMAL_REQUIREMENTS_MD>>
+<<MAINBASIS_PENDING_REQUIREMENTS_MD>>
+
 ### 生成输入角色清单
 <<GENERATION_INPUT_MANIFEST_JSON>>

@@ -41,14 +46,14 @@
 <<INDEPENDENT_SCENARIO_LIBRARY_JSON>>
 <<EXISTING_CASE_COVERAGE_MAPPING_JSON>>

-### 当前需求、原型、接口、代码或用户补充证据
+### 当前证据（MainBasis 项目只传上述两份文档；其他项目按来源策略传入）
 <<CURRENT_SOURCE_EVIDENCE_JSON>>

 ## 执行入口

 1. 确认项目名称、端名、模块名称和输入材料均属于当前任务；缺失关键输入时停止生成，不自行补造。
 2. 完整读取当前 `AGENTS.md`，并按第 3 节“标准工作流”执行。
-3. 根据项目来源策略调用对应的全局证据扫描和原型同步指令；存在阻塞项时停止生成。
+3. 根据项目来源策略调用对应的全局证据扫描和原型同步指令；已配置 MainBasis 时先执行 `AGENTS.md` 的 `BASIS-01` 和同步指令第 3.9 节，使用当前两文档及已核对版本作为生成输入。扫描/同步记录只证明过程，不参与定义业务预期。问题按 `GATE-01` 处理。
 4. 执行 `AGENTS.md` 的 `DISC-01`：独立建立规则库；两端及以上范围再执行 `STATE-01`，先按共同业务对象建立状态转换基线，检查状态连续性后投影各目标端场景。固定发现基线并使用 `scripts/validate-generation-input.mjs --phase pre-generate` 校验后，才映射已有用例。
 5. 按 `AGENTS.md` 执行目标端检查、结果完整性扫描、单结果拆分和语义去重后，将本文件输出保存为本次任务的 `current-testcase-candidate.json`，此时不得作为最终交付。
 6. 本文件仅使用状态转换基线及独立场景库中的真实未覆盖项生成缺失用例；外层流程在状态转换和场景固定后映射历史并保存 `historical-case-comparison.json`。既有用例只能经当前来源复核后保留，不得反向定义规则、状态或场景。未解释条款、待补转换与业务待确认问题分开记录。
```

### 回退方法

按上述差异逐段反向恢复本次规则和模板变化，同时回退对应配套指令、来源策略 MainBasis 配置和两项共用校验器的 MainBasis 调用。新增的 mainbasis 脚本与检查仅在没有后续依赖时移除；本次没有创建项目有效需求基线，没有改变历史工作簿。保留用户已完成的 MainBasis 目录迁移和其全部文档；不得用整仓回退覆盖其他修改。实际回退另追加引用 RCL-0048 的记录。

## RCL-0049｜Luma Live 全新生成与归档隔离

- 时间：2026-09-14；本次明确授权：归档历史生成数据，保留工具、接入记录和原始资料；不读取旧用例、不做历史比较，当前上游重建 MainBasis 后全新生成三端。
- 修改前：BASIS-01 结束于第 7 项；INPUT-01、DISC-01 及模板仍要求候选后历史比较；模板没有 HISTORY_POLICY 输入。
- 修改后：BASIS-01 新增第 8 项“全新生成模式”，以 `用例生成模式=全新生成`、`历史比较=false`、清单 `历史策略=不读取不比较` 覆盖全流程的历史映射和比较要求。Cem Kaner.txt 新增 HISTORY_POLICY 输入及该模式引用。扫描和同步指令引用同一例外，不创建空比较报告。禁止从归档、旧规则、旧场景或旧缓存找线索。
- 配套：共用输入校验拒绝历史角色、归档路径和历史比较结果；候选与最终内容、当前证据和需求版本仍校验。旧原型直读入口在已配置 MainBasis 时拒绝运行。全新模式不发布用例历史缓存。通用 Excel 工具迁出旧任务目录并移除写死的旧任务参数。
- 补充隔离：全新模式不得把其他 `work/` 任务的设计脚本伪装成“执行工具”读取；仅允许本次任务目录族的设计和根 `scripts/` 通用工具。该检查在读取所申报旧脚本内容之前执行，并有拒绝用例覆盖。
- 归档：`archive/Luma-Live/260914-001/archive-manifest.json` 记录 11 个目录、433 个文件的原路径、归档路径、大小及哈希；另保存三份派生需求原文及独立备份清单。未删除原始原型、系统概要、项目接入记录和其他项目资料。
- 验证：运行 `node --test scripts/mainbasis.test.mjs scripts/validate-generation-input.test.mjs`，检查全新模式拒绝历史输入、上游与文档变化使基线失效，保留非全新项目原有校验。
- 回退：仅移除本次 BASIS-01 第 8 项及 HISTORY_POLICY 新段，反向恢复对应模式分支；保留 RCL-0048 的 MainBasis 衔接。归档按清单逐项恢复，原路径存在时先比对，不覆盖新生成文件；不做整仓回退。

## RCL-0050｜完整需求传递与全需求覆盖门禁

- 修改时间：2026-09-14。
- 用户要求：前后两段一起修改，记住当前上游 → 核对 context → 核对 MainBasis → 独立规则/状态/三端场景 → 全新用例 → 从全部需求反查覆盖；继续禁用旧用例与历史比较。本次只修改流程和共用校验，不重新生成业务用例。
- 涉及文件：AGENTS.md、Cem Kaner.txt、全局证据扫描指令.md、原型与需求清单同步指令.md、流程追溯文件说明.md；scripts/mainbasis.mjs、requirement-traceability.mjs、validate-generation-input.mjs、validate-testcase-delivery.mjs、validate-discovery.mjs 及相应合成回归检查。
- 规则位置：快速执行顺序、TRACE-01 新章节、交付检查表；模板的追溯输入与执行入口；扫描的图片读取和文件排除；同步第 3.9 节。
- 最早失效层：需求来源与 context/MainBasis 传递核对层。新版基线为 2.0，旧 1.0 基线返回重新同步；不能只替换哈希继续使用旧生成结果。
- 修改前：只有文件哈希、非空来源映射及已设计场景覆盖检查；可缺少 context 全文回读、来源处理去向或场景分支而不被机器检查发现。
- 修改后：共用入口按 TRACE-01 强制两段全文双向对照和 context checkpoint；覆盖从全文单元初始化，实际场景契约、状态投影和候选逐项匹配；部分交付保留缺口，拒绝虚假完整声明。不会声称脚本能够自动理解全部业务语义。
- 验证：node --test scripts/requirement-traceability.test.mjs scripts/mainbasis.test.mjs scripts/validate-generation-input.test.mjs；5 项检查全部通过。覆盖来源遗漏、无来源目标、图片未查看、缺 checkpoint、变化失效、历史隔离、场景契约不符、漏状态投影、部分交付和虚假完整声明。当前 Luma Live 旧基线 verify 按预期要求重新核对，没有更新其业务正文或旧 Excel。
- 实施前文件副本：work/flow-rules-260914-001/before/；只用于本次规则差异与精确回退，不作为业务输入。

### 修改前后原文

以下差异中的减号行为修改前原文，加号行为修改后原文；未涉及的原文保留。新增 TRACE-01 及模板输入完整列出。

```diff
diff --git a/work/flow-rules-260914-001/before/AGENTS.md b/AGENTS.md
index f260c02..093023f 100644
--- a/work/flow-rules-260914-001/before/AGENTS.md
+++ b/AGENTS.md
@@ -26,6 +26,7 @@
 | `INPUT-01` | 输入角色、规则清单、历史隔离和哈希 | 局部隔离或批次阻断 |
 | `BASIS-01` | 上游同步、MainBasis 两文档更新和用例输入版本衔接 | 返回需求同步；不得使用过期基线 |
 | `DISC-01` | 从原始证据独立发现规则、场景和真实覆盖缺口 | 未解释内容局部复核；虚假覆盖声明不得通过 |
+| `TRACE-01` | 上游、context、MainBasis 全量对照及逐需求分支覆盖 | 保留漏项；部分产物不得冒充完整交付 |
 | `STATE-01` | 业务对象生命周期、状态转换、三端投影和流程闭环 | 缺失转换局部待确认；虚假闭环不得通过 |
 | `SYNC-01` | 原型与派生需求同步 | 进入需求待确认或批次阻断 |
 | `RESULT-01` | 业务结果完整性和过程态覆盖 | 补充覆盖或待确认 |
@@ -40,12 +41,12 @@

 1. 按 `SCOPE-01` 确认范围、目标端、角色、交付格式和视觉测试要求。
 2. 按 `ONBOARD-01` 确认项目身份，再按 `EVID-01` 读取来源策略。
-3. 按 `SCAN-01` 选择扫描模式并建立证据闭环；`prototype-primary` 项目同时执行 `SYNC-01`。配置 MainBasis 的项目先执行 `BASIS-01` 的版本检查，有变化时完成需求更新，再进入用例阶段。
+3. 按 `SCAN-01` 选择扫描模式并建立证据闭环；`prototype-primary` 项目同时执行 `SYNC-01`。MainBasis 项目执行 `BASIS-01` 版本检查和 `TRACE-01` 的 context 回读、两段需求传递核对后才进入用例阶段。
 4. 按 `GAP-01` 完成缺失复核，再按 `INPUT-01` 建立输入清单和当前规则清单。
 5. 按 `STATE-01` 以共同业务对象建立来源状态、动作、目标状态和观察端的状态转换基线；跨端流程先完成状态连续性检查。
 6. 按 `RESULT-01`、`ROLE-01` 和测试设计章节，将状态转换及非状态规则投影为各目标端场景；不满足 `CASE-01` 的内容按 `GATE-01` 进入候选层或用例层隔离。
 7. 按 `DISC-01` 固定独立场景库后才读取已有用例，先映射真实覆盖，再按 `LANG-01` 和 `DEDUP-01` 生成缺口用例。
-8. 输入、规则、状态转换、候选、历史比较和证据哈希通过后，将符合门槛的用例与已隔离的“需求待确认”生成最终 JSON 与 Excel；不得要求所有覆盖点都转成正式用例。
+8. 输入、规则、状态转换、候选、适用的历史比较和证据哈希通过后，按 `TRACE-01` 从全部 MainBasis 需求反查覆盖，再生成最终 JSON 与 Excel；不得要求所有覆盖点都转成正式用例，也不得把部分交付说成完整交付。
 9. 按 `DELIVERY-01` 完成交付检查；规则文件有修改时同时执行 `CHANGE-01`。

 ### 0.4 [GATE-01] 用例生成质量校验与阻断机制
@@ -254,6 +255,18 @@
 7. **不修改历史交付**：旧用例、工作簿、历史任务和旧基线保留追溯。新一轮输出使用新编号。历史脚本里写死的旧路径不得替换为当前文档后冒充重新设计；没有完成本节时，不得声称已按最新需求生成。
 8. **全新生成模式**：项目配置 `用例生成模式=全新生成`、`历史比较=false` 时，禁止读取旧用例、旧规则库、旧场景库、旧扫描语义结果和归档业务内容，也不读取旧用例寻找检索词。生成顺序为当前上游 -> 重新建立或核对当前 MainBasis -> 独立规则 -> 共享状态 -> 三端场景 -> 全新用例 -> 覆盖验证 -> JSON/Excel；本节覆盖快速流程及 INPUT-01、DISC-01、SYNC-01、DEDUP-01、DELIVERY-01 中的历史映射和比较要求。输入清单固定 `历史策略=不读取不比较`，不得登记历史参照或历史比较结果，不创建空比较报告冒充已比较。阶段耗时将 history-compare 记为“不适用：用户禁用”，旧例影响记为“不评估历史用例”；仍须保证候选与最终 JSON 一致并通过全部非历史质量门禁。该模式不发布用例历史缓存。通用工具允许复用，工具内不得包含旧用例业务设计或默认旧任务路径。原始资料和接入记录保留；历史目录仅可按用户指令恢复，不得自动回读。

+### 2.6.2 [TRACE-01] 两段需求传递与全需求覆盖
+
+本节是 MainBasis 项目的强制衔接门禁，不受生成清单 Schema 版本或“全部模块”标题影响；覆盖 BASIS-01 中简写的汇总步骤及旧实现中仅核验已登记场景的方式。固定链路为：当前上游 -> 同步并回读核对全部 context -> 生成并逐项核对 MainBasis -> 独立规则与共享状态 -> 三端场景与全新用例 -> 从全部需求反查覆盖。继续执行 BASIS-01 的历史禁用和输入隔离。机器字段与命令见 `流程追溯文件说明.md`，该文件仅定义接口。
+
+1. **源清单独立建立**：先从当前实际文件建立来源单元与定位，再做业务归一化。全文、表格、批注、HTML 内嵌交互、未带 RULE 标记的正文均需解释；不能从候选、已设计场景或已引用条款倒推分母。业务图片、截图及扫描文档必须实际查看并记录范围、解读和来源；结构、样式、竞品图片及历史生成物可以有依据排除，但不能以扩展名批量排除业务证据。历史文件只登记排除和哈希，不读取其业务内容。
+2. **上游到 context**：每项当前上游内容及同步前派生清单条目记录已同步、待确认、不适用或已替代，给出采用依据与实际目标定位。删除、替代仍执行 SYNC-01 的证据、原文备份和日志要求；因解析困难未处理的内容不得写成业务待确认。回读同步后的全部 context，反查每个目标条目有无来源或合理的非业务说明，不允许无依据新增。由 `context` 命令校验并固定不可覆盖的 checkpoint。
+3. **context 到 MainBasis**：只从 checkpoint 所绑定并重新读取的全部 context 汇总两份文档；原型补充信息应先完成上一步同步。每项 context 及每项 MainBasis 都要有双向对照；冲突与风险不得在汇总时变成已确认规则，明确规则的角色、条件、限制、分支和观察结果不得压缩丢失。不得把同一批内存整理结果双写两处当作完成核对。正文一致仍需对照，哈希一致不证明语义完整。checkpoint 后 context 或上游变化须重新核对；没有两段完整对照不得 seal。
+4. **逐需求拆分分支**：从两份 MainBasis 全文初始化 `requirement-coverage.json`，全部初始为未覆盖；全文单元数仅供核对文件是否遗漏，不是独立业务规则数或覆盖率。逐条提取角色、入口、条件、动作、结果、边界与生命周期，分别列出正常、拒绝、取消、超时、恢复及跨端影响中有依据的分支；不适用必须说明依据。一个条款有两个独立结果时不能只设计其一便标记整条已覆盖；页面被引用、整章被挂入来源不能代替具体分支证明。
+5. **状态、场景和用例一致**：覆盖分支必须关联实际独立场景，角色、端、入口、前置条件、步骤与预期契约一致；正式交付时逐分支与真实候选核对，不允许仅凭编号或关键词确认。所有本端执行或观察的状态转换都要有场景去向，缺失转换保持未覆盖或待确认，不得虚报三端闭环。规则确定但未完成设计的内容写未覆盖并说明生成缺口，不得改写成产品待确认。
+6. **完整与部分交付分开**：逐项只能标记已覆盖、部分覆盖、未覆盖、待确认或不适用；部分覆盖需同时列出已设计与尚未覆盖的分支。局部缺口允许不受影响的有效用例继续处理，但必须随交付披露缺口及影响范围；存在未覆盖、部分覆盖、待确认或未映射转换时，禁止声明完整覆盖。用户要求完整交付时，继续完成可依据当前需求补齐的内容；真正缺少业务决定的内容隔离并明确说明。不得将文件完整、JSON/Excel 校验通过或场景全部导出称作业务覆盖完整。
+7. **复核边界与失效**：生成执行者逐条做语义复核，将说明绑定完整对照内容哈希；脚本拒绝缺行、重复、无目标、失效引用、错契约及虚假完整声明，但不能代替自然语言理解或判断任意“不适用”理由是否成立。禁止批量填充“已复核”或自动签通过。需求、分支、场景、候选变化后重新核对受影响内容；共用生成、导出和交付入口必须调用该门禁。旧产物不因流程修改自动变完整，旧版基线必须按新流程重建。
+
 ### 2.7 [STATE-01] 业务对象状态转换与三端投影

 1. **先对象、后端**：两端及以上共同参与同一业务时，生成单位必须是共同业务对象的生命周期，用户 App、公会 App、管理后台只是状态转换的执行端或观察端。不得先分别生成三套场景，再按名称或已有用例编号拼接。
@@ -650,6 +663,7 @@ Excel 的“需求待确认”工作表固定使用以下 20 列：
 | 扫描与同步 | `SCAN-01`、`GAP-01`、`SYNC-01` | 扫描、缓存、依赖、缺失复核和适用的同步记录完整；证据基线未变化 |
 | 输入与历史隔离 | `INPUT-01` | 输入清单在两个阶段校验通过；规则、候选、历史比较和最终 JSON 的来源、顺序及哈希一致 |
 | MainBasis 衔接 | `BASIS-01` | 已配置项目的上游、两文档及生成输入版本一致；风险未作为正式预期 |
+| 全量追溯与覆盖 | `TRACE-01` | 两段传递逐项对照、context checkpoint、全文分母、分支契约和转换去向通过；完整/部分结论真实 |
 | 状态转换与跨端闭环 | `STATE-01` | 共同业务对象、状态、动作、角色和端有依据；状态连续且各操作端、观察端均有场景去向；未虚报闭环 |
 | 用例可执行性 | `CASE-01`、`ROLE-01`、`RESULT-01`、第 5 至 9 节 | 每条用例只有一个角色、一条路径和一个结果；入口、动作、条件及观察点均有依据 |
 | 语言与去重 | `LANG-01`、`DEDUP-01` | 没有含糊词、通用占位词、字段整句重复、条件子集重复或未关闭的人工复核 |

diff --git a/work/flow-rules-260914-001/before/Cem Kaner.txt b/Cem Kaner.txt
index 6c681f0..4125abf 100644
--- a/work/flow-rules-260914-001/before/Cem Kaner.txt
+++ b/Cem Kaner.txt
@@ -37,6 +37,11 @@
 <<MAINBASIS_FORMAL_REQUIREMENTS_MD>>
 <<MAINBASIS_PENDING_REQUIREMENTS_MD>>

+### 全文需求覆盖与两段传递核对（MainBasis 项目必填，执行 TRACE-01）
+<<CONTEXT_CHECKPOINT_JSON>>
+<<MAINBASIS_TRANSFER_JSON>>
+<<REQUIREMENT_COVERAGE_JSON>>
+
 ### 生成输入角色清单
 <<GENERATION_INPUT_MANIFEST_JSON>>

@@ -58,7 +63,7 @@
 1. 确认项目名称、端名、模块名称和输入材料均属于当前任务；缺失关键输入时停止生成，不自行补造。
 2. 完整读取当前 `AGENTS.md`，并按第 3 节“标准工作流”执行。
 3. 根据项目来源策略调用对应的全局证据扫描和原型同步指令；已配置 MainBasis 时先执行 `AGENTS.md` 的 `BASIS-01` 和同步指令第 3.9 节，使用当前两文档及已核对版本作为生成输入。扫描/同步记录只证明过程，不参与定义业务预期。问题按 `GATE-01` 处理。
-4. 执行 `AGENTS.md` 的 `DISC-01`：独立建立规则库；两端及以上范围再执行 `STATE-01`，先按共同业务对象建立状态转换基线，检查状态连续性后投影各目标端场景。固定发现基线并使用 `scripts/validate-generation-input.mjs --phase pre-generate` 校验后，才映射已有用例。
+4. 执行 `AGENTS.md` 的 `DISC-01`、`TRACE-01`：先从完整当前需求建立覆盖分母，再独立建立规则、状态转换及端侧场景；按 `流程追溯文件说明.md` 登记覆盖、场景与状态接口。执行 `pre-generate` 校验。仅允许历史比较的项目在此后映射已有用例；全新模式跳过历史读取和比较。
 5. 按 `AGENTS.md` 执行目标端检查、结果完整性扫描、单结果拆分和语义去重后，将本文件输出保存为本次任务的 `current-testcase-candidate.json`，此时不得作为最终交付。
 6. 本文件仅使用状态转换基线及独立场景库中的真实未覆盖项生成缺失用例；外层流程在状态转换和场景固定后映射历史并保存 `historical-case-comparison.json`。既有用例只能经当前来源复核后保留，不得反向定义规则、状态或场景。未解释条款、待补转换与业务待确认问题分开记录。
 7. 跨端或跨模块场景必须绑定状态转换标识和流程编号；每条正式用例仍只执行一个角色在一个端的动作，并只验证一个观察端的一个结果。

diff --git "a/work/flow-rules-260914-001/before/\345\205\250\345\261\200\350\257\201\346\215\256\346\211\253\346\217\217\346\214\207\344\273\244.md" "b/\345\205\250\345\261\200\350\257\201\346\215\256\346\211\253\346\217\217\346\214\207\344\273\244.md"
index 75f3cb4..6f4157e 100644
--- "a/work/flow-rules-260914-001/before/\345\205\250\345\261\200\350\257\201\346\215\256\346\211\253\346\217\217\346\214\207\344\273\244.md"
+++ "b/\345\205\250\345\261\200\350\257\201\346\215\256\346\211\253\346\217\217\346\214\207\344\273\244.md"
@@ -47,6 +47,8 @@ work/<项目目录名>-global-evidence-cache/layers/

 ### 3.2 文件记录

+MainBasis 项目额外执行 `TRACE-01`：承载业务信息的图片、截图和扫描文档须实际查看，记录范围与解读；不得将上文“可以只建档”理解为所有图片均不读。非业务素材和历史生成物可只登记哈希及排除原因；`不读取内容` 使用逐文件 `路径、SHA-256、说明` 数组，整理工具不加载其正文。全部 context 不得整文件排除，其中的非业务或历史片段逐项说明。
+
 每个候选证据文件记录：

 ```text
@@ -164,6 +166,8 @@ work/<项目目录名>-global-evidence-cache/layers/

 ## 9. 双重基线校验

+进入本阶段前，MainBasis 项目必须按 `TRACE-01` 完成全量来源到 context 对照、从磁盘回读并固定 context checkpoint、context 到 MainBasis 双向对照和 seal。执行命令与字段见 `流程追溯文件说明.md`；文件清单、静态控件数量或一份同源整理结果不能替代这两段核对。
+
 任务开始读取或复用证据时保存第一次基线。生成最终 JSON 和 Excel 前，对全部已读取或复用证据再次计算 SHA-256。

 - 两次一致：允许继续交付。

diff --git "a/work/flow-rules-260914-001/before/\345\216\237\345\236\213\344\270\216\351\234\200\346\261\202\346\270\205\345\215\225\345\220\214\346\255\245\346\214\207\344\273\244.md" "b/\345\216\237\345\236\213\344\270\216\351\234\200\346\261\202\346\270\205\345\215\225\345\220\214\346\255\245\346\214\207\344\273\244.md"
index f3173a8..f883bcb 100644
--- "a/work/flow-rules-260914-001/before/\345\216\237\345\236\213\344\270\216\351\234\200\346\261\202\346\270\205\345\215\225\345\220\214\346\255\245\346\214\207\344\273\244.md"
+++ "b/\345\216\237\345\236\213\344\270\216\351\234\200\346\261\202\346\270\205\345\215\225\345\220\214\346\255\245\346\214\207\344\273\244.md"
@@ -119,13 +119,13 @@
 项目配置 `MainBasis` 时，生成执行者自动按以下顺序衔接。命令在 testcase 仓库根目录运行；`<任务目录>` 使用本次新建的 `work/...` 相对路径，不复用历史任务目录。

 1. 运行 `node scripts/mainbasis.mjs verify liveshow-proto`。版本一致时复用该需求基线；失败时按返回原因进入下一步，不能把旧文档哈希补写成新基线跳过分析。
-2. 运行 `node scripts/mainbasis.mjs start liveshow-proto <任务目录>` 固定同步前快照。执行全局扫描、系统概要优先的 context 同步，再从当前上游及同步后的需求清单汇总两份 MainBasis 文档。首次衔接必须重新核对现有文档的来源优先级，不能沿用旧版“系统概要与批注同等”的声明；文档中的旧绝对链接一并按当前项目位置核对。
+2. 运行 `node scripts/mainbasis.mjs start liveshow-proto <任务目录>` 固定同步前快照与派生清单原文。执行全局扫描、系统概要优先的 context 同步，然后按 `TRACE-01` 运行 prepare-context、完成逐项对照、运行 context，回读核对全部 context 并固定 checkpoint。此后才读取这些文件汇总两份 MainBasis；不得同一批整理结果双写后直接 seal。首次衔接重新核对现有文档优先级和旧绝对链接。
 3. 保留 `global-evidence-scan-result.json`，其中 `文件清单` 使用仓库相对 `路径` 和 `SHA-256`（兼容 `SHA256`）；同步完成后更新派生需求的文件哈希。保存 `prototype-context-sync-result.json`，按第 4 节记录实际结果，并补充下列衔接字段。
 4. 对每份派生清单及两份 MainBasis，在 `文件核对` 中保存 `路径、修改前SHA256、修改后SHA256、核对说明`。首次新建文件的修改前哈希为 `null`；即使内容无变化，也说明核对范围和采用依据。实际修改须记录根目录 `需求清单变更日志.md` 的连续编号。
 5. 同步结果增加 `MainBasis复核`：`业务优先级` 固定为 `系统概要优先，原型和批注补充`；`复核说明` 写本次实际分析与隔离情况；`待确认问题` 保存问题编号数组。`来源映射` 逐项包含 `来源路径、来源SHA256、来源位置、目标路径、目标原文`，将规则及风险追溯到两份文档中的实际内容，不得使用单条示例代替完整映射。运行 `node scripts/mainbasis.mjs inspect liveshow-proto`，将实际返回的 `上游指纹` 写入该复核对象。哈希清单不表示已完成语义分析。
-6. 运行 `node scripts/mainbasis.mjs seal liveshow-proto <任务目录>`。校验通过才发布 `work/liveshow-proto-mainbasis/latest.json`，并在本次任务保存不可覆盖的 `mainbasis-baseline.json`。记录同时绑定扫描、同步结果和同步前快照；同步期间上游改变则重新 start，不发布混合版本。
+6. 两文档汇总后运行 prepare-basis，完成 context 到两文档的逐项双向对照，再运行 `node scripts/mainbasis.mjs seal liveshow-proto <任务目录>`。具体字段见 `流程追溯文件说明.md`。校验通过才发布新版 latest.json 和任务内不可覆盖的基线；缺少 checkpoint、来源漏项、目标无依据或记录变化都不能发布。旧版基线不能补写哈希冒充新版。
 7. 再次 verify，在各端 `generation-input-manifest.json` 中填写 `MainBasis基线: {路径, SHA-256}`。输入两文档分别使用 `当前业务证据/true` 和 `风险与缺口/false`；上游材料保留在需求同步记录中，不传为用例业务输入。由生成执行者从文档建立独立规则、共享对象状态和端侧场景，使用现有结构化设计渲染与共用校验生成，不运行旧任务的直接导出脚本。三端共用此基线，下游各端可以有独立任务子目录。
-8. 候选前和最终输出前分别执行 `validate-generation-input.mjs`，导出及缓存发布继续执行既有交付门禁。本节发布的只是需求版本，不得当作全部业务覆盖、测试执行或缓存发布已通过。
+8. 运行 prepare-coverage，从两份文档全文初始化全部未覆盖的清单；执行 `TRACE-01` 逐条拆分和核对场景、状态与用例契约。候选前和最终输出前分别执行 `validate-generation-input.mjs`，导出及缓存发布继续执行共用门禁。本节发布的只是需求版本，不能当作全部业务覆盖或产品测试已通过。

 `mainbasis.mjs` 自动完成文件版本核对和基线发布；读取原型、解释差异、补充需求、隔离风险和用例设计由生成执行者按本指令完成。仅执行 start/inspect 不会更新需求正文；仅有哈希相同也不能替代首次同步。
```

### 回退方法

按上述差异逐段反向恢复四份规则/指令文件，并恢复 before 目录中的同名共用脚本（仅在确认没有后续修改时）；新增接口说明和 requirement-traceability 脚本及测试仅在无后续依赖时移除。不得整仓回退、恢复归档旧业务输入或覆盖用户已有其他修改。基线 schemaVersion 回退应与 mainbasis 门禁同步，不能把旧 1.0 记录改名为 2.0。实际回退另追加引用 RCL-0050 的记录。

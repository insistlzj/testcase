# 已确认补充业务规则模板

> 用途：记录用户、产品或项目负责人已经明确确认的业务规则，供测试用例生成时作为正式证据读取。
>
> 使用方法：复制“空白规则模板”，每条业务规则使用一个独立编号。删除不适用的说明文字后，将完成文件放入目标项目的 `context/` 目录。
>
> 注意：本文中的填写示例仅用于说明格式，不代表 Luma Live 的真实或已确认需求。

## 一、文件信息

- 适用项目：`<项目名称>`
- 规则状态：已确认
- 适用版本：`<版本号、迭代名称或生效范围>`
- 确认人：`<姓名或角色>`
- 确认日期：`<YYYY-MM-DD>`
- 规则文件负责人：`<姓名或角色>`
- 与其他来源冲突时的处理：仅当具体规则明确填写了“替代旧规则”及其生效范围时，才按替代关系处理；否则按来源冲突进入需求待确认。

## 二、填写要求

1. 一个规则编号只描述一个业务决定，不合并无关功能。
2. 必须写明适用端、角色、前置状态、触发操作和确定结果。
3. 数量、金额、时间、概率、次数等参数必须填写具体值，或明确写成“以可读取的后台配置为准”。
4. 没有对应规则的字段填写“无”或“不适用”，不得根据经验猜测。
5. 与现有规则不一致时，必须注明被替代规则、适用版本、生效时间以及旧数据和进行中业务的处理方式。
6. “确定结果”可以包含多个观察点；生成测试用例时，每个独立观察结果拆成一条用例。
7. 只有场景但没有确定结果的内容，不属于已确认规则，应进入需求待确认。

## 三、空白规则模板

### CR-<模块缩写>-001 <规则名称>

- 功能模块：`<端 - 模块 - 功能>`
- 适用端：`<用户 App / 主播 App / 公会 App / 管理后台等>`
- 适用角色：`<用户、主播、房管、公会长、管理员等>`
- 适用页面或场景：`<页面名称、直播间类型或业务场景>`
- 业务对象：`<账号、直播间、直播场次、邀请、订单、礼物等>`
- 前置状态：
  1. `<执行该规则前必须满足的状态或数据条件>`
- 触发操作：
  1. `<角色执行的具体操作>`
- 确定结果：
  1. `<可独立观察和判断的业务结果>`
- 操作反馈：
  1. `<提示、按钮状态、页面停留或跳转；没有则填写“无”>`
- 数量、时间、金额或概率边界：`<具体数值、可读取配置或“不适用”>`
- 异常和中断：
  1. `<取消、拒绝、超时、中断、重复操作等情况下的确定结果；没有则填写“不适用”>`
- 跨端或跨模块结果：
  1. `<其他角色、端、页面、记录或统计的确定结果；没有则填写“不适用”>`
- 生效范围：`<新数据、指定版本、指定角色、指定地区等>`
- 生效时间：`<日期、版本发布后或其他明确条件>`
- 历史数据处理：`<继续按旧规则、迁移到新规则或不适用>`
- 进行中业务处理：`<继续按旧规则、立即采用新规则或不适用>`
- 替代旧规则：`是 / 否`
- 被替代规则及来源：`<文件、章节、规则编号或“不适用”>`
- 需求来源：`<用户确认、产品确认、正式需求等>`
- 确认人：`<姓名或角色>`
- 确认日期：`<YYYY-MM-DD>`
- 备注：`<其他必要说明>`

## 四、填写示例（非真实需求）

### EXAMPLE-LIVE-001 主播接收多个连麦邀请

- 功能模块：用户 App - 直播 - 主播连麦
- 适用端：用户/主播 App
- 适用角色：正在直播的主播
- 适用页面或场景：普通直播间
- 业务对象：连麦邀请
- 前置状态：
  1. 主播 A 正在普通直播间直播。
  2. 主播 A 当前未处于连麦状态。
  3. 主播 B 和主播 C 均正在普通直播间直播。
- 触发操作：
  1. 主播 B 向主播 A 发起连麦邀请。
  2. 主播 C 在第一个邀请未处理时向主播 A 发起连麦邀请。
- 确定结果：
  1. 主播 A 可以同时收到主播 B 和主播 C 的连麦邀请。
  2. 主播 A 可以选择其中一个邀请接受。
  3. 主播 A 接受一个邀请后，其他未处理邀请自动失效。
- 操作反馈：
  1. 失效邀请不再允许接受。
- 数量、时间、金额或概率边界：同时待处理的连麦邀请最多为 2 个。
- 异常和中断：
  1. 邀请方结束直播后，对应邀请失效。
  2. 被邀请方结束直播后，全部待处理邀请失效。
- 跨端或跨模块结果：
  1. 被接受的邀请方进入连麦状态。
  2. 未被接受的邀请方保持原直播状态。
- 生效范围：规则确认后新发起的连麦邀请。
- 生效时间：对应版本发布后。
- 历史数据处理：不适用。
- 进行中业务处理：规则生效前已经发起的邀请继续按旧规则处理。
- 替代旧规则：否
- 被替代规则及来源：不适用
- 需求来源：格式示例
- 确认人：不适用
- 确认日期：不适用
- 备注：本示例只用于说明填写格式，不得作为正式需求生成测试用例。

# 已确认需求转测试用例指令

读取 `用户App-直播模块-260828-012.xlsx` 时，先记录文件路径和 SHA-256，并将原工作簿登记为 `历史参照`，不得直接修改其中的旧用例后生成新版本。

只提取“需求待确认”中状态为“已确认”且当前没有更新来源冲突的产品结论，在本次任务 `work/` 目录保存 `confirmed-decision-extract.json`。每条摘录记录原工作簿路径和哈希、工作表与单元格、问题编号、产品结论、结论补充、适用范围、当前适用性和判断依据；无法确认仍适用的结论进入来源冲突复核，不得作为当前规则。

将通过复核的决策摘录登记为 `当前业务证据`，与其他当前需求、原型和用户补充一起建立 `business-rule-catalog.json`，再从当前规则清单生成 `current-testcase-candidate.json`。旧工作簿中的“已有用例编号”和用例内容只用于生成 `historical-case-comparison.json`，不得直接修订或新增候选用例。

处理父子问题触发关系并保留问题编号追溯；未确认记录不改动。通过输入隔离、当前规则映射、历史比较、候选与最终 JSON 同哈希及其他交付门禁后，生成下一个编号版本，不覆盖原文件。

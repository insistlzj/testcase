# 需求清单变更日志

本文件是测试用例根目录唯一的需求清单同步日志，用于记录项目 `context/` 派生需求清单和项目来源策略的实际变化，保证可以审查和回退。

- 本日志只追加，不删除、覆盖或改写历史记录；记录错误时追加更正记录。
- 实际修改派生需求清单时必须记录修改前完整原文、修改后完整原文和直接原型证据。
- 回退也必须新增记录并引用原变更编号。
- `AGENTS.md`、`Cem Kaner.txt` 等规则文件的变化记录在 `规则文件变更日志.md`，不得混入本日志。
- 不得按项目、日期或任务创建其他同类需求清单变更日志。

## RSL-0001 初始化 Luma Live 原型优先同步策略

- 修改时间：2026-08-29 17:55:01 CST
- 项目：Luma Live
- 来源策略：`prototype-primary`
- 修改原因：产品经理修改原型后可能遗漏同步需求清单，需要在每次生成测试用例前自动校正派生需求。
- 原型基线：本次仅初始化策略，尚未执行需求内容同步，因此未固定原型基线。
- 涉及文件：`liveshow-proto/需求来源策略.json`
- 派生需求清单：`context/01-用户主播App-项目需求清单.md`、`context/02-公会App-项目需求清单.md`、`context/03-管理后台-项目需求清单.md`
- 需求内容变化：无。
- 受影响用例：无；下一次生成用例前首次执行全量同步并评估。

### 修改前

```text
项目没有独立的机器可读来源策略配置，生成用例前也没有强制要求先同步 prototype 与 context。
```

### 修改后

```text
项目声明来源策略为 prototype-primary；prototype 是当前有效需求源，三份 Markdown 项目需求清单是派生需求。生成用例前自动同步，历史 Excel 和沟通记录只读。
```

### 回退方法

1. 删除 `liveshow-proto/需求来源策略.json`，项目将按全局默认 `conflict-review` 处理。
2. 不修改三份派生需求清单，因为本次没有同步需求内容。
3. 不删除本条历史记录；追加引用 `RSL-0001` 的回退记录。

## RSL-0002 同步用户 App 直播模块原型与派生需求

- 修改时间：2026-08-29 18:09:41 CST
- 项目：Luma Live
- 来源策略：`prototype-primary`
- 修改原因：重新生成用户 App 直播模块测试用例前，按当前审核通过原型同步派生需求，修正页面结构、房型展示和连麦入口等可直接定位的差异；保留原型未覆盖的隐藏业务规则，并显式标记两个来源冲突。
- 原型基线：`work/liveshow-user-live-testcases/prototype-context-sync-result.json` 中记录的 17 个文件路径、修改时间和 SHA-256。
- 涉及文件：`liveshow-proto/context/01-用户主播App-项目需求清单.md`
- 修改位置：`首页与发现/直播广场`、`直播观看与互动`、`开播设置与控制`、`连麦与 PK`
- 差异分类：新增 1、修改 4、明确删除 0、原型未覆盖 8、来源冲突 2、无法定位 0。
- 影响范围：用户 App 直播广场、普通房/门票房/密码房进房、观众互动、主播开播设置、主播连麦。
- 受影响用例：上一版 256 条正式用例均继续有效；其中 `LIVE-007` 至 `LIVE-009`、`LIVE-017`、普通房清晰度/横屏、门票后置规则、敏感词、外部分享、开播推送相关用例继续以保留需求或待确认记录为追溯依据。需要重审 0 条、已被替代 0 条、应当废弃 0 条。

### 修改前

```text
直播列表：卡片展示直播间封面、主播昵称/头像、直播标题、在线人数、分类标签。支持下拉刷新、上拉加载。排序支持 3 种 Tab 切换：热门（后台可配权重）、新人、关注。
分类：按后台配置的分类筛选直播间。
空状态：无直播时展示占位插画 + "暂无直播，去关注喜欢的主播" + "去发现"按钮。
普通房：免费进入，进入后自动播放，支持清晰度切换、横竖屏、加载中/断流重连提示。
门票房（首版上架后经后台开关灰度开启）：进房前展示门票价格与说明，确认支付金币购票后进入；购票后本场直播内可自由进出，直播结束或被主播踢出后失效；重复进入同一场已购票不再收费。购票记录写入消费明细；不支持连麦或 PK。
密码房（首版上架后经后台开关灰度开启）：进房需输入主播设置的密码，密码正确进入、错误提示重试。密码由主播私下/授权发放（如在粉丝群发房间号）。密码房在首页有展示入口，放置在直播广场列表最下方；不支持连麦或 PK。
公屏聊天/弹幕：发送文字消息、系统进场提示、表情。敏感词过滤（后台词库）。
点赞、关注主播。分享直播间到外部社交平台：WhatsApp / Facebook / Instagram。
清晰度设置：支持多档清晰度切换。
开始/结束直播；开播触发向关注粉丝推送开播提醒。
主播间连麦：仅普通房支持。一期仅支持两位主播视频连线；由主播发起邀请、对方接受后建立连线；无观众上麦麦位，不支持超过两人的连线或 PK。连麦中双方画面分屏展示，可结束连麦。门票房、密码房不展示连麦或 PK 入口。
```

### 修改后

```text
直播列表：卡片展示直播间封面、主播昵称/头像、直播标题和房间类型标识；普通房显示在线人数，密码房隐藏在线人数，门票房显示金币价格。列表仅提供“热门”“新人”2 个 Tab；已关注且正在直播的主播在 Tab 上方独立头像栏展示，无符合条件的主播时整栏隐藏。原需求中的下拉刷新、上拉加载在当前原型未覆盖，暂保留，具体行为以实现和后续确认结果为准。
分类：原型提供“全部、唱歌、跳舞、聊天、游戏、才艺”分类筛选；热门列表中包含运营横幅位。
空状态：无直播时展示占位插画 + "暂无直播，去关注喜欢的主播" + "去发现"按钮。当前原型未覆盖该空状态，暂保留。
普通房：免费进入，进入后自动播放。原需求中的清晰度切换、横竖屏、加载中和断流重连提示在当前原型未覆盖，暂保留。
门票房：直播广场卡片显示金币价格；点击后展示购票面板、门票商品、价格、余额、充值和购买入口，确认支付后进入；不支持连麦或 PK。原需求中的灰度开关、本场重复进入、直播结束或被踢后失效、消费明细规则在当前原型未覆盖，暂保留。
密码房：由主播配置是否在直播广场展示；可见卡片与其他直播卡片同区展示，显示锁定标识且不显示在线人数。点击后输入密码，正确则进入、错误提示“密码错误，请重新输入”；不支持连麦或 PK。开播设置页提示“设置 4-12 位密码”，进房页最多输入 8 位，主播直播中修改密码页要求 8 位数字，统一密码长度和字符规则待确认。原需求中的灰度开关和私下/授权发放方式在当前原型未覆盖，暂保留。
公屏聊天/弹幕：发送文字消息、系统进场提示、表情。敏感词词库在管理后台原型中存在，但命中后的用户端处理方式在当前原型未覆盖，暂保留原“敏感词过滤”需求并待确认。
点赞、关注主播。当前观众端原型提供“转发”入口，主播端提供转至粉丝群和分享给好友；历史需求中的 WhatsApp / Facebook / Instagram 未在当前原型中明确，最终外部分享平台范围待确认。
清晰度设置：支持多档清晰度切换。当前原型未覆盖该设置，暂保留。
开始/结束直播；开播触发向关注粉丝推送开播提醒。当前原型仅覆盖开播倒计时、开播成功提示和进入主播直播间，未覆盖推送结果，暂保留。
主播间连麦：仅普通房支持。一期仅支持两位主播视频连线；由主播发起邀请、对方接受后建立连线；无观众上麦麦位，不支持超过两人的连线或 PK。连麦中双方画面分屏展示，可结束连麦。门票房、密码房不提供可用的连麦或 PK 能力；密码房保留置灰且不可用的“PK”按钮，点击提示“密码房无法发起连麦”。
```

### 直接原型证据

```text
prototype/pages/user/home/live-plaza.html：热门/新人 Tab、关注直播头像栏、分类项、运营横幅、普通/门票/密码房卡片及购票/密码弹层交互。
prototype/assets/annotations.js：关注头像栏隐藏规则、门票/密码进房、密码房 PK 置灰提示、仅普通房支持两位主播连麦。
prototype/pages/user/host/start-live-settings.html：3 种房型、密码提示 4-12 位、广场展示开关、开播倒计时与成功提示。
prototype/pages/user/live/live-room-host-password.html：主播直播中修改 8 位数字密码，密码房 PK 不可用。
prototype/pages/user/live/live-room.html 与 live-room-host.html：观众转发、主播转至粉丝群/分享给好友等入口。
```

### 回退方法

1. 在 `liveshow-proto/context/01-用户主播App-项目需求清单.md` 中，将“修改后”列出的 11 段文字逐段替换为“修改前”对应原文。
2. 不修改或删除 `prototype/` 文件、历史 Excel 和本条日志。
3. 回退后追加一条引用 `RSL-0002` 的新记录，不得删除本条记录。

## RSL-0003 恢复 Luma Live 来源策略并同步直播记录需求

- 修改时间：2026-08-31 09:59:02 CST
- 项目：Luma Live
- 来源策略：`prototype-primary`
- 修改原因：生成用户 App 直播记录模块测试用例前执行原型与派生需求同步；恢复 RSL-0001 已登记但当前缺失的机器可读来源策略，并将直播记录原型已明确的日期筛选、汇总、记录字段和快照规则同步到派生需求。
- 原型基线：`work/liveshow-user-live-records/prototype-context-sync-result.json` 中记录的文件路径、修改时间和 SHA-256。
- 涉及文件：`liveshow-proto/需求来源策略.json`、`liveshow-proto/context/01-用户主播App-项目需求清单.md`
- 修改位置：项目来源策略；`主播数据中心（主播侧）/历史直播记录`
- 差异分类：新增 1、修改 1、明确删除 0、原型未覆盖 5、来源冲突 0、无法定位 0。
- 影响范围：用户 App 主播中心、直播数据、直播记录、结束直播后的记录生成。
- 受影响用例：历史 Excel 不覆盖、不删除；上一版直播模块中结束直播并生成记录的相关用例继续有效，需要重审 0 条、已被替代 0 条、应当废弃 0 条。

### 修改前

```text
项目策略：RSL-0001 已登记 `liveshow-proto/需求来源策略.json`，但任务开始时该文件不存在。

历史直播记录：
- 场次、时长、人气、收礼。
```

### 修改后

```text
项目策略：恢复 `prototype-primary` 配置；prototype 为当前有效需求源，三份 Markdown 项目需求清单为派生需求，生成用例前自动同步，历史来源只读。

历史直播记录：
- 默认展示近 7 天的历史开播场次；支持本月、本周、上月快捷筛选，以及自定义单日或连续日期范围。
- 当前日期范围内分别汇总收益、总时长和直播场次。
- 单场记录按开播时间倒序展示开播时的封面和直播间标题、开播时间、直播时长、本场收益、观众人数、收礼数量和房间类型；历史封面和标题不随当前开播设置变更。
- 开播时间使用 dd/mm/yyyy HH.mm 格式，筛选条件仅展示日期。
- 点击单场记录展示“查看本场直播数据”操作反馈；当前原型未定义后续详情页面或进一步跳转。
```

### 直接原型证据

```text
prototype/Luma Live-原型说明.md：直播记录默认近 7 天、快捷与自定义筛选、三项汇总、倒序记录字段和房间类型。
prototype/assets/annotations.js：记录快照、日期筛选、汇总指标和时间格式。
prototype/assets/mock.js：liveRecords 的 date、startedAt、title、coverAsset、income、roomType、duration、viewers、gifts 字段。
prototype/pages/user/host/live-records.html：日期筛选交互、汇总计算、记录列表渲染和单场点击反馈。
```

### 原型未覆盖

```text
1. 非主播和直播权限失效账号访问直播记录的处理方式。
2. “近 7 天”的时区、起止时刻和自然日包含规则。
3. 观众人数与收礼数量的统计口径。
4. 结束直播后新记录可查询的时效和失败恢复方式。
5. 大量历史记录的分页、加载上限和保留期限。
```

### 回退方法

1. 删除 `liveshow-proto/需求来源策略.json`，项目恢复为缺少机器可读策略配置；后续任务按全局默认 `conflict-review` 处理。
2. 在 `liveshow-proto/context/01-用户主播App-项目需求清单.md` 的“历史直播记录”下，将本次新增的五条内容整体替换为 `场次、时长、人气、收礼。`。
3. 不修改或删除 `prototype/` 文件、历史 Excel 和本条日志。
4. 回退后在 `需求清单变更日志.md` 追加引用 `RSL-0003` 的新记录，不得删除本条历史记录。

## RSL-0004 同步公会 App 直播记录需求

- 修改时间：2026-08-31 10:58:59 CST
- 项目：Luma Live
- 来源策略：`prototype-primary`
- 修改原因：生成公会 App 直播记录模块测试用例前执行原型与派生需求同步，将主页入口、默认筛选、主播筛选、汇总指标、场次列表、场次详情和返回保留条件等原型已明确规则补入公会端需求清单。
- 原型基线：`work/liveshow-guild-live-records/prototype-context-sync-result.json` 中记录的 11 个文件路径、修改时间和 SHA-256。
- 涉及文件：`liveshow-proto/context/02-公会App-项目需求清单.md`
- 修改位置：`数据与收益/直播记录`
- 差异分类：新增 1、修改 0、明确删除 0、原型未覆盖 6、来源冲突 0、无法定位 0。
- 影响范围：公会 App 首页、直播记录、主播多选、日期筛选、直播场次详情、送礼详情、全部直播记录。
- 受影响用例：历史 Excel 不覆盖、不删除；任务开始时不存在公会 App 直播记录模块历史 Excel，需要重审 0 条、已被替代 0 条、应当废弃 0 条。

### 修改前

```text
数据与收益章节仅包含“主播数据”“主播运营数据”“收益概览”和“结算台账”，没有单独描述直播记录页面的默认筛选、汇总指标、场次字段和详情交互。
```

### 修改后

```text
直播记录：公会首页提供直播记录入口；默认选择当前公会全部主播并展示今日数据，支持今日、昨日、本周、上周、本月、上月和自定义日期筛选。主播筛选支持搜索、多选和全选，选择后返回直播记录并保留日期条件。页面按筛选范围分别汇总礼物金币收益、去重开播人数和直播场次；场次列表展示直播主题、主播名称、主播 ID、开播时间、直播时长、金币收益、观看人数和收礼数量。点击场次进入直播场次详情，展示场次基础信息、观众数量、送礼观众数、礼物数量、礼物收益及送礼记录；点击送礼记录查看赠送时间、礼物名称、消费金币、礼物单价、礼物类型、赠送数量、用户名称和用户 ID。返回直播记录时保留原主播和日期筛选条件。全部直播记录页按日期范围汇总当前公会各主播的直播次数、有效天和直播时长，并可进入单个主播的直播记录。
```

### 直接原型证据

```text
prototype/pages/guild/home/guild-home.html：公会首页“直播记录”入口携带全部主播、今日日期和 today 快捷条件。
prototype/assets/annotations.js：直播记录默认全部主播和今日、日期选项、三项汇总定义、场次字段、场次详情字段、送礼详情字段及返回保留筛选条件。
prototype/Luma Live-原型说明.md 与 prototype/index.html：注册直播记录、直播场次详情、全部直播记录和主播多选页面及其模块归属。
prototype/pages/guild/data/guild-host-data.html：主播与日期筛选、汇总、倒序场次列表和详情跳转交互。
prototype/pages/guild/data/guild-violation-host-select.html：主播搜索、多选、全选和筛选条件回填。
prototype/pages/guild/data/guild-live-gift-detail.html：场次指标、送礼列表、送礼详情和返回条件保留。
prototype/pages/guild/data/guild-all-live.html 与 prototype/assets/guild-record-overviews.js：按主播汇总直播次数、有效天和直播时长，以及进入单个主播记录的交互。
```

### 原型未覆盖

```text
1. 日期边界使用的业务时区和跨日归属规则。
2. 大量直播场次和送礼记录的分页、加载上限及历史保留期限。
3. 已离开公会主播的历史直播记录是否仍可查询。
4. “全部直播记录”从正式业务页面进入的明确入口。
5. “主播数据”中的导出能力是否也适用于直播记录页面。
6. 异常网络、数据加载失败和重试时的统一交互。
```

### 回退方法

1. 在 `liveshow-proto/context/02-公会App-项目需求清单.md` 的“数据与收益”章节删除本次新增的“直播记录”整条内容，保留原有四条需求。
2. 不修改或删除 `prototype/` 文件、历史 Excel 和本条日志。
3. 回退后在 `需求清单变更日志.md` 追加引用 `RSL-0004` 的新记录，不得删除本条历史记录。

## RSL-0005 同步公会 App 数据与收益模块需求

- 修改时间：2026-08-31 11:23:26 CST
- 项目：Luma Live
- 来源策略：`prototype-primary`
- 修改原因：生成公会 App“数据与收益”模块测试用例前执行完整原型同步，将公会业绩、每日经营详情、违规记录、主播分成记录和公会分成记录的已明确页面规则补入派生需求。
- 原型基线：`work/liveshow-guild-data-income/prototype-context-sync-result.json` 中记录的 22 个文件路径、修改时间和 SHA-256。
- 涉及文件：`liveshow-proto/context/02-公会App-项目需求清单.md`
- 修改位置：`数据与收益`
- 差异分类：新增 5、修改 0、明确删除 0、原型未覆盖 9、来源冲突 2、无法定位 0。
- 影响范围：公会 App 首页、公会业绩、每日经营详情、主播业绩下钻、直播记录、违规记录、主播分成记录、公会分成记录和主播多选。
- 受影响用例：`公会App-直播记录模块-260831-001.xlsx` 的 107 条正式用例均继续有效；需要重审 0 条、已被替代 0 条、应当废弃 0 条。

### 修改前

```text
数据与收益章节已描述主播数据、直播记录、主播运营数据、收益概览和结算台账，但没有单独描述公会业绩、每日经营详情、违规记录、主播分成记录和公会分成记录的当前页面规则。
```

### 修改后

```text
公会业绩：提供日数据和月数据两个维度。日数据默认展示本月，支持本月、上月、本周、上周和自定义日期范围；按筛选范围展示公会礼物金币收益、直播场次、开播人数和达成有效天，并可点击指标切换对应趋势。日数据列表展示日期、直播场次、达标主播和收益，点击日期进入对应日期的主播业绩；月数据列表展示月份、收益和开播人数，点击月份进入对应月份的主播业绩。开播人数的跨日和跨月去重口径与当前页面脚本存在差异，最终口径待确认。

每日经营详情：展示指定日期的当日收益、直播中主播数和达成有效天主播数；主播列表可在已开播、未开播之间切换。已开播主播按当日礼物金币收益倒序展示主播名称、主播 ID、主播等级、当日收益、直播时长和有效天达成状态；点击主播进入对应主播数据。

违规记录：支持在直播间违规和账号违规之间切换，默认选择当前公会全部主播并展示今日记录；支持主播搜索、多选、全选和自定义日期范围。记录展示违规主播名称、主播 ID、违规类型、发生时间和处理结果，违规类型包括色情低俗、涉及宗教政治、暴恐血腥、未成年有害和其他；处理结果为空时显示“-”。点击违规主播进入主播主页。批注明确的今日、昨日、本周、上周、本月和上月快捷日期筛选在当前页面没有对应入口，最终交互待确认。

主播分成记录：默认选择当前公会全部主播并展示本月记录，支持今日、昨日、本周、上周、本月、上月和自定义日期筛选。列表按分成时间倒序展示主播名称、主播 ID、分成时间和美元分成金额；正数使用“+”和绿色，负数使用“-”和红色。主播筛选支持搜索、多选和全选，返回后保留日期条件。

公会分成记录：仅展示当前公会的分成记录，按分成时间倒序展示分成时间和美元分成金额；正数使用“+”和绿色，负数使用“-”和红色。
```

### 直接原型证据

```text
prototype/index.html 与 prototype/Luma Live-原型说明.md：数据与收益页面分组、页面名称和页面路径。
prototype/pages/guild/home/guild-home.html：公会业绩、运营指标、直播记录、违规记录、主播分成和公会分成入口。
prototype/assets/annotations.js：公会业绩维度和指标、每日经营详情排序、违规范围和字段、分成记录筛选和显示规则。
prototype/pages/guild/data/guild-income.html：日/月切换、日期筛选、指标趋势、日/月列表和主播业绩下钻。
prototype/pages/guild/data/guild-income-day-detail.html：每日收益、主播概况、开播状态筛选、排序和主播数据下钻。
prototype/pages/guild/data/guild-all-violations.html 与 prototype/assets/guild-record-overviews.js：违规范围、主播筛选、日期筛选、列表字段、空态和主播主页跳转。
prototype/pages/guild/data/guild-share-ledger.html 与 prototype/assets/guild-share-records.js：主播和日期筛选、倒序列表、正负分成金额及返回条件保留。
prototype/pages/guild/data/guild-share-income.html：当前公会分成记录、倒序时间和正负美元金额。
prototype/assets/guild-data-model.js 与 prototype/assets/common.js：有效天、主播日数据、金额、金币、日期和时间格式。
```

### 来源冲突

```text
1. annotations.js 要求公会业绩的开播人数按所选范围去重，guild-income.html 的日数据脚本对每日开播人数求和，月数据脚本对各月开播人数求和。
2. annotations.js 要求违规记录支持今日、昨日、本周、上周、本月、上月和自定义日期筛选，guild-all-violations.html 仅提供一个自定义日期范围入口，没有快捷日期控件。
```

### 原型未覆盖

```text
1. 公会业绩和分成记录的业务时区、跨日归属及历史保留期限。
2. 公会业绩大量日期或月份、违规记录和分成记录的大数据加载方式。
3. 已离开公会主播在各数据页面和分成记录中的历史可见范围。
4. 主播分成和公会分成记录的生成触发点、可查询时效及负数记录业务类型。
5. 原结算台账中的三方分账明细、命中规则版本和结算状态在当前公会 App 页面的展示位置。
6. 数据首次加载失败、筛选失败和重试方式。
7. 主播数据导出是否覆盖公会业绩、直播记录、违规记录和分成记录。
8. 当前日期的“直播中”与历史日期“已开播”的指标命名和统计边界。
9. 跨页面汇总指标与明细列表的最终对账处理方式。
```

### 回退方法

1. 在 `liveshow-proto/context/02-公会App-项目需求清单.md` 的“数据与收益”章节删除本次新增的“公会业绩”“每日经营详情”“违规记录”“主播分成记录”“公会分成记录”五条内容。
2. 保留 RSL-0004 已新增的“直播记录”以及原有主播数据、主播运营数据、收益概览和结算台账。
3. 不修改或删除 `prototype/` 文件、历史 Excel 和本条日志。
4. 回退后在 `需求清单变更日志.md` 追加引用 `RSL-0005` 的新记录，不得删除本条历史记录。

## RSL-0006 登记 Luma Live 公共证据清单

- 修改时间：2026-09-01 15:25:40 CST
- 项目：Luma Live
- 来源策略：`prototype-primary`
- 修改原因：用户确认系统概要中的连麦规则被误判为缺失属于重要问题，要求修改。为防止生成流程只读取三份派生需求清单，在项目来源策略中显式登记用户 App 页面架构和系统概要为公共证据。
- 涉及文件：`liveshow-proto/需求来源策略.json`
- 修改位置：新增 `公共证据清单`。
- 差异分类：来源策略证据范围登记；不修改 `context/` 需求正文，不改变原型优先策略和派生需求写入白名单。
- 影响范围：Luma Live 全部测试用例生成、审查和需求待确认判定；尤其影响连麦、礼物、账号关系、权限、金额和跨端状态规则。
- 受影响用例：既有工作簿不自动修改；后续重新生成或重审时，所有把公共证据中明确规则误列为缺失或待确认的用例需要重审。

### 修改前

```json
"派生需求清单": [
  "context/01-用户主播App-项目需求清单.md",
  "context/02-公会App-项目需求清单.md",
  "context/03-管理后台-项目需求清单.md"
]
```

### 修改后

```json
"派生需求清单": [
  "context/01-用户主播App-项目需求清单.md",
  "context/02-公会App-项目需求清单.md",
  "context/03-管理后台-项目需求清单.md"
],
"公共证据清单": [
  "context/01-用户主播App-页面架构.md",
  "context/系统概要 .md"
]
```

### 直接依据

```text
context/系统概要 .md 第 231-244 行明确描述主播连麦前提、多个邀请、接受、拒绝、取消和结束结果。
prototype/assets/annotations.js 的“收到邀请”和“连麦主播”批注明确多条邀请可同时存在，且处理一条只更新对应记录。
context/01-用户主播App-页面架构.md 负责用户 App 页面和入口归属，不属于可自动覆盖的派生需求清单，但属于生成用户 App 用例时必须读取的公共证据。
```

### 回退方法

1. 从 `liveshow-proto/需求来源策略.json` 删除 `公共证据清单` 字段，保留原有来源策略和派生需求清单。
2. 不修改或删除 `context/`、`prototype/`、历史 Excel 和本条日志。
3. 回退后在 `需求清单变更日志.md` 追加引用 `RSL-0006` 的新记录，不得删除本条历史记录。

## RSL-0007 确认 Luma Live 已删除需求文件的替代关系

- 修改时间：2026-09-01 16:33:29 CST
- 项目：Luma Live
- 来源策略：`prototype-primary`
- 修改原因：生成用户 App 直播模块测试用例前执行规则缺失复核时，发现 `项目说明.md` 仍引用 4 个已删除的需求文件。用户在当前任务中明确确认这些旧文件已由系统概要和当前保留的需求文件正式替代。
- 涉及文件：`liveshow-proto/项目说明.md`
- 修改位置：`文件入口`及其后的来源说明。
- 差异分类：来源替代关系确认；不恢复旧文件，不修改 `context/` 需求正文和 `prototype/` 原型。
- 影响范围：Luma Live 全部生成前证据扫描；已删除文件不再形成扫描阻塞，生成时改读系统概要、当前需求清单、页面架构和原型证据。
- 受影响用例：既有工作簿不自动修改；本次用户 App 直播模块重新生成使用确认后的当前来源集合。

### 修改前

```text
项目说明的文件入口仍将以下已删除文件列为现行来源：
context/01-业务对象清单.md
context/01-用户主播App-角色与用例.md
context/01-互动场景权限规则.md
context/05-页面架构-01-一级页面定义.md

项目说明还将已删除的 context/20260716145838-业务需求沟通.txt 描述为早期追溯材料，但未明确其已删除状态。
```

### 修改后

```text
核心业务对象、关系、资产、状态与跨模块规则改由 context/系统概要 .md 承担。
用户/主播端规则继续读取 context/01-用户主播App-项目需求清单.md。
页面归属继续读取 context/01-用户主播App-页面架构.md。
公会端和管理后台规则继续读取各自当前需求清单。

明确 4 个旧需求文件已由系统概要、当前保留的项目需求清单及页面架构文件正式替代，不再作为生成或同步的必读来源。
明确已删除的早期业务需求沟通文件只保留历史追溯含义，不是当前生成来源。
```

### 直接依据

```text
用户于 2026-09-01 当前任务中确认：已删除的旧需求文件已由 context/系统概要 .md 和当前保留的需求文件正式替代。
liveshow-proto/需求来源策略.json 已登记当前三份派生需求清单，以及 context/01-用户主播App-页面架构.md、context/系统概要 .md 两份公共证据。
```

### 回退方法

1. 将 `liveshow-proto/项目说明.md` 的文件入口恢复为本条“修改前”所列的旧文件引用，并删除本次新增的替代关系说明。
2. 不恢复或删除任何 `context/`、`prototype/`、历史 Excel 和本条日志。
3. 回退后在 `需求清单变更日志.md` 追加引用 `RSL-0007` 的新记录，不得删除本条历史记录。

## RSL-0008 清理用户 App 页面架构中的已删除来源引用

- 修改时间：2026-09-01 16:49:16 CST
- 项目：Luma Live
- 来源策略：`prototype-primary`
- 修改原因：按 RSL-0007 建立替代关系后继续扫描，发现公共证据 `context/01-用户主播App-页面架构.md` 仍引用已删除的互动权限文件和角色用例文件。用户已确认这些旧文件由系统概要和当前保留需求文件正式替代。
- 涉及文件：`liveshow-proto/context/01-用户主播App-页面架构.md`
- 修改位置：`十、页面边界`第 125 至 126 行。
- 差异分类：修改 2 条来源引用；不改变页面结构、入口或业务规则正文。
- 影响范围：用户 App 页面架构的关系、权限和业务规则追溯；测试用例生成不再尝试读取已删除来源。
- 受影响用例：本次用户 App 直播模块全部用例重新建立来源追溯；业务预期不因本次引用修正而改变。

### 修改前

```text
- 关注、好友和粉丝团是不同关系；关系与权限规则见《01-互动场景权限规则》。
- 业务规则见《01-用户主播App-项目需求清单》《01-用户主播App-角色与用例》；本文件只维护页面归属和入口。
```

### 修改后

```text
- 关注、好友和粉丝团是不同关系；关系与权限规则见《系统概要》和《01-用户主播App-项目需求清单》。
- 业务规则见《系统概要》《01-用户主播App-项目需求清单》；本文件只维护页面归属和入口。
```

### 直接依据

```text
用户于 2026-09-01 当前任务中确认：已删除的旧需求文件已由 context/系统概要 .md 和当前保留的需求文件正式替代。
RSL-0007 已记录项目级替代关系；liveshow-proto/需求来源策略.json 已将系统概要和页面架构登记为公共证据。
```

### 回退方法

1. 将 `liveshow-proto/context/01-用户主播App-页面架构.md` 的两条页面边界说明恢复为本条“修改前”内容。
2. 不恢复或删除任何旧需求文件、原型、历史 Excel 和本条日志。
3. 回退后在 `需求清单变更日志.md` 追加引用 `RSL-0008` 的新记录，不得删除本条历史记录。
